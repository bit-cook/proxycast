name: Release

on:
  push:
    tags:
      - "v*"
    paths-ignore:
      - "docs/**"
      - "*.md"
      - ".github/workflows/deploy-docs.yml"
      - ".github/workflows/update-homebrew.yml"
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release (e.g., v0.1.0)"
        required: true
        default: "v0.1.0"
      source_ref:
        description: "Optional ref resolving to the same commit that triggered this workflow"
        required: false
        default: ""

permissions:
  contents: write
  packages: write
  actions: read
  id-token: write
  attestations: write

env:
  CARGO_NET_RETRY: 10
  RUSTUP_MAX_RETRIES: 10
  CARGO_TERM_COLOR: always

jobs:
  prepare_release:
    name: Prepare GitHub release
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.source_ref || github.ref }}

      - name: Validate release source identity
        shell: bash
        run: |
          set -euo pipefail
          CANDIDATE_SHA="$(git rev-parse 'HEAD^{commit}')"
          WORKFLOW_SHA="$(git rev-parse "${GITHUB_SHA}^{commit}")"
          if [ "$CANDIDATE_SHA" != "$WORKFLOW_SHA" ]; then
            echo "Checked-out source_ref must resolve to the workflow commit before a release can be created" >&2
            exit 1
          fi

      - name: Ensure GitHub release exists
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.version || github.ref_name }}"
          TARGET_REF="${{ github.sha }}"
          NOTES_FILE="$RUNNER_TEMP/release-notes.md"

          if [[ "$TAG" == *-* ]]; then
            echo "::error::Stable release does not accept prerelease tag: $TAG"
            exit 1
          fi

          if [ -f RELEASE_NOTES.md ]; then
            cp RELEASE_NOTES.md "$NOTES_FILE"
          else
            PREV_TAG="$(git tag --sort=-v:refname | grep -v "^${TAG}$" | head -n 1)"
            if [ -z "$PREV_TAG" ]; then
              CHANGELOG="$(git log --pretty=format:"- %s (%h)" "${TAG}" 2>/dev/null || git log --pretty=format:"- %s (%h)")"
            else
              CHANGELOG="$(git log --pretty=format:"- %s (%h)" "${PREV_TAG}..${TAG}" 2>/dev/null || git log --pretty=format:"- %s (%h)" "${PREV_TAG}..HEAD")"
            fi

            {
              echo "## Lime ${TAG}"
              echo
              echo "### Changes"
              echo "${CHANGELOG}"
            } > "$NOTES_FILE"
          fi

          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release edit "$TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --target "$TARGET_REF" \
              --title "Lime $TAG" \
              --notes-file "$NOTES_FILE"
            echo "Release $TAG already exists; notes refreshed"
            exit 0
          fi

          gh release create "$TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$TARGET_REF" \
            --title "Lime $TAG" \
            --draft \
            --notes-file "$NOTES_FILE"

  build:
    name: Build Electron ${{ matrix.name }}
    needs: prepare_release
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-15
            target: aarch64-apple-darwin
            host_platform: darwin
            arch: arm64
            feed: darwin-arm64
            forge_targets: dmg,zip
            name: macOS-arm64
          - platform: macos-15-intel
            target: x86_64-apple-darwin
            host_platform: darwin
            arch: x64
            feed: darwin-x64
            forge_targets: dmg,zip
            name: macOS-x64
          - platform: windows-2022
            target: x86_64-pc-windows-msvc
            host_platform: win32
            arch: x64
            feed: win32-x64
            forge_targets: squirrel
            name: Windows-x64

    runs-on: ${{ matrix.platform }}
    env:
      NODE_OPTIONS: --max-old-space-size=8192
      ELECTRON_CACHE: ${{ github.workspace }}/.cache/electron
      LIME_ELECTRON_UPDATES_URL: ${{ (secrets.LIME_UPDATES_BASE_URL || vars.LIME_UPDATES_BASE_URL || 'https://updates.limecloud.com') }}/lime/stable/${{ matrix.feed }}/

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Capture release candidate identity
        shell: bash
        run: |
          set -euo pipefail
          CANDIDATE_SHA="$(git rev-parse 'HEAD^{commit}')"
          WORKFLOW_SHA="$(git rev-parse "${GITHUB_SHA}^{commit}")"
          if [[ ! "$CANDIDATE_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            echo "Invalid release candidate commit SHA: $CANDIDATE_SHA" >&2
            exit 1
          fi
          if [ "$CANDIDATE_SHA" != "$WORKFLOW_SHA" ]; then
            echo "Checked-out source_ref must resolve to the workflow commit before provenance can be attested" >&2
            exit 1
          fi
          CANDIDATE_RUN_ID="release-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-${{ matrix.host_platform }}-${{ matrix.arch }}"
          echo "LIME_CANDIDATE_SHA=$CANDIDATE_SHA" >> "$GITHUB_ENV"
          echo "LIME_GATE_RUN_ID=$CANDIDATE_RUN_ID" >> "$GITHUB_ENV"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: "pnpm-lock.yaml"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          targets: ${{ matrix.target }}

      - name: Configure Windows MSVC linker
        if: matrix.host_platform == 'win32'
        shell: pwsh
        run: ./scripts/lib/windows-msvc-linker.ps1 -Target x86_64-pc-windows-msvc

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: lime-rs
          shared-key: "rust-${{ matrix.target }}"
          cache-on-failure: true
          cache-all-crates: true
          save-if: ${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/') }}

      - name: Refresh sandboxed rusty_v8 artifacts
        shell: bash
        run: |
          node scripts/lib/rusty-v8-artifacts.mjs --github-env
          cargo clean --manifest-path lime-rs/Cargo.toml -p v8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --config.node-linker=hoisted

      - name: Validate Electron macOS signing secrets
        if: startsWith(matrix.platform, 'macos')
        shell: bash
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          missing=()
          [ -n "${APPLE_CERTIFICATE:-}" ] || missing+=("APPLE_CERTIFICATE")
          [ -n "${APPLE_CERTIFICATE_PASSWORD:-}" ] || missing+=("APPLE_CERTIFICATE_PASSWORD")
          [ -n "${APPLE_ID:-}" ] || missing+=("APPLE_ID")
          [ -n "${APPLE_PASSWORD:-}" ] || missing+=("APPLE_PASSWORD")
          [ -n "${APPLE_SIGNING_IDENTITY:-}" ] || missing+=("APPLE_SIGNING_IDENTITY")
          [ -n "${APPLE_TEAM_ID:-}" ] || missing+=("APPLE_TEAM_ID")
          [ -n "${KEYCHAIN_PASSWORD:-}" ] || missing+=("KEYCHAIN_PASSWORD")
          if [ "${#missing[@]}" -gt 0 ]; then
            printf 'Missing Electron macOS signing/notarization secrets: %s\n' "${missing[*]}" >&2
            exit 1
          fi

      - name: Validate Electron Windows signing secrets
        if: matrix.host_platform == 'win32'
        shell: bash
        env:
          WINDOWS_SIGNING_CERTIFICATE: ${{ secrets.WINDOWS_SIGNING_CERTIFICATE }}
          WINDOWS_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_SIGNING_CERTIFICATE_PASSWORD }}
        run: |
          set -euo pipefail
          missing=()
          has_certificate=0
          has_password=0
          [ -z "${WINDOWS_SIGNING_CERTIFICATE:-}" ] || has_certificate=1
          [ -z "${WINDOWS_SIGNING_CERTIFICATE_PASSWORD:-}" ] || has_password=1
          if [ "$has_certificate" -eq 0 ] && [ "$has_password" -eq 0 ]; then
            echo "Electron Windows signing secrets are not configured; Forge Squirrel will produce unsigned installer assets."
            exit 0
          fi
          [ "$has_certificate" -eq 1 ] || missing+=("WINDOWS_SIGNING_CERTIFICATE")
          [ "$has_password" -eq 1 ] || missing+=("WINDOWS_SIGNING_CERTIFICATE_PASSWORD")
          if [ "${#missing[@]}" -gt 0 ]; then
            printf 'Incomplete Electron Windows signing secrets: %s\n' "${missing[*]}" >&2
            exit 1
          fi
          echo "Electron Windows signing secrets detected; Forge Squirrel signing will be enabled."

      - name: Import Electron macOS signing certificate
        if: startsWith(matrix.platform, 'macos')
        shell: bash
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          KEYCHAIN_PATH="$RUNNER_TEMP/lime-electron-signing.keychain-db"
          CERTIFICATE_PATH="$RUNNER_TEMP/lime-electron-signing.p12"
          echo "$APPLE_CERTIFICATE" | base64 --decode > "$CERTIFICATE_PATH"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          existing_keychains=()
          while IFS= read -r keychain; do
            [ -n "$keychain" ] || continue
            existing_keychains+=("$keychain")
          done < <(security list-keychains -d user | tr -d '"')
          security list-keychains -d user -s "$KEYCHAIN_PATH" "${existing_keychains[@]}"
          security import "$CERTIFICATE_PATH" \
            -P "$APPLE_CERTIFICATE_PASSWORD" \
            -A \
            -t cert \
            -f pkcs12 \
            -k "$KEYCHAIN_PATH"
          security set-key-partition-list \
            -S apple-tool:,apple:,codesign: \
            -s \
            -k "$KEYCHAIN_PASSWORD" \
            "$KEYCHAIN_PATH"
          echo "Available macOS signing identities in release keychain:"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"
          echo "LIME_MACOS_KEYCHAIN=$KEYCHAIN_PATH" >> "$GITHUB_ENV"

      - name: Prepare Electron Windows signing certificate
        if: matrix.host_platform == 'win32'
        shell: bash
        env:
          WINDOWS_SIGNING_CERTIFICATE: ${{ secrets.WINDOWS_SIGNING_CERTIFICATE }}
          WINDOWS_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_SIGNING_CERTIFICATE_PASSWORD }}
        run: |
          set -euo pipefail
          if [ -z "${WINDOWS_SIGNING_CERTIFICATE:-}" ] && [ -z "${WINDOWS_SIGNING_CERTIFICATE_PASSWORD:-}" ]; then
            echo "Skipping Electron Windows signing certificate preparation; no Windows signing secrets are configured."
            exit 0
          fi
          CERTIFICATE_PATH="$RUNNER_TEMP/lime-electron-signing.pfx"
          WINDOWS_SIGNING_CERTIFICATE_PATH="$CERTIFICATE_PATH" node - <<'NODE'
          const fs = require("node:fs");
          const certificate = process.env.WINDOWS_SIGNING_CERTIFICATE || "";
          const certificatePath = process.env.WINDOWS_SIGNING_CERTIFICATE_PATH;
          fs.writeFileSync(certificatePath, Buffer.from(certificate, "base64"));
          NODE
          echo "LIME_WINDOWS_SIGNING_CERTIFICATE_FILE=$CERTIFICATE_PATH" >> "$GITHUB_ENV"
          echo "LIME_WINDOWS_SIGNING_CERTIFICATE_PASSWORD=$WINDOWS_SIGNING_CERTIFICATE_PASSWORD" >> "$GITHUB_ENV"

      - name: Prepare sherpa-onnx runtime
        shell: bash
        run: |
          set -euo pipefail
          node scripts/prepare-sherpa-onnx-runtime.mjs \
            --target "${{ matrix.target }}"

      - name: Build Electron app
        shell: bash
        env:
          CARGO_PROFILE_RELEASE_LTO: "off"
          CARGO_PROFILE_RELEASE_CODEGEN_UNITS: 32
          CARGO_INCREMENTAL: 0
          CARGO_TARGET_DIR: ${{ github.workspace }}/lime-rs/target
          SCCACHE_GHA_ENABLED: "true"
          RUSTC_WRAPPER: sccache
          LIME_ELECTRON_SIGN: ${{ (startsWith(matrix.platform, 'macos') || matrix.host_platform == 'win32') && '1' || '' }}
          APPLE_SIGNING_IDENTITY: ${{ startsWith(matrix.platform, 'macos') && secrets.APPLE_SIGNING_IDENTITY || '' }}
          APPLE_ID: ${{ startsWith(matrix.platform, 'macos') && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ startsWith(matrix.platform, 'macos') && secrets.APPLE_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ startsWith(matrix.platform, 'macos') && secrets.APPLE_TEAM_ID || '' }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          npm run electron:build
          FORGE_PACKAGE_LOG="$RUNNER_TEMP/electron-forge-package-${{ matrix.host_platform }}-${{ matrix.arch }}.log"
          run_forge_package() {
            set +e
            npx electron-forge package \
              --platform "${{ matrix.host_platform }}" \
              --arch "${{ matrix.arch }}" 2>&1 | tee "$FORGE_PACKAGE_LOG"
            package_status=${PIPESTATUS[0]}
            set -e
          }
          is_transient_macos_package_error() {
            [ "${{ matrix.host_platform }}" = "darwin" ] || return 1
            grep -Eq "NSURLErrorDomain|The Internet connection appears to be offline|No network route|HTTPError\\(statusCode: nil|_NSURLErrorNWPathKey=unsatisfied|Response code 502 \\(Bad Gateway\\)|Response code 504 \\(Gateway Time-out\\)|A timestamp was expected but was not found|The timestamp service is not available" "$FORGE_PACKAGE_LOG"
          }
          package_attempt=1
          run_forge_package
          while [ "$package_status" -ne 0 ] && [ "$package_attempt" -lt 3 ] && is_transient_macos_package_error; do
            package_attempt=$((package_attempt + 1))
            echo "Electron Forge package hit a transient macOS packaging or notarization network error; retrying package attempt $package_attempt/3 after 60 seconds."
            sleep 60
            run_forge_package
          done
          if [ "$package_status" -ne 0 ]; then
            exit "$package_status"
          fi
          FORGE_MAKE_LOG="$RUNNER_TEMP/electron-forge-make-${{ matrix.host_platform }}-${{ matrix.arch }}.log"
          run_forge_make() {
            set +e
            npx electron-forge make \
              --skip-package \
              --platform "${{ matrix.host_platform }}" \
              --arch "${{ matrix.arch }}" \
              --targets "${{ matrix.forge_targets }}" 2>&1 | tee "$FORGE_MAKE_LOG"
            forge_status=${PIPESTATUS[0]}
            set -e
          }
          run_forge_make
          if [ "$forge_status" -ne 0 ] && [ "${{ matrix.host_platform }}" = "darwin" ] && grep -q "hdiutil detach /Volumes/Lime" "$FORGE_MAKE_LOG" && grep -q "No such file or directory" "$FORGE_MAKE_LOG"; then
            echo "Electron Forge DMG cleanup hit a missing /Volumes/Lime mount; retrying make once after best-effort cleanup."
            hdiutil detach "/Volumes/Lime" -force || true
            sleep 5
            run_forge_make
          fi
          echo "Electron Forge output files:"
          if [ -d release-electron ]; then
            find release-electron -type f | sort
          else
            echo "release-electron output directory is missing"
          fi
          if [ "${{ matrix.host_platform }}" = "darwin" ]; then
            APP_PATH="release-electron/Lime-darwin-${{ matrix.arch }}/Lime.app"
            if [ -d "$APP_PATH" ]; then
              codesign --display --verbose=4 "$APP_PATH" || true
              codesign --verify --deep --strict --verbose=4 "$APP_PATH" || true
            else
              find release-electron -name "*.app" -type d -print 2>/dev/null || true
            fi
          fi
          if [ "$forge_status" -ne 0 ]; then
            exit "$forge_status"
          fi
          make_assets=()
          if [ -d release-electron/make ]; then
            while IFS= read -r asset; do
              [ -n "$asset" ] || continue
              make_assets+=("$asset")
            done < <(find release-electron/make -type f \( -name "*.dmg" -o -name "*.zip" -o -name "*.exe" -o -name "*.nupkg" -o -name "RELEASES" -o -name "RELEASES.json" \) | sort)
          fi
          if [ "${#make_assets[@]}" -eq 0 ]; then
            echo "Electron Forge make produced no release assets" >&2
            find release-electron -maxdepth 5 -type f | sort
            exit 1
          fi
          printf 'Electron Forge make assets:\n'
          printf ' - %s\n' "${make_assets[@]}"

      - name: Verify Electron package resources
        shell: bash
        run: |
          set -euo pipefail
          manifest_count="$(find release-electron -name 'desktop-resources.manifest.json' -type f | wc -l | tr -d ' ')"
          if [ "$manifest_count" -eq 0 ]; then
            echo "Electron desktop resource manifest is missing from release-electron" >&2
            exit 1
          fi
          find release-electron -name 'desktop-resources.manifest.json' -type f -print
          node scripts/electron/verify-package-resources.mjs \
            --package-root release-electron \
            --platform "${{ matrix.host_platform }}" \
            --arch "${{ matrix.arch }}"

      - name: Run packaged macOS native host Gate B
        if: matrix.host_platform == 'darwin'
        shell: bash
        run: |
          set -euo pipefail
          APP_EXECUTABLE="release-electron/Lime-darwin-${{ matrix.arch }}/Lime.app/Contents/MacOS/Lime"
          test -x "$APP_EXECUTABLE"
          node scripts/electron/macos-native-host-gate-b.mjs \
            --electron-executable "$APP_EXECUTABLE" \
            --arch "${{ matrix.arch }}" \
            --candidate-sha "$LIME_CANDIDATE_SHA" \
            --run-id "$LIME_GATE_RUN_ID" \
            --release-trust \
            --evidence-dir ".lime/qc/gui-evidence/macos-native-host-gate-b-${{ matrix.arch }}"

      - name: Upload packaged macOS native host Gate B evidence
        if: ${{ always() && matrix.host_platform == 'darwin' }}
        uses: actions/upload-artifact@v4
        with:
          name: macos-native-host-gate-b-evidence-${{ matrix.arch }}
          path: .lime/qc/gui-evidence/macos-native-host-gate-b-${{ matrix.arch }}
          if-no-files-found: warn
          retention-days: 7

      - name: Stage Electron release assets
        shell: bash
        env:
          RELEASE_TAG: ${{ github.event.inputs.version || github.ref_name }}
        run: |
          set -euo pipefail
          node scripts/electron/stage-release-assets.mjs \
            --forge-dir release-electron \
            --out-dir "release-assets/${{ matrix.target }}" \
            --target-triple "${{ matrix.target }}" \
            --version "$RELEASE_TAG"
          find "release-assets/${{ matrix.target }}" -maxdepth 1 -type f | sort

      - name: Download Windows N-1 Squirrel installer
        if: matrix.host_platform == 'win32'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          N_MINUS_ONE_VERSION="$({
            CANDIDATE_VERSION="$VERSION" node --input-type=module <<'NODE'
          import { execFileSync } from "node:child_process";
          import { selectNMinusOneVersion } from "./scripts/electron/windows-squirrel-rc-smoke.mjs";

          const tags = execFileSync("git", ["tag", "--list"], {
            encoding: "utf8",
          }).split(/\r?\n/);
          process.stdout.write(
            selectNMinusOneVersion({
              candidateVersion: process.env.CANDIDATE_VERSION,
              tags,
            }),
          );
          NODE
          })"
          N_MINUS_ONE_TAG="v$N_MINUS_ONE_VERSION"
          mkdir -p windows-n-minus-one-package
          gh release download "$N_MINUS_ONE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --pattern "Lime-$N_MINUS_ONE_VERSION.Setup.exe" \
            --dir windows-n-minus-one-package
          test -f "windows-n-minus-one-package/Lime-$N_MINUS_ONE_VERSION.Setup.exe"
          echo "N_MINUS_ONE_VERSION=$N_MINUS_ONE_VERSION" >> "$GITHUB_ENV"

      - name: Smoke installed Windows Squirrel candidate
        if: matrix.host_platform == 'win32'
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          node scripts/electron/windows-squirrel-rc-smoke.mjs \
            --installer-dir "release-assets/${{ matrix.target }}" \
            --candidate-feed-dir "release-assets/${{ matrix.target }}" \
            --evidence-dir ".lime/qc/windows-squirrel-rc/$VERSION" \
            --n-minus-one-installer-dir windows-n-minus-one-package \
            --n-minus-one-version "$N_MINUS_ONE_VERSION" \
            --version "$VERSION" \
            --candidate-sha "$LIME_CANDIDATE_SHA" \
            --run-id "$LIME_GATE_RUN_ID"

      - name: Run installed Windows CodeMode Gate B
        if: matrix.host_platform == 'win32'
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          CODE_MODE_GATE_EXECUTABLE="$(node -e "const fs=require('node:fs');const summary=JSON.parse(fs.readFileSync('.lime/qc/windows-squirrel-rc/$VERSION/summary.json','utf8'));const executable=summary.evidence?.installation?.executable;if(!executable)throw new Error('Windows Squirrel evidence is missing the installed executable');process.stdout.write(executable);")"
          CANDIDATE_RUN_ID="$(node -e "const fs=require('node:fs');const summary=JSON.parse(fs.readFileSync('.lime/qc/windows-squirrel-rc/$VERSION/summary.json','utf8'));if(!summary.candidateRunId)throw new Error('Windows Squirrel evidence is missing candidateRunId');process.stdout.write(summary.candidateRunId);")"
          export LIME_GATE_RUN_ID="$CANDIDATE_RUN_ID"
          node scripts/agent-runtime/code-mode-electron-gate-b.mjs \
            --electron-executable "$CODE_MODE_GATE_EXECUTABLE" \
            --output ".lime/qc/gui-evidence/code-mode-electron-gate-b-windows/code-mode-electron-gate-b-summary.json" \
            --timeout-ms 600000

      - name: Run installed Windows native host Gate B
        if: matrix.host_platform == 'win32'
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          NATIVE_HOST_GATE_EXECUTABLE="$(node -e "const fs=require('node:fs');const summary=JSON.parse(fs.readFileSync('.lime/qc/windows-squirrel-rc/$VERSION/summary.json','utf8'));const executable=summary.evidence?.installation?.executable;if(!executable)throw new Error('Windows Squirrel evidence is missing the installed executable');process.stdout.write(executable);")"
          CANDIDATE_RUN_ID="$(node -e "const fs=require('node:fs');const summary=JSON.parse(fs.readFileSync('.lime/qc/windows-squirrel-rc/$VERSION/summary.json','utf8'));if(!summary.candidateRunId)throw new Error('Windows Squirrel evidence is missing candidateRunId');process.stdout.write(summary.candidateRunId);")"
          export LIME_GATE_RUN_ID="$CANDIDATE_RUN_ID"
          node scripts/electron/windows-native-host-gate-b.mjs \
            --electron-executable "$NATIVE_HOST_GATE_EXECUTABLE" \
            --evidence-dir ".lime/qc/gui-evidence/windows-native-host-gate-b" \
            --timeout-ms 120000

      - name: Validate Windows packaged Gate B evidence identity
        if: ${{ always() && matrix.host_platform == 'win32' }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          node scripts/electron/windows-packaged-evidence.mjs \
            --version "$VERSION" \
            --candidate-sha "$LIME_CANDIDATE_SHA" \
            --squirrel-summary ".lime/qc/windows-squirrel-rc/$VERSION/summary.json" \
            --code-mode-summary ".lime/qc/gui-evidence/code-mode-electron-gate-b-windows/code-mode-electron-gate-b-summary.json" \
            --native-host-summary ".lime/qc/gui-evidence/windows-native-host-gate-b/summary.json" \
            --output ".lime/qc/gui-evidence/windows-packaged-evidence/$VERSION/summary.json"

      - name: Uninstall Windows Squirrel candidate
        if: ${{ always() && matrix.host_platform == 'win32' }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          node scripts/electron/windows-squirrel-rc-smoke.mjs \
            --cleanup-summary ".lime/qc/windows-squirrel-rc/$VERSION/summary.json"

      - name: Upload Windows Squirrel RC evidence
        if: ${{ always() && matrix.host_platform == 'win32' }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-squirrel-rc-evidence-${{ matrix.target }}
          path: .lime/qc/windows-squirrel-rc
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Windows CodeMode Gate B evidence
        if: ${{ always() && matrix.host_platform == 'win32' }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-code-mode-gate-b-evidence-${{ matrix.target }}
          path: .lime/qc/gui-evidence/code-mode-electron-gate-b-windows
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Windows native host Gate B evidence
        if: ${{ always() && matrix.host_platform == 'win32' }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-native-host-gate-b-evidence-${{ matrix.target }}
          path: .lime/qc/gui-evidence/windows-native-host-gate-b
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Windows packaged Gate B evidence identity
        if: ${{ always() && matrix.host_platform == 'win32' }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-packaged-evidence-${{ matrix.target }}
          path: .lime/qc/gui-evidence/windows-packaged-evidence
          if-no-files-found: warn
          retention-days: 7

      - name: Upload staged release assets artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-assets-${{ matrix.target }}
          path: release-assets
          if-no-files-found: error
          retention-days: 7

      - name: Show sccache stats
        if: always()
        run: sccache --show-stats

  publish_release_assets:
    name: Publish Electron release assets
    needs: build
    if: needs.build.result == 'success'
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Download staged release assets
        uses: actions/download-artifact@v4
        with:
          pattern: release-assets-*
          path: release-assets
          merge-multiple: true

      - name: Inspect Electron release assets
        shell: bash
        run: |
          set -euo pipefail
          assets=()
          while IFS= read -r asset; do
            [ -n "$asset" ] || continue
            assets+=("$asset")
          done < <(find "release-assets" -type f | sort)
          if [ "${#assets[@]}" -eq 0 ]; then
            echo "No staged Electron release assets found" >&2
            exit 1
          fi
          printf 'Downloaded Electron release assets:\n'
          printf ' - %s\n' "${assets[@]}"
          if find "release-assets" -type f \( -name "*.app.tar.gz" -o -name "*.sig" -o -name "latest.json" -o -name "latest.yml" -o -name "latest-mac.yml" -o -name "*.blockmap" \) | grep -q .; then
            echo "::error::Legacy updater assets must not be published by the Electron release workflow"
            find "release-assets" -type f \( -name "*.app.tar.gz" -o -name "*.sig" -o -name "latest.json" -o -name "latest.yml" -o -name "latest-mac.yml" -o -name "*.blockmap" \) | sort
            exit 1
          fi

      - name: Prepare GitHub release upload assets
        env:
          RELEASE_TAG: ${{ github.event.inputs.version || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          node scripts/electron/prepare-github-release-assets.mjs \
            --assets-dir release-assets \
            --out-dir release-github-assets \
            --version "$RELEASE_TAG"

      - name: Attest Electron release provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0
        with:
          subject-path: release-github-assets/*

      - name: Upload Electron assets to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.version || github.ref_name }}"
          assets=()
          while IFS= read -r -d '' asset; do
            assets+=("$asset")
          done < <(find "release-github-assets" -type f -print0 | sort -z)
          if [ "${#assets[@]}" -eq 0 ]; then
            echo "No prepared GitHub release assets found" >&2
            exit 1
          fi
          printf 'Uploading GitHub release assets:\n'
          printf ' - %s\n' "${assets[@]}"
          gh release upload "$TAG" "${assets[@]}" \
            --repo "$GITHUB_REPOSITORY" \
            --clobber

      - name: Publish GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.inputs.version || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          gh release edit "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --draft=false \
            --latest

  publish_updater_assets_r2:
    name: Publish Electron updater assets to Cloudflare R2
    needs: publish_release_assets
    if: needs.publish_release_assets.result == 'success'
    continue-on-error: true
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Download staged release assets
        uses: actions/download-artifact@v4
        with:
          pattern: release-assets-*
          path: release-assets
          merge-multiple: true

      - name: Upload Electron updater assets to Cloudflare R2
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          LIME_RELEASES_R2_BUCKET: ${{ secrets.LIME_RELEASES_R2_BUCKET }}
          RELEASE_TAG: ${{ github.event.inputs.version || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          BUCKET="${LIME_RELEASES_R2_BUCKET:-lime-releases}"
          PLAN="$RUNNER_TEMP/electron-r2-upload-plan.json"
          node scripts/electron/update-feed-r2-upload-plan.mjs \
            --assets-dir release-assets \
            --bucket "$BUCKET" \
            --channel stable \
            --version "$RELEASE_TAG" \
            --output "$PLAN"

          node - "$PLAN" <<'NODE' > "$RUNNER_TEMP/electron-r2-upload.sh"
          const fs = require('fs');
          const plan = JSON.parse(fs.readFileSync(process.argv[2], 'utf8'));
          for (const item of plan) {
            const args = [
              'npx wrangler@latest r2 object put',
              JSON.stringify(`${item.bucket}/${item.key}`),
              '--remote',
              '--file',
              JSON.stringify(item.file),
              '--content-type',
              JSON.stringify(item.contentType),
              '--cache-control',
              JSON.stringify(item.cacheControl),
            ];
            console.log(args.join(' '));
          }
          NODE
          bash "$RUNNER_TEMP/electron-r2-upload.sh"

      - name: Clean old updater assets from Cloudflare R2
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          LIME_MINIMUM_SUPPORTED_VERSION: ${{ secrets.LIME_MINIMUM_SUPPORTED_VERSION }}
          LIME_R2_KEEP_RELEASES: ${{ vars.LIME_R2_KEEP_RELEASES }}
          LIME_RELEASES_R2_BUCKET: ${{ secrets.LIME_RELEASES_R2_BUCKET }}
          RELEASE_TAG: ${{ github.event.inputs.version || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          BUCKET="${LIME_RELEASES_R2_BUCKET:-lime-releases}"
          KEEP="${LIME_R2_KEEP_RELEASES:-3}"
          if ! npx wrangler@latest r2 object --help | grep -q "object list"; then
            echo "::warning::wrangler r2 object list is unavailable; skipping old R2 updater cleanup"
            exit 0
          fi
          npx wrangler@latest r2 object list "$BUCKET" \
            --remote \
            --prefix "lime/stable/v" \
            --json > "$RUNNER_TEMP/r2-objects.json"
          node scripts/electron/r2-release-cleanup-plan.mjs \
            --input "$RUNNER_TEMP/r2-objects.json" \
            --output "$RUNNER_TEMP/r2-delete-keys.txt" \
            --current "$RELEASE_TAG" \
            --keep "$KEEP" \
            --minimum "${LIME_MINIMUM_SUPPORTED_VERSION:-}" \
            --channel stable
          if [ ! -s "$RUNNER_TEMP/r2-delete-keys.txt" ]; then
            echo "No old R2 updater assets to delete"
            exit 0
          fi
          printf 'Deleting old R2 updater assets:\n'
          cat "$RUNNER_TEMP/r2-delete-keys.txt"
          while IFS= read -r key; do
            [ -n "$key" ] || continue
            npx wrangler@latest r2 object delete "${BUCKET}/${key}" --remote
          done < "$RUNNER_TEMP/r2-delete-keys.txt"

  publish_cli_assets:
    name: Build CLI npm ${{ matrix.npm_tag }}
    needs: publish_release_assets
    if: needs.publish_release_assets.result == 'success'
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            package: lime-darwin-arm64
            npm_tag: darwin-arm64
            name: macOS-arm64-cli
          - platform: macos-15-intel
            target: x86_64-apple-darwin
            package: lime-darwin-x64
            npm_tag: darwin-x64
            name: macOS-x64-cli
          - platform: windows-2022
            target: x86_64-pc-windows-msvc
            package: lime-win32-x64
            npm_tag: win32-x64
            name: Windows-x64-cli
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            package: lime-linux-x64
            npm_tag: linux-x64
            name: Linux-x64-cli

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup Python for npm packaging
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Linux CLI build dependencies
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libasound2-dev libxdo-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          targets: ${{ matrix.target }}

      - name: Configure Windows MSVC linker
        if: startsWith(matrix.target, 'x86_64-pc-windows-msvc')
        shell: pwsh
        run: ./scripts/lib/windows-msvc-linker.ps1 -Target x86_64-pc-windows-msvc

      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.9

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: lime-rs
          shared-key: "cli-${{ matrix.target }}"
          cache-on-failure: true
          cache-all-crates: true
          save-if: ${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/') }}

      - name: Refresh sandboxed rusty_v8 artifacts
        shell: bash
        run: |
          node scripts/lib/rusty-v8-artifacts.mjs --github-env
          # The CLI payload is built with the release profile. Clear the matching
          # target-specific V8 fingerprint so cached build-script output cannot
          # reference a missing gn_out static archive.
          CARGO_TARGET_DIR="${{ github.workspace }}/lime-rs/target" \
            cargo clean \
              --manifest-path lime-rs/Cargo.toml \
              --package v8 \
              --release \
              --target "${{ matrix.target }}"

      - name: Prepare CLI sherpa-onnx runtime
        if: matrix.target != 'x86_64-unknown-linux-gnu'
        shell: bash
        run: |
          set -euo pipefail
          node scripts/prepare-sherpa-onnx-runtime.mjs \
            --target "${{ matrix.target }}"

      - name: Build CLI runtime payload
        shell: bash
        env:
          CARGO_PROFILE_RELEASE_LTO: "off"
          CARGO_PROFILE_RELEASE_CODEGEN_UNITS: 32
          CARGO_INCREMENTAL: 0
          CARGO_TARGET_DIR: ${{ github.workspace }}/lime-rs/target
          SCCACHE_GHA_ENABLED: "true"
          RUSTC_WRAPPER: sccache
        run: |
          set -euxo pipefail
          cargo build --manifest-path lime-rs/Cargo.toml \
            -p cli --bin lime --release --target "${{ matrix.target }}"
          cargo build --manifest-path lime-rs/Cargo.toml \
            -p app-server --bin app-server --release --target "${{ matrix.target }}"
          cargo build --manifest-path lime-rs/Cargo.toml \
            -p code-mode-host --bin code-mode-host --release --target "${{ matrix.target }}"
          if [ "${{ matrix.target }}" = "x86_64-pc-windows-msvc" ]; then
            cargo build --manifest-path lime-rs/Cargo.toml \
              -p tool-runtime \
              --bin windows-sandbox-setup \
              --bin windows-sandbox-runner \
              --release \
              --target "${{ matrix.target }}"
          fi

      - name: Finalize CLI runtime libraries
        if: matrix.target != 'x86_64-unknown-linux-gnu'
        shell: bash
        run: |
          set -euo pipefail
          node scripts/prepare-sherpa-onnx-runtime.mjs \
            --target "${{ matrix.target }}"

      - name: Package CLI npm platform tarball
        id: package_cli
        shell: bash
        run: |
          set -euxo pipefail
          VERSION="${{ github.event.inputs.version || github.ref_name }}"
          VERSION="${VERSION#v}"
          PROFILE_DIR="lime-rs/target/${{ matrix.target }}/release"
          VENDOR_ROOT="$RUNNER_TEMP/lime-cli-vendor"
          BIN_DIR="$VENDOR_ROOT/${{ matrix.target }}/bin"
          mkdir -p "$BIN_DIR" "packages/cli/dist"

          EXE_SUFFIX=""
          if [ "${{ matrix.target }}" = "x86_64-pc-windows-msvc" ]; then
            EXE_SUFFIX=".exe"
          fi
          cp "$PROFILE_DIR/lime${EXE_SUFFIX}" "$BIN_DIR/"
          cp "$PROFILE_DIR/app-server${EXE_SUFFIX}" "$BIN_DIR/"
          cp "$PROFILE_DIR/code-mode-host${EXE_SUFFIX}" "$BIN_DIR/"
          if [ -n "$EXE_SUFFIX" ]; then
            cp "$PROFILE_DIR/windows-sandbox-setup.exe" "$BIN_DIR/"
            cp "$PROFILE_DIR/windows-sandbox-runner.exe" "$BIN_DIR/"
          fi
          while IFS= read -r runtime_library; do
            [ -n "$runtime_library" ] || continue
            cp -L "$runtime_library" "$BIN_DIR/"
          done < <(
            find "$PROFILE_DIR" -maxdepth 1 \
              \( -type f -o -type l \) \
              \( -name '*.dylib' -o -name '*.dll' -o -name '*.so' -o -name '*.so.*' \) \
              -print | sort
          )

          ASSET_PATH="packages/cli/dist/lime-npm-${{ matrix.npm_tag }}-${VERSION}.tgz"
          python3 packages/cli/scripts/build_npm_package.py \
            --package "${{ matrix.package }}" \
            --release-version "$VERSION" \
            --vendor-src "$VENDOR_ROOT" \
            --pack-output "$ASSET_PATH"
          test -f "$ASSET_PATH"
          echo "asset_path=$ASSET_PATH" >> "$GITHUB_OUTPUT"

      - name: Upload CLI npm tarball to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.inputs.version || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          ASSET_PATH="${{ steps.package_cli.outputs.asset_path }}"
          if [ -z "$ASSET_PATH" ] || [ ! -f "$ASSET_PATH" ]; then
            echo "CLI asset missing: $ASSET_PATH" >&2
            exit 1
          fi

          gh release upload "$RELEASE_TAG" "$ASSET_PATH" \
            --repo "$GITHUB_REPOSITORY" \
            --clobber

      - name: Upload CLI npm tarball artifact
        uses: actions/upload-artifact@v4
        with:
          name: cli-npm-${{ matrix.npm_tag }}
          path: ${{ steps.package_cli.outputs.asset_path }}
          if-no-files-found: error
          retention-days: 7

      - name: Show sccache stats
        if: always()
        run: sccache --show-stats

  publish_cli_npm:
    name: Publish CLI npm packages
    needs: publish_cli_assets
    if: needs.publish_cli_assets.result == 'success'
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Setup Node.js for npm trusted publishing
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          scope: "@limecloud"

      - name: Setup Python for npm packaging
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Download CLI npm platform tarballs
        uses: actions/download-artifact@v4
        with:
          pattern: cli-npm-*
          path: dist/npm
          merge-multiple: true

      - name: Stage CLI npm root package
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.inputs.version || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${RELEASE_TAG#v}"
          ROOT_TARBALL="dist/npm/lime-npm-${VERSION}.tgz"
          python3 packages/cli/scripts/build_npm_package.py \
            --package lime \
            --release-version "$VERSION" \
            --pack-output "$ROOT_TARBALL"
          test -f "$ROOT_TARBALL"
          gh release upload "$RELEASE_TAG" "$ROOT_TARBALL" \
            --repo "$GITHUB_REPOSITORY" \
            --clobber

      - name: Publish CLI npm packages platform-first
        env:
          VERSION: ${{ github.event.inputs.version || github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${VERSION#v}"
          ROOT_TARBALL="dist/npm/lime-npm-${VERSION}.tgz"
          PLATFORM_TARBALLS=(
            "dist/npm/lime-npm-linux-x64-${VERSION}.tgz"
            "dist/npm/lime-npm-darwin-x64-${VERSION}.tgz"
            "dist/npm/lime-npm-darwin-arm64-${VERSION}.tgz"
            "dist/npm/lime-npm-win32-x64-${VERSION}.tgz"
          )

          for required_tarball in "${PLATFORM_TARBALLS[@]}" "$ROOT_TARBALL"; do
            if [ ! -f "$required_tarball" ]; then
              echo "Missing npm tarball: $required_tarball" >&2
              exit 1
            fi
          done

          publish_tarball() {
            local tarball="$1"
            shift
            set +e
            output="$(npm publish "$tarball" --access public --provenance "$@" 2>&1)"
            status=$?
            set -e
            echo "$output"
            if [ "$status" -eq 0 ]; then
              return
            fi
            if grep -qiE "previously published|cannot publish over|version already exists" <<< "$output"; then
              echo "Skipping already-published package version: $tarball"
              return
            fi
            return "$status"
          }

          publish_tarball "${PLATFORM_TARBALLS[0]}" --tag linux-x64
          publish_tarball "${PLATFORM_TARBALLS[1]}" --tag darwin-x64
          publish_tarball "${PLATFORM_TARBALLS[2]}" --tag darwin-arm64
          publish_tarball "${PLATFORM_TARBALLS[3]}" --tag win32-x64
          publish_tarball "$ROOT_TARBALL"
