# Codex Desktop 完整对齐执行计划

> status: `stopped-with-known-gaps`（2026-08-27 按用户要求结束长时间续跑）
> owner: root + 各领域 current owner
> upstream: `/Users/coso/Documents/dev/rust/codex`（当前观测 HEAD：`99660ab3c7b861c916e467581fa9b8723504d66b`）
> checklist progress: `45/46 (97.8%)`；这是未加权任务数，不等于交付完成度。live provider/current chain 与非空 patch 已取得真实证据；唯一剩余项仍包含 Pier `reward=1` artifacts 与覆盖当前 SHA 的正式分发硬门槛。

> 结束裁决（2026-08-27）：真实 run `20260827T060242Z-oxvg-structural-selector-preservation` 已在 `custom-79f1f42e-8172-45a3-bb6b-b9afd2d76df5 / agnes-2.5-flash` 上完成 current chain：`30` 个 provider steps、预算 `321972/500000`、无 step/token exhaustion，并生成 `49,660` bytes patch（SHA-256 `67c027a8c220a387e417c40f01bf49a6d09963d5f8ac288f51323a05a62bb981`，候选 HEAD `ef46e91c81a869ee48717b569dde11f856e4e464`）。Pier `0.3.1` 和 Colima daemon 前置检查通过，但 verifier 子进程没有发现隔离 Docker Compose plugin，`docker compose --project-name ...` 以 `unknown flag: --project-name` 失败，未生成 `reward.json`、`ctrf.json` 或 `test-stdout.txt`；这不是候选测试失败，也不能记为 verifier 通过。覆盖当前 SHA 的 notarized/正式 package 同样未执行。本计划停止继续重跑并保持 `45/46`，不得宣称 `100%`。

> 当前主目标（2026-08-26）：关闭 E 阶段 live provider/Pier/DeepSWE verifier 与当前 SHA 正式分发证据。C-3 已由 Windows Quality run `32975574520`（SHA `19e08daa2`，job `98199506057`，artifact `9609510419`）关闭；readiness 提升提交 SHA `438e8e4eb` 又由 run `32978840712`、job `98210288038`、artifact `9610757889` 独立复验：setup 为 `firewall_rules=3`、`wfp_filters=14`，schema v3 七项矩阵 `7/7`，offline identity 与 loopback TCP 阻断真实通过。manifest/resource verifier 已显式覆盖 runner 缺失和 digest 漂移 fail closed，Windows backend capability 保持 `Ready/enforced=true`，实际 setup/read-back 失败仍返回 `UpdateRequired`。
> 参考原则：`/Users/coso/Documents/dev/rust/codex` 是协议、Thread/Turn/Item、runtime lifecycle、tool/approval 和 GUI 语义的直接实现参考源；Lime 只在产品范围、provider 和桌面宿主边界做显式适配，不以旧矩阵或同名 legacy API 代替对齐。
> 目标：把 Lime 的产品主链、协议字段、GUI 工作区、执行安全和真实 Desktop 证据推进到当前 Codex Desktop 语义；不恢复已退役 runtime、v0 或 `agentSession/*` 兼容主链。

## 1. 事实源与当前阶段

当前阶段：D-1 provider capability、A-Queue、A-Project、Strict Review、D-2 MCP event stream/resource origin、D-3 Environment lifecycle、Thread Revert、Thread/Item current 字段/恢复复核、Browser/Computer Use requirements 产品裁决与 C-3 Windows restricted execution 均已完成。Queue 公共 JSON-RPC 5/5、Project 4/4、Revert 5/5、MCP 3/3、Environment 3/3、Environment remote Thread/Turn 1/1、Strict Review 1/1、provider capability 1/1 已使用本机缓存 V8 artifact 通过真实公共入口；MCP runtime generation 8/8、MCP owner 166/166、MCP Agent bridge 20/20、filesystem gateway 4/4、provider rollback prefix 1/1、remote process transport 1/1 与 remote reconnect 1/1 也已通过。Codex 当前 HEAD 的 245 个方向化 method identity 已完成三态裁决：168 current、3 Windows planned、74 product-scope-excluded；Bedrock account discover/setup 与 `configRequirements/read` 已明确 excluded。Browser task intent 和 `ElectronBrowserTabHost` turn-scoped approval 保持 Lime current owner，不复制 Codex MDM/Computer Use policy。MCP status Renderer/smoke consumer 已迁到 `{data,nextCursor}`，auth status 已对齐 exact `notLoggedIn/bearerToken/oAuth`；`codex_apps` 已从冻结 tool route 生成 canonical `appContext`，以最近 64 条、单条 1024 bytes 的边界从 Thread 历史恢复 resource authority，并在读取时重新校验 URI、connector、link/account 与当前 MCP tool metadata。独立 MCP resource Gate B 已通过两次真实 Electron/App Server 启动，证明同一 canonical Item/appContext 冷恢复、WebContentsView resource 可见、伪造 connector 被忽略、URI 不匹配 fail closed，且恢复过程不重跑 provider/tool。macOS arm64 本地 packaged candidate、`1.135.0` 正式 Developer ID/notarization/stapling、DMG/ZIP 发布、Windows Squirrel N-1 packaged Gate B 与 C-3 Windows schema v3 `7/7` 已有真实证据。live provider/current chain 与非空 patch 已取得真实证据；当前唯一剩余 checklist 是 Pier/DeepSWE verifier `reward=1` artifacts 和覆盖当前 SHA 的正式 package，不以 method 百分比、受控 fixture、旧基线发布或 ad-hoc 签名冒充这些证据。

> 最新 D-2 状态（2026-08-24）：active ready barrier、terminated forwarding、Thread archive/delete/unload/connection cleanup、有限 runtime-generation reconnect 与旧 generation shutdown 已落地；公共 App Server JSON-RPC 3/3、MCP runtime generation 8/8、MCP owner 162/162 已在缓存 V8 环境实际运行。Settings MCP Runtime 已由 `useMcpEvents` 消费 typed `active/event/terminated` notification，并通过 `McpEventStreamStatus` 展示订阅 ID、事件数、重连次数和终态；五语言、组件回归和独立 Electron Gate B 已关闭。Gate B 证据见 `.lime/qc/gui-evidence/mcp-event-stream-electron-gate-b/mcp-event-stream-electron-gate-b-summary.json`，不调用模型回合或 live provider。

> 最新 D-2 resource 状态（2026-08-25）：canonical `appContext` producer、bounded provenance、link/account authority、cold restore、Workspace consumer 与独立 Electron Gate B 均已关闭。证据见 `.lime/qc/gui-evidence/mcp-resource-origin-electron-gate-b/mcp-resource-origin-electron-gate-b-summary.json`；proof level 是 controlled Gate B，不证明 live provider、remote connector、compaction、packaged app 或 Windows。

> 最新 GUI 聚合门禁状态（2026-08-25）：图片 Turn 的 Token 用量 recent replay 已收敛为“图片消息缺 usage 且对应 Turn 已终态”时才补读，避免活动 Turn 过早消费唯一 replay；hook 单元回归 17/17、图片单场景 Gate B、完整 `smoke:agent-runtime-current-fixture` 与 Shell-01 `verify:gui-smoke` 21/21 均通过。Expert Plaza -> Expert Panel Skills follow-up 同轮也已回归通过。该证据仍是 controlled Gate B，不证明 live provider 或 packaged app。

> 最新正式分发状态（2026-08-26）：同一基线 SHA `0975674a` 的 GitHub Release run `32864956267` 已成功完成 `1.135.0` macOS arm64/x64 与 Windows x64 发布。macOS arm64 日志明确记录 hardened runtime、Developer ID authority、timestamp、`Notarization Ticket=stapled`、`codesign --verify --deep --strict` 成功和资源 verifier 通过；正式 DMG/ZIP/RELEASES 已发布。Windows artifact `windows-squirrel-rc-evidence-x86_64-pc-windows-msvc`（ID `9570576514`）证明 `1.134.0 -> 1.135.0` Squirrel N-1 下载/安装、快捷方式、安装目录权限和安装后真实 Electron Shell-01 21/21，summary SHA-256 为 `f18b45169f11dfebf750627f480d03f7f6f7f1c763f5902fc4ee1ceafae320b5`。这些证据关闭该基线正式 macOS 分发与 Windows packaged/Squirrel 缺口，但不包含当前 C-3/readiness SHA，也不替代 live provider/verifier 或覆盖当前 SHA 的正式分发。

> 最新当前工作树 packaged 状态（2026-08-26）：从未提交工作树构建 `1.135.0` arm64 Developer ID candidate，输出目录为 `.tmp/codex-alignment-packaged-current-20260826/Lime-darwin-arm64/Lime.app`。精确 SHA-1 identity `DE71B9673ECED29A0DCA5D11CC498B072C7AA860` 对应 `Developer ID Application: Digital Easy Inc. (93RF7VQ26C)`；`codesign --verify --deep --strict`、仓库 `verify-package-resources.mjs --package-root .tmp/codex-alignment-packaged-current-20260826 --platform darwin --arch arm64` 均通过，主包 hardened runtime、TeamIdentifier `93RF7VQ26C`、timestamp 与 designated requirement 有效。包内 executable 直启 `verify:gui-smoke` 通过 Shell-01 21/21：`electron-ipc`、`app_server_handle_json_lines`、App Server `appserver.v0` 初始化均命中，56 次 App Server IPC，console/page/invoke/legacy/mock/crash/unresponsive 均为 0。该证据证明当前工作树的 signed packaged candidate 与真实 Electron Shell-01，不证明 Apple notarization/stapling、DMG/ZIP/正式发布、Windows、live provider 或 verifier。

唯一产品链：

```text
Renderer typed gateway
  -> Electron Desktop Host / preload（仅宿主能力与 JSONL 转发）
  -> App Server JSON-RPC
  -> RuntimeCore / model-provider / tool-runtime
  -> Thread / Turn / Item projection + durable store
  -> Codex App-style GUI
```

当前基线：

- Lime 已推送 HEAD：`438e8e4eb16a3ebf39d2eec40db04fc9111aa20b`
- Codex HEAD：`99660ab3c7b861c916e467581fa9b8723504d66b`
- method product-scope matrix 已锁定当前 Codex HEAD：245 个方向化 identity，`171 implemented / 0 planned / 74 product-scope-excluded`；method 分类不等于字段、GUI、恢复或平台全面完成。Windows setup/warning lifecycle 已收敛为 current，但仍只由真实 Windows runner evidence 支撑。
- 当前工作树已有浏览器/Electron/benchmark 并行改动。本计划首轮只写本计划、协议事实源和协议 owner 文件，避让这些热区。

### 当前上游漂移收口（2026-08-25）

当前 Codex 工作树为 `99660ab3c7b861c916e467581fa9b8723504d66b`，相对历史矩阵锁定版本新增的能力必须先完成产品裁决和 owner 映射：

| 上游增量                                                                                              | 当前结论                                                                                                                                                                                                                                                                                                                                                                                                | 下一步                                                                                                                                                |
| ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `thread/queue/{add,list,update,delete,reorder,start}`、`thread/queue/changed`                         | Lime current owner 已建立：RuntimeCore durable queue、App Server JSON-RPC、GUI 只读 projection；owner、公共 JSON-RPC 与独立 Electron Gate B 已运行通过；状态条只保留标题/数量/读取或错误状态，canonical 时间线唯一承载 submission 正文                                                                                                                                                                  | current 完成；重复正文视觉层级已收口，后续只随 Codex Queue contract 漂移复核                                                                          |
| `project/{list,read,create,import,update,move,delete}`、`project/changed`、`thread/project/updated`   | Lime current owner 已建立：App Server Project SQLite repository、UUIDv7/opaque cursor、幂等键、排序、Thread assignment 与 connection-scoped experimental gate；公共 JSON-RPC fixture、GUI consumer 与独立 Electron Gate B 已运行通过                                                                                                                                                                    | current 完成；后续只随 Codex Project contract 漂移复核，不用 workspace API 建立第二套 owner                                                           |
| `thread/revert`、`thread/reverted`                                                                    | Lime current owner 已建立：exact v2 contract/schema/generated types、Thread 独占串行 scope、RuntimeCore append-only `history.rollback` replacement、provider/read-model/cold hydration effective stream、metadata-only response 与 notification；共享 GUI action、replace read-model refresh 和独立 Electron Gate B 已完成；旧 `thread/rollback` deprecated                                             | current 完成；后续只随 Codex contract 漂移复核，不恢复 deprecated `thread/rollback` 或本地文件回滚语义                                                |
| `autoApprovalReview/strictReviewRequired`                                                             | Lime current owner 已建立：Guardian strict-review signal、exact DTO/schema/client、`StrictReviewStatus` GUI consumer 与 connection-scoped experimental gate；真实双连接 `run_json_lines` 1/1 已通过                                                                                                                                                                                                     | current 完成；独立 Electron Gate B、五语言和组件回归已关闭，不用普通 warning 代替                                                                     |
| `account/bedrock/{discover,setup}`                                                                    | `product-scope-excluded / forbidden-to-restore`：只服务 Codex account-auth；Lime 的 `amazon-bedrock` 继续由 provider-neutral `model-provider` credential/config owner 承接                                                                                                                                                                                                                              | 裁决完成；不得新增同名 account API、Electron facade 或 compat wrapper                                                                                 |
| `mcpServerStatus/list` v2 分页、`runtimeStatus`，以及 `mcpServer/resource/read` origin/connector 字段 | v2 protocol/App Server owner 已存在；Renderer 与 current/live/Electron smoke 已只读 `{data,nextCursor}` 并拒绝旧 `{servers}`；auth status exact casing/lowering 已完成；MCP App 传 canonical origin identity；服务端从 bounded canonical history 恢复 `appContext`，校验 completed Item、URI、connector、link/account 和当前 tool authority，并把 canonical connector/link 写入真实 MCP request `_meta` | current 完成；独立 Electron Gate B 已证明首轮与冷恢复 authority 稳定、伪造 connector 无效、URI mismatch fail closed，后续只随 Codex contract 漂移复核 |
| Browser/Computer Use requirements/config 与 realtime existing-call                                    | Browser/embedded host 和模型能力已有 Lime 专属 owner；Codex realtime 仍 product-scope-excluded                                                                                                                                                                                                                                                                                                          | 只补 requirements 的边界审计，不复制 Codex TUI/realtime 或建立第二套 computer-use config                                                              |

### GUI 产品面与 Gate B 分类（2026-08-24）

| 能力                  | 当前 Renderer 事实                                                                                                                                                                                                           | 当前缺口与退出条件                                                                                                                                                                             |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Thread Queue          | `ThreadQueueStatus` + `useAgentSessionThreadQueue` 已消费 `thread/queue/list` 与 `thread/queue/changed`，稳定 DOM 为 `thread-queue-status`；状态条只显示标题、数量、读取或错误状态，canonical 时间线唯一承载 submission 正文 | 独立 Electron Gate B 已完成：创建 Thread、添加 submission、从 GUI 打开同一 Thread，marker 在时间线唯一可见，状态条不含正文且无 `thread-queue-items`；组件/Gate B/真实 Electron 回归已通过      |
| Provider capability   | `ModelSelector` + `ModelProviderCapabilityBadges` 已消费 `modelProvider/capabilities/read`，稳定 DOM 为 `model-selector-provider-capability-panel`                                                                           | 已完成独立 Electron Gate B：真实打开 ModelSelector，三项 capability 可见，且 current method 命中 1 次、mock/error 为 0                                                                         |
| Project               | `ThreadProjectSelector` + `useThreadProjectDirectory` 已消费 Project 目录、Thread 归属与 typed notification；普通对话和 Task Center 页头均有稳定入口                                                                         | 独立 Electron Gate B 已完成：GUI 从 Alpha 切换到 Beta，`thread/metadata/update` 与 cold `thread/read` identity 一致，mock/console/page/invoke error 为 0                                       |
| Thread Revert         | `ThreadRevertTrigger` + `ThreadRevertDialog` 已在 canonical/fallback 消息投影复用同一 action；typed `thread/revert` 成功后以 replace 模式刷新 canonical read model                                                           | 五语言、稳定 DOM、失败保留历史、Thread identity 防漂移和独立 Electron Gate B 已完成；证据见 `.lime/qc/gui-evidence/thread-revert-electron-gate-b/`                                             |
| Strict Review         | `StrictReviewStatus` 已在现有 Composer/approval surface 消费 exact `autoApprovalReview/strictReviewRequired`，稳定 DOM 与下一步状态已建立                                                                                    | 已完成五语言、组件回归和独立 Electron Gate B；证据见 `.lime/qc/gui-evidence/strict-review-electron-gate-b/`，不冒充 live provider 或真实危险命令                                               |
| MCP event stream      | `McpEventStreamStatus` 已在现有 MCP Runtime 明细投影 typed `active/event/terminated`，稳定 DOM 含 subscription ID/phase，重连次数由同一 projection 计算                                                                      | 已完成五语言、组件回归和独立 Electron Gate B；证据见 `.lime/qc/gui-evidence/mcp-event-stream-electron-gate-b/`，不调用模型回合或 live provider                                                 |
| MCP resource origin   | Workspace MCP App 从 canonical Item 的 `appContext`/resource URI 建立 WebContentsView surface，并以 `originCallId` 请求同一 Thread authority；`codex_apps` 不依赖本地 Plugin 安装态                                          | 已完成 canonical producer/cold restore、bounded provenance、link/account revalidation、稳定组件回归和独立 Electron Gate B；证据见 `.lime/qc/gui-evidence/mcp-resource-origin-electron-gate-b/` |
| Environment lifecycle | `TaskCenterEnvironmentPanel` + `useThreadEnvironmentLifecycleStatus` 已消费 `environment/status` 与 typed connected/disconnected notification，稳定 DOM 覆盖环境 identity/status                                             | 已完成五语言、组件回归和独立 Electron Gate B：真实远端 Environment 断线与有限重连在同一 canonical Thread 可见，mock/console/page/invoke error 为 0                                             |

当前执行顺序：Provider capability、Queue（含重复正文视觉收口）、Project、Strict Review、D-2 MCP event stream/resource origin、D-3 Environment、Thread Revert、Browser/Computer Use requirements 产品裁决、Thread/Item current 字段/恢复复核、Agent current 聚合门禁、本机 macOS packaged candidate、`1.135.0` 正式 macOS 分发、Windows Squirrel N-1 packaged Gate B 和 C-3 Windows restricted execution 已关闭。C-3 的 restricted-token、ACL/Job、ConPTY、elevated setup、offline/online account、Firewall/WFP、readiness read-back、runner packaging guard 与 schema v3 `7/7` 已形成同一 current 链；当前只剩 live provider/Pier/DeepSWE verifier 和覆盖当前 SHA 的正式分发。每完成一项即更新本计划与运行证据，不批量提前勾选。

> 最新 Queue/CI 收口状态（2026-08-26）：Queue 状态条已删除 submission preview 与 `thread-queue-items` 正文列表，canonical 时间线成为唯一正文 owner；五语言只保留标题、loading、数量和刷新失败状态。组件、Gate B 与 Sherpa 定向回归 25/25，`npm run i18n:check` 保持 100%。真实 Queue Gate B 已通过：`timelineMarkerCount=1`、`queueStatusContainsMarker=false`、`queueItemsPresent=false`、47 次真实 Electron IPC、`mockFallbackHitCount=0`、console/page/invoke error 均为 0，证据见 `.lime/qc/gui-evidence/thread-queue-electron-gate-b/thread-queue-electron-gate-b-summary.json`。根 `package.json`/lock importer 已声明直接 `ws@8.18.3`，修复 Quality CI Frontend Full 的 `Failed to resolve import "ws"`；Windows Shell Runtime 上限调为 60 分钟，restricted security matrix 已前移到 Sherpa/sidecar 重型阶段之前，Sherpa 清理/解压前后输出阶段日志。`smoke:agent-runtime-current-fixture` 完整通过，`npm run verify:gui-smoke` Shell-01 21/21、contracts 299 checks、typecheck、scripts/legacy governance 均通过；App Server Windows sandbox fail-closed 定向单测 4/4 通过。上述本机测试不替代 Windows restricted matrix 或 live verifier；正式 macOS 与 Squirrel 证据由独立 Release run `32864956267` 证明。

## 2. 交付分层

### P0：Thread 工作区产品面

- active Thread 成为唯一主画布对象，首页首屏优先新建任务。
- canonical timeline 只渲染一次 User/Agent/Reasoning/Tool/Approval/Plan/FileChange/SubAgent/Compaction。
- Composer 统一 send、steer、interrupt、queue、approval、request-user-input。
- 直接迁移 `agentSession/*` 主写链到 `thread/*`、`turn/*`，旧入口只允许 retired guard。
- Thread 生命周期形成 fork、resume、archive/unarchive、revert、steer、interrupt 的 GUI 闭环。

### P1：协议与数据合同

- 保持当前 Codex HEAD 的 method/notification/product-scope matrix 可重现；revision 漂移时先做集合差和产品裁决。
- 对 queue、project、revert、MCP event stream、strict review、Windows setup/warning 做逐项产品裁决。
- 补 Thread/Item 字段 parity：`projectId`、typed source/status/path/cwd/threadSource，以及 delivery、plugin/script、readOnlyHint、typed reasoning effort。
- 验证 `experimental_api` 对 experimental methods/notifications 的 gating 行为；没有行为证据就不能宣称 parity。

### P1：Coding execution 与平台安全

- command/test 默认路径切到统一 sandbox-aware process owner，补实时输出、stdin、interrupt、terminate 和 Workbench 控制。
- 完成 Windows/MSVC、restricted token、workspace write、外部路径拒绝、ACL rollback、进程树终止和大输出证据。
- 补 session-scope approval key、sandbox denied 安全升级重试和规则草案。

### P1：模型、MCP、Environment、Multi-Agent

- 为当前 Codex freeform 模型补 canonical mapping、capability、readiness、model switch Electron Gate B。
- 补 MCP deferred tool expose/recover/resume、event stream 和独立 Gate B。
- 补 Environment instructions、world-state durable full/patch history、typed context-budget terminal。
- 补 Multi-Agent spawn/wait/followup/interrupt 的完整 Electron identity evidence。

### P2：证据与治理

- 受控 smoke 只能证明 product path；补 live provider、verifier artifact、patch match、Pier/DeepSWE evidence 后才能宣称 Desktop coding pass。
- 同步 current/compat/deprecated/dead 分类，修正旧 snapshot 与最新 Gate B 证据冲突。

## 3. 方法分类原则

| 分类                     | 规则                                                                                                                                                                    |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `current`                | exact method/field、current owner、真实消费链和对应 evidence 都存在。                                                                                                   |
| `needs-re-audit`         | 上游当前存在，但 Lime 产品范围、GUI 消费或生命周期尚未裁决。                                                                                                            |
| `product-scope-excluded` | Codex account/commerce、remote control、realtime、marketplace/share、external-agent migration 等不属于 Lime Desktop；Environment exec-server 已进入 D-3 current scope。 |
| `deprecated`             | Codex 已明确 deprecated，例如 `thread/rollback`；不得恢复。                                                                                                             |
| `dead`                   | Lime 旧 `agentSession/*`、v0 wrapper、mock fallback 等已由 current owner 替代的路径。                                                                                   |

## 4. 阶段、写集与退出条件

### A：当前协议矩阵与 Thread/Item contract

写集：`internal/refactor/v1/11-codex-method-product-scope-matrix.md`、协议 fixture、`lime-rs/crates/app-server-protocol/src/protocol/v2/**`、对应 generated contract tests。

退出条件：矩阵 upstream revision 更新；每个新增 method 有分类、owner、evidence；Thread/Item schema diff 有正向和负向测试；`npm run test:contracts` 通过。

当前子进度：

- [x] Item wire 字段对齐：`delivery`、`pluginId`、`scriptPath`、`readOnlyHint`、typed `reasoningEffort`，并从 canonical metadata 投影。
- [x] Thread wire 字段对齐：必需 `status`、必需可空 `projectId`、typed `SessionSource`/`ThreadSource`、`PathBuf` `path/cwd`、`gitInfo`/`historyMode`/`canAcceptDirectInput` 投影。
- [x] Rust schema fixture 与 `packages/app-server-client` generated protocol types 同步。
- [x] Thread/Item round-trip 与 projection 正向断言；`app-server-protocol` 123 tests、schema fixture test、`app-server-client` 34 tests、`npm run test:contracts` 通过。
- [x] current Codex `99660ab3` method matrix 已刷新为 245 个方向化 identity：168 current、3 Windows planned、74 excluded；新增 22 项 current identity 均有同名 manifest，Bedrock account 2 项有禁止回流守卫。
- [x] Browser/Computer Use requirements 产品裁决：`configRequirements/read` 及其 Browser/Computer Use MDM 字段保持 `product-scope-excluded`；Lime `browser_requirement` 仅为 Turn intent，`ElectronBrowserTabHost` 的 turn-scoped approval/permission/human takeover 是唯一 current owner，不新增第二 policy/config surface，并补 manifest 负向回流守卫。
- [x] Thread/Item current 字段与恢复复核：`projectId`、typed source/status/path/cwd、delivery、plugin/script、readOnlyHint、reasoning effort、`agentNickname`、`agentRole` 均已有 schema/projection/canonical cold read owner；本轮 Rust related integration 继续覆盖 Thread read/fork/resume/revert/search 与 AgentControl 恢复链。
- [x] App Server projection crate 定向测试：通过 `scripts/lib/rusty-v8-artifacts.mjs` 注入本机缓存 archive/binding 后，`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server processor::thread::projection::tests` 20/20 通过。

### A-Queue：Thread durable queue

写集：`lime-rs/crates/app-server/src/runtime/thread_queue.rs`、`runtime/tests/thread_queue.rs`、`processor/thread_queue.rs`、`tests/thread_queue_jsonrpc.rs`、Queue v2 protocol/schema/generated types，以及 `src/lib/api/agentRuntime/threadQueueClient.ts` 和 GUI Queue status consumer。

当前子进度：

- [x] Codex exact `thread/queue/{add,list,update,delete,reorder,start}` DTO、method catalog、schema、generated TypeScript 与 connection-scoped `experimentalApi` gate。
- [x] RuntimeCore durable owner：FIFO 顺序、重复 client ID、每 Thread 100 条容量、分页默认 25/最大 100、输入 sidecar、多模态 cold hydration、active/interrupted fail-closed 与 Completed/Failed 自动继续。
- [x] App Server processor 与 `thread/queue/changed` projection；GUI 通过 typed list gateway 和通知订阅消费 current Queue projection，不恢复旧 snapshot 写平面。
- [x] Rust RuntimeCore 单测覆盖 CRUD/reorder、重复 client ID、cold restart、多模态、active/interrupted 和 FIFO。
- [x] RuntimeCore/App Server 生命周期回归已写入：archive 后禁止 Queue 读写、unarchive 恢复 durable queue、delete 清理 session/sidecar/event state、cold persisted queue 在 `thread/resume` 后唤醒；Interrupted 语义保持暂停，显式 `thread/queue/start` 仍可启动。
- [x] 公共 JSON-RPC fixture 5/5 通过，覆盖 experimental gate、CRUD/分页/通知/FIFO、中断后显式恢复、多模态 cold restart 和 cold `thread/resume` 自动派发；cold resume 复用 admission 后后台执行/event hub 路径，真实连接可收到 `turn/completed`。
- [x] 独立 Electron Gate B 已完成：真实 preload setup 创建 canonical Thread 并添加 Queue submission，GUI safeInvoke trace 命中同 Thread 的 `thread/read` 与 `thread/queue/list`，`thread-queue-status`、header 和 marker 可见；mock/error 为 0。证据见 `.lime/qc/gui-evidence/thread-queue-electron-gate-b/thread-queue-electron-gate-b-summary.json`。该证据不启动 Turn、不调用模型。

### A-Project：Project durable owner 与 Thread assignment

写集：`app-server-protocol` Project v2 contract、`app-server` Project processor / canonical SQLite owner / public JSON-RPC fixture，以及 Thread `projectId` start/list/metadata integration；不新增 workspace compat API。

当前子进度：

- [x] Codex exact `project/{list,read,create,import,update,move,delete}`、`project/changed`、`thread/project/updated` DTO、method/envelope、schema、generated TypeScript 与 connection-scoped `experimentalApi` gate。
- [x] Project SQLite current owner：UUIDv7 identity、opaque cursor、position、roots、metadata、时间戳、create/import 幂等、move/no-op/anchor 校验和 delete assignment cleanup。
- [x] Thread 集成：`thread/start.projectId`、`thread/list.projectId` omitted/null/value 三态、`thread/metadata/update.projectId`，assignment 持久化到 canonical `thread.metadata.projectId`；active/archived Thread 均由同一 owner 处理。
- [x] Owner 单测 3/3 通过，覆盖 CRUD/分页/幂等/排序、import 事务原子性、active/archived assignment、delete cleanup 和损坏 metadata fail-closed。
- [x] 公共 `run_json_lines` JSON-RPC fixture 4/4 通过，覆盖方法/字段门禁、双连接通知隔离、CRUD/分页/排序、Thread assignment、import 通知顺序与非法输入拒绝。
- [x] 独立 Electron Gate B 已完成：真实 Electron 中从侧栏打开 canonical Thread，Project 目录恢复初始归属并切换到新 Project；`project/create|list`、`thread/start|read|metadata/update` 均经 `electron-ipc -> app_server_handle_json_lines`，cold readback 与 GUI identity 一致，mock/console/page/invoke error 为 0。

### B：Thread workspace GUI

写集：仅由 GUI owner 接管 `AgentChatWorkspace`、Thread header、timeline registry、Composer/action-required 相关文件；不得在本阶段新开协议 method。

退出条件：固定桌面/窄宽视口下 Thread、状态、下一步和变更位置可识别；同一 Item 不在多个 surface 重复成为主操作；`npm run verify:gui-smoke` 与真实 Electron Gate B 通过。

当前子进度：

- [x] Thread header 优先消费 canonical `thread/read.status`，Topic 状态只作缺失兜底。
- [x] Thread header 保留 `canAcceptDirectInput` 事实字段，并补 canonical status/direct-input 回归断言。
- [x] Thread header 接入现有 `thread/setName` current 链，提供本地化重命名菜单和回归测试。
- [x] Thread header action menu 通过 `AgentRuntimeAdapter -> useAgentSession -> workspace owner` 接入 `thread/archive`；归档成功后清理 active read model 并回到首页。
- [x] Thread header action menu 通过同一 current owner 接入 `thread/fork`；校验新 Thread identity，fork 成功后复用现有 `switchTopic` 打开新 Thread，保留原 Thread。
- [x] `thread/resume` 已由 `AgentRuntimeAdapter.resumeThread`、stream recovery 和 session hydration 消费；上游 `thread/rollback` 明确 deprecated，不恢复为 GUI 操作。
- [x] current `thread/revert` 已由用户消息旁的共享 GUI action 消费；确认态说明 Thread/文件保留，typed request 成功后以 replace 模式刷新 canonical read model，失败保留历史。五语言、稳定 DOM、组件回归与独立 Electron Gate B 已关闭，证据见 `.lime/qc/gui-evidence/thread-revert-electron-gate-b/thread-revert-electron-gate-b-summary.json`。
- [x] 真实 Electron Gate B 已取得：Shell-01 GUI smoke 21/21 assertions；session-history fixture 覆盖 archive/unarchive、restart readback、resume；Claw fixture 覆盖真实 renderer/preload/App Server bridge、输入发送、`turn.completed` 和 read model 完成态。证据见 `.lime/qc/project-gates/codex-alignment-gate-b-20260823/shell-01-electron-smoke/summary.json`、`.lime/qc/gui-evidence/agent-session-history-electron-fixture/agent-session-history-electron-fixture-summary.json`、`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-summary.json`。
- [x] Agent current 聚合门禁最新回归已关闭图片 Token 用量与 Expert Panel Skills follow-up：图片场景保持单任务卡、单媒体、completed read model，并恢复可见 Token usage；完整 fixture 覆盖 active Turn steer、approval/cancel/continue、Plan、MCP、media/content factory、Skills Runtime 与 Expert Plaza。证据见 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-image-command-replay-fix-summary.json`、`.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-expert-panel-skills-runtime-regression-summary.json`。

### C：执行、审批和 Windows

写集：`lime-rs/crates/tool-runtime/src/execution_process/**`、agent execution owner、approval policy owner、对应 App Server contract 和测试；Windows 平台文件必须由同一 owner 交接。

退出条件：command/test 走统一 process owner；审批复用/拒绝重试语义可恢复；Windows/MSVC 和真机 evidence 覆盖 workspace、外部路径、ACL、终止、大输出；未验证时保持 fail-closed。

当前子进度：

- [x] C-1 已确认 `command/exec` 与 standalone `process/*` 归 connection-scoped App Server + `tool-runtime::execution_process` current owner；实时 raw-byte delta、stdin/PTY、interrupt、terminate、timeout、bounded output、跨连接隔离、响应前 notification barrier 和断连清理均已有 owner 与回归覆盖，不新增第二套 process supervisor。
- [x] C-1 unified exec lifecycle slice（2026-09-06）：`tool-runtime::unified_exec` 将同一 `ExecutionProcessSnapshot` 的 process/status/stdin/bounded-output facts 写入 canonical result；App Server `CodingEventMirror` 消费 `Command` Item 的 `ItemStarted -> ItemUpdated -> ItemCompleted`，投影统一的 `command.started/output/exited` 与 `test.started/completed`，并复用现有 `ExecutionProcessServer`，不新增 JSON-RPC 或并行 supervisor。统一 exec 与 sandbox-aware `RuntimeLiveExecutionGateway` 的 owner 方向已对齐；定向 Cargo 测试因本机 `rusty_v8 v150.4.0` Darwin arm64 archive HTTP 404 未执行成功。
- [x] C-2 已接入 session-scoped shell approval cache：key 绑定 command、cwd、requested sandbox policy 和 requested permissions；仅 `exec_command` 支持 `allow_for_session`，browser/guardian 不复用 shell cache；provider session close 清理内存 cache，且 cache 命中使用 `RuntimeToolApprovalSource::Reused` 跳过重复用户审批。
- [x] C-2 sandbox-denied retry 已由统一 orchestrator 承接：按 approval policy 判断是否允许 danger-full-access 重试；普通 user approval 复用于 sandbox retry，strict guardian 重新审核，拒绝和 cancel fail closed；新增 Cached 零审批、key 隔离、shell/browser contract 回归测试。
- [x] C-3 Windows 安全证据入口已落到 current `tool-runtime::execution_process` owner：新增 `windows_restricted_execution` integration test，并接入 `.github/workflows/quality.yml` 的 Windows runner；测试矩阵覆盖 workspace write、`.git/.codex/.agents` 与外部路径拒绝、allowlisted stdin handle、bounded large output、Job Object descendant cleanup/terminate。
- [x] C-3 setup/warning producer 与 Renderer consumer 已落到 current App Server/tool-runtime owner：`readWindowsSandboxReadiness`、`startWindowsSandboxSetup`、`windowsSandbox/setupCompleted`、`windows/worldWritableWarning` 均有 typed API、schema 校验、事件订阅、Elevated/Unelevated 操作和五种 locale 文案；`setupStart` 异步执行 bounded Everyone-write ACL audit，发现 sample、额外路径或扫描失败时发出 warning，setup completion 不直接提升 readiness，仍以重新读取的真实状态为准。Windows API 编译与真机 enforcement 已由 run `32975574520` 关闭。
- [x] C-3 Windows packaged/Squirrel evidence：GitHub Release run `32864956267` 的 Windows-x64 job `97858018518` 完成 MSVC sidecar/Forge package、资源 verifier、正式 release staging 和真实 `1.134.0 -> 1.135.0` Squirrel N-1 更新。artifact `9570576514` 的 L8 summary 为 16/16，安装后的 `Lime.exe` Shell-01 为 21/21、45 次真实 `app_server_handle_json_lines`、零 mock/legacy/console/page/invoke error；summary SHA-256 为 `f18b45169f11dfebf750627f480d03f7f6f7f1c763f5902fc4ee1ceafae320b5`。
- [x] C-3 当前工作树 macOS signed packaged candidate：Developer ID identity、主/Helper bundle identity、`codesign --verify --deep --strict`、资源 verifier 与包内 executable Shell-01 21/21 已通过；证据目录 `.tmp/codex-alignment-packaged-current-20260826/` 与 `.lime/qc/project-gates/standalone-shell-01-20260825171233-4797/shell-01-electron-smoke/summary.json`。该证据只证明当前候选包，不能替代 notarization/stapling 或正式发布。
- [x] C-3 Windows restricted execution 真机 evidence：Codex `windows-sandbox-rs` 对齐的 restricted-token、sandbox account user SID、capability SID、TokenDefaultDacl、ACL lease/rollback、STARTUPINFOEX handle allowlist、Job Object descendant cleanup、bounded pipe reader与 restricted ConPTY 已进入 current owner；account user SID 只进入 restricting SID access check，不进入默认 DACL 充当 capability。elevated setup helper、offline/online local account、machine-scope DPAPI artifacts、3 条 per-user Firewall rule 与 14 条 WFP filter 均由 readiness 全量 read-back。Quality run `32975574520`（SHA `19e08daa2`，job `98199506057`，artifact `9609510419`）在 clean Windows/MSVC runner 完成 setup，并以 schema v3 `7/7` 证明 workspace write、metadata/external deny、offline identity/network block、large output、stdin、ConPTY、Everyone-write audit 与 terminate/job cleanup；matrix `missing/failed/ignored/unexpected` 均为空。Electron release manifest 生成 `windowsSandboxRunnerSha256`，resource verifier 对 runner 缺失和 digest 漂移显式 fail closed，定向打包回归 `32/32`。production capability 现为 `SandboxBackendStatus::Ready/enforced=true`；App Server 仍先验证真实 setup artifacts/accounts/Firewall/WFP，任何缺失或漂移返回 `UpdateRequired`。

  已删除替换路径：真实 Windows run `32947104967`（SHA `22b4262aa`）证明宿主 `CreateProcessAsUserW` 缺少 privilege，`CreateProcessWithTokenW` 无法可靠承接 pipe/ConPTY；这两条 fallback 归类为 `dead / forbidden-to-restore`。current 结构固定为宿主使用 DPAPI 凭证和 `CreateProcessWithLogonW` 启动 sandbox-account runner，runner 从自己的 primary token 派生 restricted token，再以 `CreateProcessAsUserW + STARTUPINFOEX` 原子附着 Job/handle list/ConPTY；有界 named-pipe framed IPC 承接 spawn、stdin/close/resize/terminate、output、exit/error。runner 仍属于 `tool-runtime::execution_process` 唯一 owner，Electron 只把其可执行文件与 App Server 资源一起打包。

本轮 C-3 窄写集（已关闭）：`lime-rs/crates/tool-runtime/{Cargo.toml,src/bin/windows-sandbox-runner.rs,src/execution_process/windows*.rs,tests/windows_restricted_execution.rs}`、`lime-rs/crates/app-server-daemon/src/lib.rs`、`packages/app-server-client/src/sidecar-types.ts`、`scripts/check-app-server-client-contract.mjs`、`scripts/prepare-sherpa-onnx-runtime.test.mjs`、`scripts/lib/{electron-app-server-assets*,electron-dev-sidecar*,windows-restricted-execution-evidence*}`、`scripts/electron/{prepare-app-server-assets.mjs,verify-package-resources*}`、`.github/workflows/quality.yml`、本计划与 `internal/aiprompts/{architecture,quality-workflow}.md`。退出条件已满足：宿主不直接为 sandbox account token 启动用户命令；runner pipe ACL/PID/framing/cleanup 有界；restricted token、Job、handle list/ConPTY、stdin/resize/terminate、output、ACL lease 与 network isolation 已通过 clean Windows `7/7`；Windows manifest/resource verifier 包含 runner 并对缺失/digest 漂移 fail closed。

架构影响：重大；Windows restricted execution 从宿主直接 token spawn 改为同一 `tool-runtime` owner 下的 sandbox-account runner sidecar，且 Windows Electron package 增加必需 runtime resource。

架构图已更新：`internal/aiprompts/architecture.md#42-windows-sandbox-readiness-owner`。

责任开发者确认：coso，2026-08-26。

确认内容：已核对目录归属、数据流、依赖方向、协议边界和验证门禁。

### D：模型/MCP/Environment/Multi-Agent evidence

写集：各自 current domain owner，不跨 owner 复制 catalog、snapshot 或 GUI 状态。

退出条件：每个能力有 provider request、cold read/recovery、Electron Gate B 或明确 product-scope exclusion；不存在 mock fallback。

当前子进度：

- [x] D-1 Codex `modelProvider/capabilities/read` 已在 Lime v2 protocol、App Server dispatch、schema、generated/typed client、`modelRegistry` facade 与 ModelSelector GUI consumer 建立 exact owner；App Server handler 复用 `model-provider::provider_capabilities`，并从当前 `RuntimeCore` 注入的 config route 读取 provider，未知/配置失败 fail closed。公共 JSON-RPC official Responses/兼容网关路由 1/1 通过；独立 Electron Gate B 以隔离 official OpenAI Responses route 从真实首页打开 ModelSelector，GUI 显示 `namespaceTools=false`、`imageGeneration=true`、`webSearch=true`，trace 命中 `electron-ipc -> app_server_handle_json_lines -> modelProvider/capabilities/read` 1 次，console/page/invoke/mock error 均为 0。证据见 `.lime/qc/gui-evidence/model-provider-capabilities-electron-gate-b/model-provider-capabilities-electron-gate-b-summary.json`；该证据不调用模型，不冒充 live provider readiness。
- [x] D-2 Codex MCP experimental event stream / deferred tool recovery 已完成 current owner、GUI consumer 与 Electron evidence。Codex current v2 有 `mcpServer/event/stream/start`、`mcpServer/event/stream/stop`、`mcpServer/event/stream/notification`，并以 `experimental_api` gating；Lime 已补齐 exact DTO、method/envelope、schema fixture、generated TypeScript、protocol round-trip 和 connection-scoped capability gate。当前实现已将 `rmcp` custom `events/stream` 请求和 server notification 接入 session-owned `McpThreadRuntime`，App Server 按 `(connectionId, subscriptionId)` 持有真实转发 task，start 等待 `notifications/events/active` ready barrier，Thread/runtime/transport 结束转发 `notifications/events/terminated`，并在有限窗口内尝试 runtime-generation reconnect；archive/delete/unload/connection close 会统一清理 owner task，启动失败和未知 server、重复 subscription、非当前连接均 fail closed。真实 `run_json_lines` + LocalAppDataSource stdio fixture 已运行 3/3；`runtime_state::mcp_runtime_tests` 8/8、`lime-mcp --lib` 162/162 通过，覆盖旧 generation shutdown、失败保留前代、并发 ensure 和断线恢复。Settings MCP Runtime 通过 `useMcpEvents` + `McpEventStreamStatus` 消费 active/event/terminated lifecycle，五语言与组件回归通过；独立 Electron Gate B 使用 `runtime` backend、真实 provider route preflight、Thread-owned stdio MCP 与 preload/IPC/App Server JSON-RPC，active/event/terminated、重连态和终态可见，`app_server_handle_json_lines` 49 次、`app_server_drain_events` 8 次、Electron IPC 57 次，mock/legacy/invoke/console/page error 均为 0。证据见 `.lime/qc/gui-evidence/mcp-event-stream-electron-gate-b/mcp-event-stream-electron-gate-b-summary.json`；该证据不调用模型回合或 live provider。远端已发送 terminated 时不再追加 synthetic terminated，避免重复通知。Lime v2 当前已有 MCP status/elicitation/tool-call 主体，`mcpTool/listForContext`、`mcpTool/search` 仍落在 v0/legacy surface，不能把它们当作 v2 parity。
- [x] D-2 MCP status/resource 已完成。`mcpServerStatus/list` Renderer gateway 遍历 opaque cursor，只接受 `{data,nextCursor}` 并投影 `runtimeStatus/pluginId/authStatus`；旧 `{servers}` 只保留负向回流测试。auth wire 已对齐 exact `notLoggedIn/bearerToken/oAuth`，OAuth `available=false` 投影 `NotLoggedIn`。Workspace MCP App 从 canonical Item 的 `appContext`/resource URI 建立 surface，并以 `originCallId` 请求同一 Thread authority；`codex_apps` 不再依赖本地 Plugin 安装态。MCP tool lifecycle 从冻结 route 生成 canonical `connectorId/linkId/resourceUri/appName/actionName`，materializer、read model 与 history merge 保持其冷恢复；origin ledger 最多 64 条、单条 1024 bytes。资源读取要求 completed canonical Item、URI 一致，并重新校验当前 tool connector/link authority；account ambiguity、缺失必需 link、authority drift 和 Renderer 伪造 connector 均 fail closed，真实 MCP wire 使用 canonical `threadId/selected_connector_ids/link_id`。公共 App Server JSON-RPC 3/3、canonical projection/cold materialization/lifecycle emitter 各 1/1、`agent-protocol --lib` 43/43、`thread-store --lib` 38/38、MCP runtime generation 8/8、MCP owner 166/166、MCP Agent bridge 20/20、公共 `mcp_exact_jsonrpc` 3/3、Workspace/Gate B Vitest、contracts、current smoke、typecheck 与脚本治理均通过。独立 Gate B 两次启动真实 Electron/App Server，canonical Item `item_call-mcp-resource-origin` 与 `appContext` 冷恢复稳定，WebContentsView marker 可见，三次 resource wire 均使用 `calendar/link-calendar`，伪造 Renderer connector 被忽略、URI mismatch fail closed，冷恢复未重跑 provider/tool，mock/legacy/invoke/console/page error 均为 0。证据见 `.lime/qc/gui-evidence/mcp-resource-origin-electron-gate-b/mcp-resource-origin-electron-gate-b-summary.json`；该 controlled fixture 不证明 live provider、remote connector、compaction、packaged app 或 Windows。
- [x] D-3 Codex current Environment `environment/add|info|status` 已完成。Lime 已新增 exact DTO、v2 method/envelope/catalog、schema fixtures、generated TypeScript、protocol round-trip、connection-scoped `experimentalApi` gating、typed App Server client facade、公共 `run_json_lines` fixture，以及 `thread/environment/connected|disconnected` lifecycle notification contract 和 typed lifecycle consumer。真实 Environment public JSON-RPC 3/3 已通过，覆盖 local/unknown/gate 与远端 WebSocket registry；`tool-runtime --test filesystem_gateway` 4/4 已通过，覆盖远程 Read/Glob/Grep/apply_patch 的单一 gateway、断线透传和无 gateway fail-closed。已按 Codex current 将 `environment/info.cwd` 收敛为跨平台 canonical `file:` `PathUri`，native cwd 只在 App Server 边界转换为 URI。当前本地 `environment/status` 返回 local=ready；远端 registry 支持 Pending/Ready/Disconnected/Unknown，远端 URL 校验和连接失败均 fail closed，并具备有限后台健康探测/重连，不在连接异常时伪造 Ready。Thread/Turn 选择现在由 Environment registry 做 environment ID、绝对 cwd、workspace roots 去重和默认 root 规范化，并写入 Thread metadata；选择请求完成时按当前连接状态发射 connected/disconnected 通知，Pending 不伪造 connected。`thread/resume` 从持久化 Thread metadata 恢复 typed selection 并按当前 registry 状态重新发射生命周期通知，损坏 metadata fail closed。远端 registry 已支持 `environments.json` 冷启动重建，状态变化会主动广播到所有选中该 Environment 的 Thread；Environment selection 变更会追加 `world_state` full/patch 到 RuntimeCore canonical event log，重复快照不重复写。typed `RuntimeWorldState` 已新增多 Environment selection，以稳定 ID 顺序携带 id/cwd/workspace roots/primary/status/shell，并按 Codex 多环境 `<environment_context>` 进入 provider；执行链已把 Environment identity 从 ToolCall 贯通到 `LiveExecutionRequest`，本机 process owner明确拒绝非 local identity。远端 process transport/lowering 与 fs transport/lowering 已接入 current owner；remote process WebSocket transport 1/1 与 socket-loss reconnect 1/1 实际通过。公共 `environment_remote_turn_jsonrpc` 1/1 已通过真实 `run_json_lines -> thread/start -> turn/start -> RuntimeBackend -> provider tool call -> process/start/read`，并在重建 App Server、`thread/resume(excludeTurns=true)` 后验证省略 `turn/start.environments` 仍继承持久化 sticky Environment。Task Center 现有环境浮层通过 `useThreadEnvironmentLifecycleStatus` 首次读取 `environment/status` 并消费同一 Thread 的 typed lifecycle notification，五语言、稳定 DOM 与组件回归已完成；独立真实 Electron Gate B 已证明 connected -> disconnected -> reconnected、同一 Environment/Thread identity、62 次 electron-ipc、零 mock/invoke/console/page error。证据见 `.lime/qc/gui-evidence/environment-lifecycle-electron-gate-b/`；该 Gate 不启动 Turn、不调用模型，也不冒充 live provider 证据。

> D-2 event stream/resource、D-3 Environment 与 C-3 Windows restricted execution 子进度已关闭。MCP/Environment 已具备公共 integration、恢复证据、稳定 GUI consumer、五语言/组件回归和独立 Electron Gate B；Windows sandbox 已具备 `7/7` 真机矩阵与 runner packaged fail-closed 守卫。下一阶段只保留 live provider/Pier/DeepSWE verifier 和覆盖当前 SHA 的正式 package。

### E：Desktop coding evidence 与收尾治理

退出条件：controlled 与 live evidence 分开；live provider、verifier artifacts、patch SHA、recovery coverage 全通过；更新架构图、执行计划、事实源和治理回流守卫。

当前子进度：

- [x] macOS arm64 本地 packaged candidate：current Forge 隔离构建、主/Helper bundle identity、`codesign --verify --deep --strict`、app-server/code-mode-host sidecar 完整性、包内 executable Shell-01、packaged external-backend fail-closed、ZIP/local feed 与压缩完整性均已验证。
- [x] 正式 macOS 分发：GitHub Release run `32864956267` 在同一基线 SHA `0975674a` 上完成 arm64/x64 Developer ID signing、hardened runtime、Apple notarization/stapling、deep-strict codesign、资源 verifier、DMG/ZIP/RELEASES staging 与正式发布。arm64 日志记录 `Notarization Ticket=stapled`、主包 `valid on disk`/`satisfies its Designated Requirement`；正式 arm64 DMG SHA-256 为 `17e59fca7d98c4a7c9da3791cb07999b5ae78e65756a18c0f27577750b92a0d1`，ZIP 为 `8f0ca46e847701ee57dc664ba2757dbf0e2c6abed176c2a30e240320604e30ed`。
- [x] Windows Squirrel 安装/升级与 packaged Gate B：同一 Release run 的 Windows-x64 job 和 artifact `9570576514` 已完成 `1.134.0 -> 1.135.0` N-1 自动更新、候选安装、快捷方式、权限与安装后 Shell-01 21/21。
- DeepSWE current-chain harness 已改为 exact v2 wire：`thread/start` 只提交 current 参数并读取返回的 canonical `thread.id/thread.sessionId`，`turn/start` 使用 `threadId/input/additionalContext` 并读取 canonical `turn.id`，读取与中断分别固定为 `thread/read {threadId,includeTurns:true}` 和 `turn/interrupt {threadId,turnId}`；旧 `sessionId/appId/workspaceId/workingDir/businessObjectRef/runtimeRequest` 只保留负向断言。provider budget/generation 通过 `additionalContext.metadata` lowering；`--first-visible-output-timeout-ms` 只覆盖显式 DeepSWE 诊断回合，不新增产品协议或第二套 runtime 配置。provider event/evidence、failure 与 value helper 已拆回独立 owner，core 当前为 990 行；定向回归 `26/26`、contracts `299 checks`、scripts governance 与 Prettier 通过，窄写集 diff check 待收尾复验。
- [ ] live provider/current chain 与非空 patch SHA 已完成；Pier/DeepSWE `reward=1` artifacts 和覆盖当前 SHA 的正式分发未完成。Windows restricted execution 已由 run `32975574520` 的 schema v3 `7/7` 关闭。

## 5. 必跑验证

- 协议/跨层：`npm run test:contracts`
- Rust：`npm run test:rust:related -- <changed paths>`
- Agent 主链：`npm run smoke:agent-runtime-current-fixture`
- GUI：`npm run verify:gui-smoke`
- 本地门禁：`npm run verify:local`
- 治理：`npm run governance:legacy-report`、`npm run governance:scripts`

高风险变更必须同时保留 Gate A/Gate B 证据，不能用 owner 单测替代真实 Electron 链。

## 6. 当前进度日志

- 2026-08-23：完成当前 Lime/Codex HEAD 只读差异审计，确认 GUI、协议新项、Thread/Item 字段、Windows、模型控制和 Desktop evidence 为主要缺口；确认 Hooks、compaction lineage、CodeMode、controlled smoke 等不应重复列为实现缺口。
- 2026-08-23：建立本计划，当前阶段为 A，下一刀是以 Codex current Rust DTO 为基线更新协议产品范围矩阵并补 Thread/Item contract。
- 2026-08-23：完成阶段 A 的 Thread/Item contract 子刀。直接参考 Codex `app-server-protocol/src/protocol/v2/thread_data.rs` 与 `item.rs`，收敛 Lime v2 DTO、projection、schema 和 TS 生成物；下一刀转入 B 阶段的 Thread workspace GUI 盘点。
- 2026-08-23：完成阶段 B 的第一刀。Thread workspace header 改为优先读取 canonical Thread status，并透传 `canAcceptDirectInput`；24 个受影响 Vitest 用例、TypeScript 检查和 Prettier 检查通过。`npm run test:related` 包装器因将 `electron` 目录误作为输入失败，已改用同配置 `vitest run` 完成验证。
- 2026-08-23：完成阶段 B 的第二刀。Thread header 通过现有 `renameTopic -> thread/setName` current owner 提供本地化重命名菜单；25 个受影响 Vitest 用例、TypeScript 检查和 Prettier 检查通过。`npm run verify:gui-smoke` 因并行 renderer build lock 等待，重试后仍未获得真实 smoke 证据；已终止本轮启动的两组 smoke 进程，待锁释放后重跑。
- 2026-08-23：完成阶段 B 的第三刀。补齐 `sessionClient.d.ts`、`AgentRuntimeAdapter` 可选 archive/fork 能力、`useAgentSession` 的 `archiveTopic`/`forkTopic`、workspace setup/scene/header 接线，并补齐五种 locale 的 fork/archive 文案；App Server session client 对 fork 返回 Thread identity 做 fail-closed 校验。定向 Vitest 74 tests、TypeScript、Prettier 和 `npm run test:contracts`（299 checks）通过。
- 2026-08-23：本轮 `npm run verify:gui-smoke` 已启动至 `build:renderer:electron:smoke`，约 90 秒持续停在 `[electron-renderer-build] waiting for renderer build lock`，以退出码 130 终止本轮等待；当前没有新增真实 Electron Gate B 证据，待并行 renderer build 释放后重跑。
- 2026-08-23：完成 B-2 生命周期语义复核。Codex current 的 `thread/resume` 是恢复/重建 Thread read model 的协议动作，Lime 已在 stream recovery/session hydration 通过同一 current adapter 消费；Codex `thread/rollback` 已 deprecated，且只改历史不回滚本地文件，因此不恢复为新的 GUI 主操作。B 阶段剩余真实 Electron Gate B 证据。
- 2026-08-23：`npm run i18n:check:json` 通过，五种 locale 的 8623 个源 key 全覆盖；`npm run smoke:agent-runtime-current-fixture` 的历史/流式/fixture guard 共 162 个已执行用例通过，随后在重建 renderer fixture 时再次受 renderer build lock 阻塞并以退出码 130 终止；未遗留本轮 smoke 进程。
- 2026-08-23：回收 renderer stale build lock 后，官方 `npm run verify:gui-smoke` 通过；Shell-01 Gate B 21/21 assertions、session-history Electron fixture、Claw current fixture 均通过，B 阶段真实桌面证据关闭。期间修复 Rust projection 中 typed `ThreadSource` 序列化与 `metadata` move 两处编译问题；未覆盖并行浏览器/Electron/benchmark 热区。
- 2026-08-23：C-1 执行链审计确认 `command/exec` 与 standalone `process/*` 已统一到 connection-scoped App Server + `tool-runtime::execution_process` owner，具备实时 raw-byte delta、stdin/PTY、terminate、timeout、output cap、跨连接隔离、响应前 notification barrier 和断连清理；后续不新开第二套 process supervisor，C 阶段下一刀转为 approval cache / sandbox-denied retry / Windows evidence。
- 2026-08-23：完成 C-2 implementation slice。以 Codex current approval cache / sandbox retry 语义为参考，在 `AgentRuntimeState` 增加 session-scoped、内存限定 256 条的 shell approval cache；`current_provider_turn/tool_executor/orchestration` 绑定 shell contract、approval key 和 `allow_for_session` 记录/命中；`tool-runtime::execution_orchestrator` 新增 `RuntimeToolInitialApproval::Cached` 并标记 `Reused`；App Server action response fallback 保留 `decision`/`decisionScope`，避免 session decision 在 live session route 丢失。新增回归覆盖 cache 零审批、command/cwd/sandbox/permissions key 隔离、browser once-only contract 与 session close 清理。`rustfmt`、`git diff --check` 通过；定向 Cargo 仍被 `rusty_v8 v150.4.0` aarch64 macOS 预编译包 HTTP 404 阻塞，未将该测试记为通过。
- 2026-08-23：C-3 对照 Codex `exec-server/src/process_sandbox.rs`、`exec-server/src/process.rs` 与 `windows-sandbox-rs/src/unified_exec/**` 完成 current owner 复核。Lime 已有 restricted-token/ACL/Job Object/pipe lifecycle foundation，但没有 Windows toolchain、Windows runtime 或 Desktop Gate B 证据；继续保持 `Planned/enforced=false`，下一刀只在 Windows runner 上执行平台证据矩阵，不恢复 retired runner/TUI setup 或伪造 `Ready`。
- 2026-08-23：C-3 平台证据矩阵实现入口完成。新增 `lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs`，直接启动 restricted-token current process owner，覆盖 workspace 可写、`.git/.codex/.agents` 和 workspace 外拒绝、400KB 输出 bounded/truncated、terminate 后 descendant marker 不落盘；`.github/workflows/quality.yml` 的 `windows_shell_runtime` job 新增该测试。macOS 静态 rustfmt、`git diff --check`、`npm run test:contracts` 与 `npm run governance:legacy-report` 通过；本机无 Windows target，尝试 `cargo test -p tool-runtime --test windows_restricted_execution --no-run` 仍在 `rusty_v8 v150.4.0` aarch64-apple-darwin 预编译包 HTTP 404 处阻塞，故不提升 Windows readiness。
- 2026-08-23：完成 C-3 协议 exact contract slice。按 Codex current `windows_sandbox.rs` 对齐 `WindowsSandboxSetupMode`、`windowsSandbox/setupStart`、`WindowsSandboxSetupStartParams/Response`、`windowsSandbox/setupCompleted`、`WindowsSandboxSetupCompletedNotification` 与 `windows/worldWritableWarning` DTO；同步 v2 method/envelope、schema registry、JSON fixtures、generated TypeScript 和 typed request/connection client facade。`cargo test -p app-server-protocol --lib` 124/124 通过；App Server handler、connection-scoped 异步 setup 和真实 Windows readiness 仍未完成，当前不改变 `SandboxBackendStatus::Planned/enforced=false`。
- 2026-08-23：完成 C-3 Renderer consumer slice。`src/lib/api/windowsSandbox.ts` 统一承接 readiness/setup typed request、绝对路径与 setup mode 校验、completion/warning notification projection；App Server frontend client method/types/spec 已注册，`WindowsSandboxReadinessStatus` 接入 Elevated/Unelevated setup、异步 completion、world-writable warning 和 fail-closed readiness refresh；五种 locale 文案同步。定向 Vitest 8/8、`npm run i18n:check:json`、`npm run check:protocol-types`、`npm run test:contracts`（299 checks）、`npm run governance:legacy-report` 和 `git diff --check` 通过。`npm run typecheck` 仍被并行改动的 `useAgentChatWorkspaceSceneRuntime.tsx` 三处既有 `string | null` 错误阻塞；`cargo fmt --check` 仍被并行 Rust 文件格式漂移阻塞，未修改其热区。
- 2026-08-23：D-1 只读盘点确认 Lime 已有 `model/list` 分页目录、typed capability snapshot、provider catalog 和 route capability current owner；与 Codex current 的独立 `modelProvider/capabilities/read` 仍存在 exact contract 差距。该方法下一步应直接复用 `model-provider::provider_capabilities` 计算，不复制第二套 model catalog，也不把 model/list 伪装成替代实现。
- 2026-08-23：D-1 protocol/App Server/typed client slice 完成。新增 `modelProvider/capabilities/read` exact 三布尔字段 contract，App Server 按当前 provider route 复用 `ProviderCapabilities`，未知 provider、配置加载失败和未知响应均 fail closed；protocol 125 tests、app-server-client 124 tests、Lime 相关 Vitest 55 tests、299 项 contracts 通过。App Server crate 公共编译仍被本机 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞，尚未将 integration evidence 标记完成。
- 2026-08-23：D-1 GUI consumer slice 完成。ModelSelector 打开时通过 `modelRegistryApi.readModelProviderCapabilities()` 读取当前运行 provider 的 exact 三布尔快照，在同一 provider 区域展示工具命名空间、图片生成、网页搜索状态；请求失败和未知响应均 fail closed；新增 provider capability badge 组件、五语言文案和 46 个 ModelSelector/component tests，`i18n:check:json` 全量覆盖通过。补充 App Server route 语义回归（official OpenAI、openai-compatible、unsupported provider）；`model-provider` 4 tests 通过，`app-server` 公共 crate 集成证据仍受 `rusty_v8` archive HTTP 404 阻塞。
- 2026-08-23：D-2 MCP 只读审计确认 Codex current 的 event stream 三件套属于 experimental API，Lime v2 尚未注册对应 method/notification；Lime 当前 deferred tool/list-for-context/search 主要仍是 v0/legacy surface，虽有 inventory metadata 和 fail-closed API 测试，但不能宣称与 Codex 的 v2 event subscription、recovery/resume 语义一致。下一步先建立 method/product-scope matrix 和 gating 回归，再在 current MCP owner 内实现真实订阅链。
- 2026-08-23：完成 D-2 protocol/gating slice。直接对照 Codex `app-server-protocol/src/protocol/v2/mcp.rs` 和 `protocol/common.rs`，新增 `mcpServer/event/stream/start`、`stop`、`notification` 的 exact DTO、v2 method/envelope、schema registry/fixtures、generated TypeScript 与 125 个 protocol tests；将 Lime initialize capability 收敛为 wire `experimentalApi`，transport 按连接保存 capability，公共 JSON-RPC 入口在未声明时返回 `INVALID_REQUEST`，补 App Server public-entry regression。`npm run test:contracts`（299 checks）、`check:protocol-types`、rustfmt、diff check 通过；App Server crate 集成测试仍受本机 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞。下一刀是 current MCP subscription owner 与 thread/cold recovery evidence，继续保持无 GUI 入口、无 mock fallback。
- 2026-08-23：完成 D-2 subscription owner implementation slice。参考 Codex `app-server/src/request_processors/mcp_event_stream.rs` 与 Lime `rmcp 0.12` current client service，将 server custom notification 原样投影到 `McpServerEventStreamNotification`；`McpThreadRuntime`/`McpClientManager` 以 session+thread generation 持有 broadcast owner，App Server 按连接和 subscription id 管理 start/stop task，start 强制当前连接已订阅 canonical Thread 且 server 已在该 Thread runtime 运行，stop/transport close/receiver close 清理任务。验证：`cargo test -p app-server-protocol --lib` 125/125、`npm run test:contracts` 299 checks、rustfmt、`git diff --check` 通过；`cargo check` 的 Lime MCP/App Server 编译仍被本机 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞，故不提前宣称公共 integration 或 Electron Gate B 完成。下一刀是 active/terminated readiness、runtime 替换后的 cold recovery/resume/reconnect 和公共入口 integration test。
- 2026-08-23：完成 D-2 lifecycle/recovery implementation slice。对照 Codex `request_processors/mcp_event_stream.rs` 与 `core::CodexThread::start_mcp_event_stream`，Lime current MCP owner 新增真实 `events/stream` custom request、session-owned `McpEventStream` request/notification route、active ready barrier、terminated projection、runtime generation replacement 的有限 reconnect；App Server stream task 在 Thread archive/delete/unload、transport close 和 connection cleanup 时统一 abort，启动失败/超时 fail closed。验证：Rustfmt、`git diff --check`、`npm run test:contracts`（299 checks）通过；`cargo check -p lime-mcp -p lime-agent -p app-server --lib` 仍被本机 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞，未将公共 integration 或 Electron Gate B 记为完成。下一刀是恢复环境后补 App Server public JSON-RPC integration、MCP current fixture 和独立 Electron Gate B。
- 2026-08-23：补入 D-2 public JSON-RPC integration/current fixture。新增 `mcp_exact_jsonrpc` 的真实 `run_json_lines` 场景：通过 LocalAppDataSource 安装 stdio Plugin MCP server、启动 Thread、声明 `experimentalApi`、等待 `notifications/events/active` 后转发 event，并通过 stop/transport close 清理；已有 transport-level 未声明 capability fail-closed 回归继续覆盖门禁。另将重复 subscription 检查前移，避免重复请求在正常路径触达远端。`rustfmt` 与 `git diff --check` 通过；定向 `cargo test -p app-server --test mcp_exact_jsonrpc --no-run` 仍因 rusty_v8 archive HTTP 404 未执行，故 integration/current fixture 只记为代码已写入、运行证据待环境恢复，不标记 D-2 完成。
- 2026-08-23：修复 D-2 终止通知生命周期边界。若远端已发送 `notifications/events/terminated`，App Server stream task 记录该事实并直接结束，不再追加 synthetic terminated；只有连接、Thread 或 runtime 在未收到远端终止事件时关闭，才发送一次合成终止通知。`rustfmt --check` 与 `git diff --check` 继续通过。
- 2026-08-23：完成 D-3 Environment public integration fixture slice。新增 `environment_jsonrpc`，通过真实 `run_json_lines` 覆盖 `initialize` 能力声明、`environment/info` local shell/cwd、`environment/status` ready/unknown、未知 `environment/info` 错误和远程 `environment/add` fail-closed；第二个场景验证未声明 `capabilities.experimentalApi` 时 Environment 方法统一返回 `INVALID_REQUEST`。新测试已通过 rustfmt/diff check，但本机 App Server crate 编译仍被 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞，故仅记录 fixture 已写入，未宣称 App Server integration 或 Electron Gate B 完成。下一刀是恢复编译环境后执行该 fixture，并继续补 exec-server lifecycle/recovery 与 durable world-state history。
- 2026-08-23：按 Codex current `app-server-protocol/src/protocol/v2/environment.rs` 复核并修正 D-3 wire drift：`EnvironmentInfoResponse.cwd` 从普通字符串改为严格 canonical `file:` `PathUri`，拒绝非-file scheme、credentials、port、query、fragment 和 NUL；补 protocol 负向/正向 round-trip，重新生成 schema fixtures 与 TypeScript。`cargo test -p app-server-protocol --lib` 126/126、`npm run check:protocol-types` 和 `npm run test:contracts`（299 checks）通过；`cargo test -p app-server --test environment_jsonrpc --no-run` 重试仍在 `librusty_v8_ptrcomp_sandbox_release_aarch64-apple-darwin.a.gz` HTTP 404 处阻塞。下一刀保持为恢复 App Server integration 运行证据，再推进 exec-server connection lifecycle/recovery。
- 2026-08-23：按 Codex current app-server protocol 与 app-server suite 补 D-3 Environment connection lifecycle contract。新增 `thread/environment/connected`、`thread/environment/disconnected` exact notification DTO、v2 envelope/catalog、schema manifest/generated TypeScript、Rust round-trip（127/127）和 app-server-client lifecycle typed consumer；修复 Agent Runtime lifecycle union 的类型收窄并通过 `npm test --prefix packages/app-server-client`（125 tests）。当前仅完成协议/消费合同，尚未由 Lime runtime 发射真实 connected/disconnected；下一刀仍是 Environment registry + exec-server transport owner。
- 2026-08-23：完成 D-3 Environment registry/transport implementation slice。按 Codex `exec-server/src/environment.rs`、`client.rs` 和 `app-server/src/request_processors/environment_processor.rs` 重建 Lime current owner：`environment/add` 校验并替换远端 WebSocket registry entry，后台执行 `initialize`/`initialized`/`environment/info`/`environment/status` 握手；`environment/info` 等待初始连接并返回 canonical `PathUri`，`environment/status` 对 ready 连接使用现有 socket 做 fail-fast health probe，连接失败映射为 `disconnected`，不伪造 ready。新增公共 `environment_jsonrpc` WebSocket fixture，覆盖 remote add、Pending->Ready、info 和 server close 后 disconnected；fixture 已 rustfmt/diff check，但 App Server crate 仍因本机 `rusty_v8` v150.4.0 arm64 archive HTTP 404 无法编译运行。协议 127/127、app-server-client 125 tests、contracts 299 checks 通过。仍缺真实 Thread environment selection/lifecycle notification 发射、reconnect/resume、durable world-state history 和 Electron Gate B。
- 2026-08-23：完成 D-3 Thread/Turn environment selection implementation slice。按 Codex `resolve_turn_environment_selections` 与 `ThreadManager::validate_environment_selections` 语义，Environment registry 现在校验非空且唯一的 environment ID、绝对 POSIX/Windows cwd、workspace roots；roots 去重且缺省回填 cwd。`thread/start` 将规范化选择写入 canonical Thread metadata，`turn/start` 复用同一 lowering；local/ready 选择发射 `thread/environment/connected`，已知断线选择发射 `thread/environment/disconnected`，Pending 不伪造 connected。新增 registry 单测覆盖默认 root、重复 ID 和相对路径拒绝；rustfmt、git diff check 通过。App Server 定向编译仍被本机 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞，因此未把 lifecycle 运行证据标记完成。下一刀是把 selection 绑定到 Thread resume/cold hydration，并实现远端连接主动断线、reconnect/resume 与 RuntimeCore 执行 lowering。
- 2026-08-23：完成 D-3 persisted selection resume slice。`thread/resume` 现在从 canonical Thread metadata 读取 typed environment selections，复用 registry 校验/规范化，并按当前 Ready/Disconnected/Pending 状态恢复 lifecycle 通知；损坏 metadata 或冷启动后缺失的远端 registry entry 均 fail closed。新增持久化 selection 解析回归。当前只关闭本地/已重建 registry 的 resume 绑定，远端 environment config 尚未进入 durable owner，因此 cold restart 自动重建、主动断线转发和 reconnect 仍未完成。
- 2026-08-23：完成 Codex product-scope/governance convergence slice。D-1 current `modelProvider/capabilities/read`、D-3 Environment methods/notifications 已从过时 excluded/dead guard 收回 current matrix，产品范围矩阵更新为 `146 implemented / 3 planned / 72 excluded`；新增 current manifest 同名契约回归。`npm run governance:legacy-report` 边界违规从 13 降为 0，范围守卫 9/9 通过；同步运行 `npm run generate:protocol-types` 后 `npm run test:contracts` 稳定通过。下一刀仍回到远端 registry cold restart、主动断线/reconnect 与 RuntimeCore lowering，不把 governance 收口当作 execution parity。
- 2026-08-23：完成 D-3 bounded reconnect implementation slice。Environment registry 为每个远端 entry 增加 retired generation、后台健康探测和 2 秒间隔的有限 reconnect；替换同 ID entry 会停止旧 watcher，连接失败保持 `Disconnected/Pending`，不伪造 `Ready`。这只覆盖 transport-level recovery，尚未把状态变化广播到每个已选 Thread，也未实现 cold-start registry hydration 或 RuntimeCore 远端执行 lowering。
- 2026-08-23：完成 D-3 registry durable/recovery slice。Environment registry 增加配置根 `environments.json` 的原子持久化与启动 hydration；只恢复通过 ID/URL 校验的远端条目，连接仍从 `Pending` 开始。registry status event bus 接入 App Server current notification hook，Ready/Disconnected 变化主动投影给所有选中该 Environment 的 Thread；archive/delete/unsubscribe 清理 Thread 选择索引。Environment selection 现在由 RuntimeCore canonical event log 追加 `world_state.v1` 的 environments full/patch，按 session 去重并保留完整 state，供冷启动重放。新增代码已 rustfmt、`git diff --check` 通过；`app-server-protocol` 127/127 与 `packages/app-server-client` 125 tests 通过。App Server crate 编译仍被本机 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞，故不提前宣称 integration、完整 RuntimeWorldState provider lowering 或 Electron Gate B。下一刀是接入 RuntimeCore 真实远端 process/filesystem executor lowering，并为持久化 registry、广播和 world-state full/patch 补定向单测。
- 2026-08-23：完成 D-3 typed provider context 与 execution identity foundation slice。`agent-protocol::RuntimeWorldState` 新增 typed 多 Environment selection，App Server 在 `turn/start` 从 registry 捕获 id/cwd/workspace roots/primary/status/shell 请求级快照，provider renderer 按 Codex 单环境/多环境形态输出；primary cwd 不再被本机 workspace cwd 覆盖。`ToolCall -> RuntimeToolExecutionContext -> RuntimeUnifiedExecToolRequest -> LiveExecutionRequest` 贯通 Environment identity 和对应 cwd，`ExecutionProcessServer` 对非 `local` identity 明确返回 `UnsupportedEnvironment`，禁止静默本地执行。`agent-protocol` world-state 4/4、`git diff --check` 通过；Agent Runtime/App Server 测试仍在既有 `rusty_v8` HTTP 404 处阻塞。D-3 保持进行中，下一刀是让 current Environment registry 实现并注入 Codex exec-server `process/*` 与 `fs/*` transport。
- 2026-08-23：完成 D-3 远程 process transport implementation slice。新增 Environment 单一 WebSocket reader/writer actor 的 `process/start|read|write|signal|terminate` 并发请求；远程进程接入现有 `ExecutionProcessServer`，通过 `process/read` 轮询转换为统一 `ExecutionOutputDelta`，并写回统一 `ExecutionProcessSnapshot`、background terminal、terminate/signal/status 清理路径。remote sandbox lowering 只接受 Codex 已知 `read-only`、`workspace-write`、`danger-full-access`，未知策略返回 sandbox denial；新增 `RemoteProcessCommand: Debug` 与 wire lowering 回归。`cargo fmt --check`、`git diff --check` 通过；App Server crate 定向编译仍被 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 阻塞。当前实现属于 `current` process owner，但尚未宣称远程 process 公共 JSON-RPC fixture、cold restart/reconnect/resume 或 Electron Gate B；下一刀是同一 Environment owner 的 Codex `fs/readFile|writeFile|open|readBlock|close|getMetadata|readDirectory|walk|canonicalize` transport 与 RuntimeCore/tool-runtime lowering。

- 2026-08-23：D-3 filesystem lowering slice 开始。新增写集限定为 `tool-runtime` filesystem gateway、Environment registry 的 `fs/*` lowering、RuntimeBackend/AgentRuntimeState 注入和远程 `Read/Glob/Grep/apply_patch` 执行分支；本地工具路径保持 current owner，不新增 renderer/Electron fallback。退出条件是远程工具调用按 Environment identity 使用 `fs/*`，未知或断线 Environment 继续 fail closed，补 gateway/remote fixture 回归；当前尚未完成，App Server crate 运行证据仍受本机 `rusty_v8` aarch64 archive HTTP 404 阻塞。
- 2026-08-23：完成 D-3 filesystem gateway/lowering implementation slice。`RuntimeFileSystemGateway` 作为 `tool-runtime` current filesystem owner 接入 Environment registry；远端复用单一 WebSocket actor 的 `fs/readFile|writeFile|open|readBlock|close|getMetadata|readDirectory|walk|canonicalize|createDirectory|remove`，`Read/Glob/Grep/apply_patch` 按 ToolCall Environment identity 走 gateway，本机 owner 保持不变。补充测试专用 `filesystem_gateway` fixture，覆盖远程 Read 不读本机、Glob/Grep 使用 walk/read、apply_patch 使用 gateway 与断线错误透传；远程 Environment 缺 gateway 时四条工具路径统一 fail closed。修正远程 filesystem sandbox：读取默认 `read-only`、写入/apply_patch 默认 `workspace-write`、`danger-full-access` 才允许 unrestricted，未知策略返回错误。rustfmt、`git diff --check` 通过；`cargo test -p tool-runtime --test filesystem_gateway` 已启动但在既有 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 处阻塞，fixture 尚无运行证据。下一刀是恢复依赖后运行 gateway fixture，并扩展 App Server WebSocket fixture 覆盖真实 `fs/*` 请求和远程 apply_patch 的 create/remove。
- 2026-08-23：补入 D-3 EnvironmentRegistry 真实 WebSocket filesystem fixture。单测 server 覆盖 `initialize`/`environment/info`/`environment/status` 后，真实调用 registry gateway 的 `fs/readFile`、`fs/writeFile`、`fs/getMetadata`、`fs/canonicalize`、`fs/readDirectory`、`fs/walk`、apply_patch 所需 `fs/createDirectory` 与 `fs/remove`，并断言每个方法都经过同一连接。代码已 rustfmt、`git diff --check` 通过；App Server/tool-runtime 运行仍受 `rusty_v8` v150.4.0 aarch64 archive HTTP 404 阻塞，未宣称 fixture 运行证据。
- 2026-08-23：完成 D-3 filesystem gateway ownership slice。删除 `tool-runtime` 进程级 `OnceLock` gateway，改由 `RuntimeCore -> RuntimeBackend -> AgentRuntimeState -> RuntimeToolStepSnapshot -> RuntimeToolExecutionContext` 显式注入同一 Environment filesystem gateway；普通本机/单测 snapshot 默认无 gateway，CodeMode nested tool 复用当前 step snapshot，避免多个 App Server/test 实例互相污染。`RequestProcessor` 只在 RuntimeCore backend 接受注入时启用远程 Environment lowering；未知/不支持 backend 继续 fail closed。rustfmt、`git diff --check` 通过；`cargo check -p agent-runtime -p lime-agent --lib` 与 `cargo check -p tool-runtime --lib` 均在既有 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 处阻塞，未将编译或 fixture 运行标记为完成。下一刀是依赖恢复后执行 tool-runtime filesystem fixture、App Server environment fixture，并补远程 process 公共 JSON-RPC 与 cold restart/reconnect/resume 证据。
- 2026-08-23：补入 D-3 Environment reconnect recovery fixture。真实 WebSocket server 接受两次连接，第一次完成 `initialize -> initialized -> environment/info -> environment/status` 后主动断开，第二次完成同样握手；registry 断线探测和 2 秒 bounded reconnect 断言 `Ready -> Disconnected -> Ready`，不伪造 Ready。远程 process 公共 fixture 已存在于 `execution_process/tests.rs`，本轮不重复实现。定向测试启动仍在 `rusty_v8 v150.4.0` aarch64 archive HTTP 404 处阻塞，保留为 evidence-pending。
- 2026-08-23：补入 D-3 cold-start Environment fixture。测试直接写入合法 `environments.json`，创建新 `EnvironmentRegistry` 后由 hydration 自动发起真实 WebSocket `initialize -> environment/info -> environment/status` 握手，并断言冷启动连接进入 `Ready` 且 cwd 保留 canonical `file:` URI。App Server 运行证据仍受 `rusty_v8` archive HTTP 404 阻塞。
- 2026-08-23：收紧 D-3 apply_patch 最小权限 lowering。远程 UpdateFile 先以 `read-only` sandbox 读取源文件，再以 `workspace-write` 写入目标、创建父目录和删除旧路径；避免把读取请求错误标成 `write`。rustfmt、`git diff --check` 继续通过。
- 2026-08-23：修复 D-2 runtime replacement cleanup 缺口。`AgentRuntimeState::ensure_mcp_runtime_generation` 现在只在新 generation 启动成功后发布替换，并在释放 runtime map 锁后显式 shutdown 旧 generation；线程不匹配失败路径也会关闭刚启动的临时 generation，避免旧/临时 stdio 连接和事件路由泄漏。新增 `replacing_generation_shuts_down_previous_servers` Node stdio fixture 回归；`rustfmt` 与 `git diff --check` 通过。定向 `cargo test -p lime-agent runtime_state::mcp_runtime_tests --lib` 仍因 `rusty_v8 v150.4.0` 在 `aarch64-apple-darwin` 缺少预编译 archive（GitHub HTTP 404）阻塞，未将该回归标记为运行通过。`npm run test:contracts`、`npm run governance:legacy-report` 和 `cargo test -p app-server-protocol --lib` 本轮已重新启动，仍需读取各命令最终退出结果；D-2/D-3 公共 App Server 与 Electron evidence 继续保持 `evidence-pending`。
- 2026-08-23：完成本轮验证收口。`cargo test -p app-server-protocol --lib` 127/127 通过；`npm run test:contracts` 299 checks、命令/脚本/发布流程/文档边界守卫全部通过；`npm run governance:legacy-report` 扫描 2048 文件、边界违规 0；`npm test --prefix packages/app-server-client` 完成 TypeScript build 与 app-server-client 测试。`cargo test -p lime-mcp --lib` 与 `cargo test -p lime-agent runtime_state::mcp_runtime_tests --lib` 均在同一 `rusty_v8 v150.4.0` `aarch64-apple-darwin` archive HTTP 404 处阻塞，未把 MCP replacement 回归标记为运行证据。计划仍为 `active`：D-2/D-3 的公共 App Server fixture、远程 process/filesystem 运行证据、cold restart/resume、独立 Electron Gate B，以及 C-3 Windows/MSVC/真机和 E live evidence 未完成。
- 2026-08-23：修复 D-2 replacement recovery 分支。App Server MCP event stream task 在订阅已进入 active 后，将旧 runtime shutdown 导致的 `McpEventStream::recv()` transport `Err` 与 clean EOF 一并纳入有限 reconnect；订阅尚未 active 时仍 fail closed，不会把启动错误误当恢复。`rustfmt --check` 与 `git diff --check` 通过；公共 App Server integration/Electron 运行证据仍受 V8 archive 阻塞，未标记为完成。
- 2026-08-23：补强 D-2 generation startup fail-closed。`ensure_mcp_runtime_generation` 在 `McpThreadRuntime::start` 任一失败（包括 bridge snapshot 构建失败）时显式 shutdown 临时 generation，确保部分启动的 MCP server 不会泄漏；随后才进入 replacement publish。`rustfmt --check` 与 `git diff --check` 通过。
- 2026-08-24：继续治理与验证收口。`runtime.rs` 的 `ExecutionBackend` trait 已迁入 `runtime/execution_backend.rs`，provider route/base-url helper 已归 `runtime/model_providers.rs`，runtime 文件降至 558 行；`processor/mod.rs` 的 MCP event-stream cleanup、Environment runtime/selection、通知 hook 与 connection helper 已迁入 `processor/mcp.rs`、`processor/environment.rs`、`processor/notifications.rs`，facade 降至 769 行，低于 800 行治理阈值。同步 item inventory 的 `commandExecution.pluginId/scriptPath` 与 `mcpToolCall.readOnlyHint` 字段事实；`npx vitest run src/lib/governance/appServerRuntimeBoundary.test.ts src/lib/governance/agentItemInventoryBoundary.test.ts` 29/29 通过，`cargo fmt --check`、`git diff --check` 通过。`cargo check -p app-server --lib` 仍在本机 `rusty_v8 v150.4.0` `aarch64-apple-darwin` 预编译 archive HTTP 404 处阻塞；未将 Rust 编译、D-2/D-3 公共 App Server integration 或 Electron Gate B 记为完成。计划保持 `active`，下一刀是依赖恢复后执行公共 fixture，并继续 C-3 Windows/MSVC/真机与 E live evidence。
- 2026-08-24：完成本轮全量前端回归收口。smart Vitest 在修复 Browser pending intent 可选字段形状后从第 110 批续跑至第 116 批，全部通过；无历史投影时 `historicalProjection` 不再显式写入 `null`，保留既有 exact-shape 合同，有历史数据时仍投影只读历史事实。随后 `npm run test:contracts`（299 checks）、`npm run governance:legacy-report`（2048 文件、边界违规 0）、`npm run governance:scripts`、`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check` 与 `git diff --check` 全部通过。Rust App Server 集成、D-2/D-3 公共 fixture 运行、Windows/MSVC/真机、独立 Electron Gate B 与 E live provider/verifier 证据仍未完成，计划继续保持 `active`。
- 2026-08-24：完成当前 Codex HEAD 漂移只读审计。上游从历史锁定 `c9c6c0d` 到当前 `99660ab3` 新增 24 个方向化 method：实验性 `thread/queue/*`（7）、`project/*`（7）、`thread/revert`/`thread/reverted`、`autoApprovalReview/strictReviewRequired`、`account/bedrock/{discover,setup}` 与 MCP event stream（3，Lime 已有实现）。同时发现 MCP status 已从 Lime 当前 v0 `servers` 产品快照漂移为 v2 `data/nextCursor`、`detail/threadId`、`runtimeStatus` exact contract；MCP resource read 新增 `originCallId/connectorId`，Thread/Item/permissions/config 还有字段漂移。Queue、Project、revert、strict review、Bedrock 尚无 Lime current owner；Browser/Computer Use requirements 属于配置策略面，需产品范围裁决；realtime existing-call 仍为 excluded。历史矩阵与 fixture 不在本条审计中擅自改 hash，以上全部登记为 open gap，计划保持 `active`，不得把旧 98.0% 产品范围数字当作当前 HEAD 完成度。
- 2026-08-24：完成 MCP status/resource current-contract implementation slice。按 Codex HEAD v2 DTO 新增 `McpServerStatus`、分页 `ListMcpServerStatusParams/Response`、`runtimeStatus`/`detail`/`threadId`、server/tool/resource/template typed inventory，以及 `mcpServer/resource/read` 的 `originCallId`/`connectorId`/`originCallId` response 字段；同步 v2 envelope/method ingress、schema registry/fixtures、generated TypeScript 和 App Server typed client。LocalAppDataSource 通过同一 MCP manager 生成状态页并执行 cursor/limit/detail lowering；RuntimeCore tool inventory 改读 v2 data，旧状态 dispatch 已从生产主链移除。Renderer Settings API 读取 v2 data 后只在配置 owner 做 identity lowering，旧配置 CRUD 仍走其 current owner。验证：`cargo test -p app-server-protocol --lib` 127/127、`npm run check:protocol-types`、`npm run test:contracts` 299 checks、MCP 前端定向 34/34、`cargo fmt --check`、`git diff --check` 通过。App Server crate 公共编译和 JSON-RPC fixture 仍受 `rusty_v8 v150.4.0` arm64 预编译 archive HTTP 404 阻塞，未将运行证据标记完成；旧 v0 status schema/手写客户端 DTO 仍列为后续治理清理项。
- 2026-08-24：进入 A-Queue protocol implementation。确认 Lime 没有 Queue durable current owner，`thread-store` 也没有 queue trait；本轮只落 Codex exact `thread/queue/{add,list,update,delete,reorder,start}` DTO、method catalog、v2 request/response/notification envelope、schema registry 与 generated TypeScript ingress，并复用 connection-scoped `experimentalApi` gate。Queue persistence、dispatch、turn execution、archive/delete/resume 生命周期和真实运行证据仍未完成，在 owner 建立前不得标记 `implemented`。
- 2026-08-24：完成 Strict Review 与 connection-scoped experimental API 收口。`autoApprovalReview/strictReviewRequired` 已有 exact DTO、method/envelope、schema registry/generated TypeScript、typed client validator/type guard；Guardian strict-auto-review 同时投影 `item/autoApprovalReview/started` 与 strict-review signal，并归入 Agent Runtime signal router。`AppServerEventBridge` 注入共享 `TransportExperimentalApi`，对 Queue、Strict Review、process、Environment、MCP stream、moderation 等 experimental notifications 在最终 connection send 边界 fail closed；双连接回归证明未声明 `experimentalApi` 的连接收不到 Queue/Strict Review 通知。协议 129/129、app-server-client 129/129、Queue 前端定向 7/7、contracts 299 checks 通过。
- 2026-08-24：完成 A-Queue RuntimeCore 生命周期实现与回归写入。新增 Queue archive/unarchive/delete/cold-resume integration cases；`thread/resume` 仅在最近非 Queue turn 不是 Canceled 时唤醒冷持久化队列，Interrupted 继续暂停，显式 `thread/queue/start` 可恢复；delete 清理 durable event/projection/sidecar 后同 ID Thread 重建为空。四个 Queue Rust 文件已 `rustfmt --check`、`git diff --check` 通过。`cargo test --manifest-path "lime-rs/Cargo.toml" -p app-server --test thread_queue_jsonrpc --offline` 仍在 `rusty_v8 v150.4.0` `aarch64-apple-darwin` archive HTTP 404 处阻塞，未把 fixture 运行标为完成。
- 2026-08-24：完成 A-Project current owner 与公共运行证据。Project SQLite repository、Thread assignment、七个 experimental method、两个 experimental notification 和三个 Thread Project 字段 gate 已落到唯一 App Server JSON-RPC 主链；新增 owner 回归与公共 `run_json_lines` fixture。使用本机缓存的 `rusty_v8 v150.4.0` archive/binding 显式注入后，Project owner 3/3、公共 JSON-RPC 4/4、`app-server-protocol` 130/130、`npm run check:protocol-types`、`npm run test:contracts` 299 checks、Rust fmt 与 `git diff --check` 全部通过。Project 状态提升为 `implementation-complete / evidence-pending`，剩余交付缺口为独立 Electron Gate B；整体计划仍保持 `active`，下一刀优先清理同一 V8 阻塞下的 Queue/MCP/Environment 公共 fixture，再进入 `thread/revert`。
- 2026-08-24：关闭 A-Queue 公共运行证据缺口。首次运行 `thread_queue_jsonrpc` 暴露 fixture 未注入 canonical store，以及 cold resume 同步模式把 runtime events 送入空 callback 的真实缺陷；补齐隔离 `ProjectionStore`，并让恢复队列复用 admission 后后台执行/event hub 主链。随后公共 JSON-RPC 5/5 与 cold-resume owner 单测 1/1 通过，覆盖 CRUD/分页/通知/FIFO、中断暂停、多模态重启与真实 `turn/completed` 投影。Queue 剩余缺口仅为独立 Electron Gate B，状态提升为 `implementation-complete / evidence-pending`。
- 2026-08-24：完成 `thread/revert` implementation slice。按 Codex experimental paginated history replacement 语义，补齐 v2 `ThreadRevertParams`/`ThreadRevertResponse`/`thread/reverted`、request/response/notification envelope、method catalog、schema registry/JSON fixture、Rust/TypeScript typed client 与 connection-scoped `experimentalApi` gate；RuntimeCore 以 append-only `history.rollback` marker 替换 effective event stream，保留原 Thread identity、返回 metadata-only response 与 turns/items backwards cursor，不截断旧 JSONL、不回滚本地 workspace 文件，并在 active turn 时复用 interrupt/cancel contract。公共 `thread_revert_jsonrpc` 已覆盖 replacement、notification、cursor、items hydration、repeated revert、active interrupt、cold resume/workspace invariant 与 missing-turn exact error（5 个场景已写入）；新增真实 `run_json_lines` 双连接 transport gate fixture 代码，当前因本机 `rusty_v8 v150.4.0` aarch64 archive HTTP 404、`V8_FROM_SOURCE=1` 又需要不可用的 GN/Ninja 网络下载而未运行。新增 provider-history 单测验证 rollback 后保留 prefix、移除 reverted turn。同步更新 `internal/aiprompts/commands.md`，将旧 `thread/rollback` 保持 `deprecated`，不新增 GUI 入口。`thread/revert` 状态为 `implementation-complete / evidence-pending`；下一刀是恢复 V8 依赖后运行公共 fixture，并补独立 Electron Gate B。
- 2026-08-24：完成本轮公共运行证据与 Revert 修复。通过 `target/debug/gn_out/obj/librusty_v8.a` 压缩归档和 registry `src_binding_ptrcomp_release_aarch64-apple-darwin.rs` 注入，`thread_queue_jsonrpc` 5/5、`mcp_exact_jsonrpc` 3/3、`environment_jsonrpc` 3/3、`thread_revert_jsonrpc` 5/5、`runtime::provider_history` prefix 单测 1/1、`tool-runtime filesystem_gateway` 4/4、`lime-agent runtime_state::mcp_runtime_tests` 8/8、`lime-mcp --lib` 162/162 全部通过。Revert 运行期间发现 append-only JSONL 的序号生成错误：rollback 后 effective `StoredSession.events.len()` 小于物理尾序号，导致重复 revert 写入序号回退；已改为从当前持久事件最后 sequence 单调递增，并对溢出 fail closed。active interrupt fixture 改为真实 `run_json_lines` transport，正确观察 `turn/completed(interrupted)` 与 `thread/reverted` 异步通知；cold resume 带 `excludeTurns=true`，符合 paginated exact contract。Provider history 运行证据确认 rollback 后第三轮保留 prefix、移除被撤销第二轮输入。D-2/D-3 公共 integration 与 owner evidence 已不再受 V8 HTTP 404 阻塞；剩余仍为远程 process/cold resume、Queue/Project/Revert/MCP/Environment 独立 Electron Gate B、Strict Review/D-1 provider Electron evidence、C-3 Windows/MSVC/真机和 E live provider/verifier。
- 2026-08-24：关闭 Strict Review 与 D-1 provider capability 公共运行证据缺口。新增 `strict_review_jsonrpc`，通过真实 `run_json_lines` 双连接验证已订阅同一 Thread 时 `experimentalApi` gate、`guardian.review.started -> item/autoApprovalReview/started + autoApprovalReview/strictReviewRequired`、canonical thread/turn identity，1/1 通过。`model_provider_capabilities_jsonrpc` 首次运行暴露 fixture 把默认 `openai` Chat Completions 路由误期望为 Responses hosted capability；fixture 改为显式 `openai-response` 后，official OpenAI Responses 与兼容网关 fail-closed 1/1 通过。D-3 remote process WebSocket transport 1/1、socket-loss reconnect 1/1 也已运行通过；由于仍是 owner 层测试，公共 Thread/Turn JSON-RPC 与 cold restart/resume 继续 evidence-pending。计划仍为 `active`，剩余为各能力独立 Electron Gate B、D-3 public/cold evidence、C-3 Windows/MSVC/真机和 E live provider/verifier。
- 2026-08-24：关闭 D-1 provider capability 独立 Electron Gate B。新增 `smoke:model-provider-capabilities-electron-gate-b`，在隔离 `openai-response + api.openai.com` 配置和 unavailable backend 下启动真实 Electron，从首页点击 ModelSelector，稳定 DOM 显示三项 capability 状态 `[false,true,true]`；结构化 trace 命中 `electron-ipc -> app_server_handle_json_lines -> modelProvider/capabilities/read` 1 次，console/page/invoke/mock error 均为 0，截图确认 capability panel 完整可见。纯 evidence guard 3/3 与真实 fixture 均通过；证据位于 `.lime/qc/gui-evidence/model-provider-capabilities-electron-gate-b/`。D-1 当前完成，但不声称 live provider/model turn；下一刀进入 Queue 独立 Electron Gate B。
- 2026-08-24：关闭 A-Queue 独立 Electron Gate B。新增 `smoke:thread-queue-electron-gate-b`，在隔离 userData/unavailable backend 中经真实 preload 执行 `thread/start -> thread/queue/add`，从侧栏打开同一 canonical Thread 后由 GUI safeInvoke 命中 `thread/read -> thread/queue/list`；header、`thread-queue-status` 与唯一 submission marker 可见，四段 identity 一致，console/page/invoke/mock error 均为 0。纯 evidence guard 4/4 与真实 fixture 通过，证据位于 `.lime/qc/gui-evidence/thread-queue-electron-gate-b/`。首轮运行还证明 reload 会重置 safeInvoke trace，因此 setup 使用真实 preload 调用结果、GUI read 使用 trace 分层取证，不伪造 setup trace。截图暴露 pending 文本在时间线与队列条重复显示，登记为后续视觉层级收口，不影响 current Queue transport/identity Gate B 结论。
- 2026-08-24：关闭 D-3 remote process 公共 Thread/Turn 与 cold resume 证据缺口。首次真实 `environment_remote_turn_jsonrpc` 运行发现三层 production lowering 缺口：省略 `turn/start.environments` 未继承 canonical Thread metadata，primary Environment cwd 只进入 world-state 未进入工具执行 context，原生工具默认 Environment 仍为 `local`；修复后又由公共链路暴露 `unified_exec` 对远端虚拟 cwd 错做本机 `canonicalize`，现仅 `local` 保持本机目录校验，非 local cwd 原样交给 exec-server。最终 fixture 1/1 通过两轮 `provider tool call -> process/start/read -> commandExecution(completed)`，第二轮重建 App Server、恢复持久 registry、`thread/resume(excludeTurns=true)`，并在省略 Turn Environment 参数时继续命中同一远端 cwd。定向验证：App Server typed world-state 1/1、sticky Environment parser 1/1、agent primary/provenance 2/2、tool-runtime remote cwd 1/1、公共 fixture 1/1；Rust fmt 与 scoped `git diff --check` 通过。D-3 仍为 `[~]`，剩余只计稳定 GUI lifecycle consumer、五语言/组件回归与独立 Electron Gate B；当前执行顺序推进到 Project GUI 产品面。
- 2026-08-24：关闭 A-Project GUI 产品面与独立 Electron Gate B。新增 `projectDirectory` typed gateway、`useThreadProjectDirectory` notification consumer 和 `ThreadProjectSelector`，支持 opaque cursor 全量目录、Thread 归属恢复/切换/清空、当前工作区创建并归属，普通对话由 `ChatNavbar` 承接，Task Center 由真实 `ThreadWorkspaceHeader` actions 承接；五语言新增 55 条资源。首次 Gate B 暴露空 Thread 尚无 timeline read model 时入口被隐藏，以及 Task Center 可见页头 owner 不是 `ChatNavbar`；修复为优先使用 canonical read model、可见页头 `sessionId` 与 Task Rail identity，并补稳定回归。最终 Project 定向回归 7 files / 52 tests、`npm run typecheck`、scoped ESLint、`npm run i18n:check`（100%）、`npm run test:contracts`（299 checks）、`npm run governance:scripts`、`npm run verify:gui-smoke` 与 `git diff --check` 通过。`smoke:project-directory-electron-gate-b` 经真实 Electron 点击完成 Alpha -> Beta 切换，5 个必需 current method 全命中，53 次 electron-ipc、4 次 canonical Thread read，mock/console/page/invoke error 均为 0；证据位于 `.lime/qc/gui-evidence/project-directory-electron-gate-b/`。A-Project 当前完成，计划仍为 `active`，下一刀进入 Strict Review GUI 产品面。
- 2026-08-24：关闭 Strict Review GUI 与独立 Electron Gate B。`StrictReviewStatus` 在现有 Composer/approval surface 消费 exact `autoApprovalReview/strictReviewRequired`，显示 review identity、required 状态和下一步动作；五语言资源、组件回归与 `smoke:strict-review-electron-gate-b` 均通过。真实 Electron Gate B 证据命中 `thread/start`、`thread/resume`、`turn/start`、`app_server_handle_json_lines` 与 `app_server_drain_events`，canonical Thread/Turn identity、strict-review method、GUI 状态一致，54 次 electron-ipc，mock/invoke/console/page error 均为 0；证据见 `.lime/qc/gui-evidence/strict-review-electron-gate-b/strict-review-electron-gate-b-summary.json`。该证据不冒充 live provider 或真实危险命令。
- 2026-08-24：关闭 D-2 MCP event stream GUI consumer 与独立 Electron Gate B。`useMcpEvents` 通过 typed `mcpServer/event/stream/notification` 消费 active/event/terminated，`McpEventStreamStatus` 在现有 MCP Runtime 明细展示订阅 ID、事件数、重连态和终态，五语言与组件回归通过。`npm run smoke:mcp-event-stream-electron-gate-b -- --timeout-ms 30000 --keep-temp` 先构建 renderer，再使用 `runtime` backend、localhost provider route preflight、Thread-owned stdio MCP 和真实 preload/IPC/App Server JSON-RPC 验证 active/event/terminated、重连态和终态；Gate B summary 记录 `app_server_handle_json_lines` 49 次、`app_server_drain_events` 8 次、Electron IPC 57 次，request/invoke/console/page/mock/legacy error 均为 0，截图和结构化证据见 `.lime/qc/gui-evidence/mcp-event-stream-electron-gate-b/`。该 Gate 不调用模型回合或 live provider；D-3 Environment lifecycle GUI/Gate B 与 Thread Revert GUI 仍是下一刀。
- 2026-08-25：修复真实 Electron `cancel -> continue` 场景中的正文重复投影。根因是共享 `AppServerEventBus` 的 `includeRecent: true` 订阅在每次 `drainEvents` 都把 Electron Host 的 `#recentNotifications` 与实时结果合并，导致最近的 `item/agentMessage/delta` 被持续回放；backend ledger、App Server durable history 和 route tombstone 均未重复，故 route 关闭不能解决该活动 route 的回放问题。`AppServerEventBus` 现在按 subscription 记录 `completedRecentReplays`：`includeRecent` 只在首次成功 drain 生效，后续仅消费实时 drain；首次失败可重试，unsubscribe/reset 清理 replay 状态。同步保留 cancel 前关闭 route、session/turn tombstone、response/drain 稳定镜像去重，并删除临时诊断日志。定向 Vitest 4 files / 85 tests 通过，Prettier 与 `git diff --check` 通过；真实 Electron `cancel-then-continue` Gate B 与独立 `cancel` 场景均通过，停止态正文与继续回合摘要各只出现 1 次，旧/新回合未串线，`dedupeGuardHits`、`scopedDedupeGuardHits`、console/page/invoke error 均为空；证据见 `.lime/qc/gui-evidence/claw-chat-current-fixture/cancel-then-continue-dedup-20260825-summary.json` 及 `verify:gui-smoke` summary。计划仍保持 `active`，本轮只关闭重复投影缺陷，不宣称整个 Codex Desktop 对齐完成；下一刀继续 Environment lifecycle GUI/Gate B 与 Thread Revert GUI。
- 2026-08-25：关闭 D-3 Environment lifecycle GUI consumer 与独立 Electron Gate B。`readEnvironmentRuntimeStatuses` 通过 typed current `environment/status` 首次读取选中 Environment，`useThreadEnvironmentLifecycleStatus` 从 canonical Thread metadata 恢复 identity 并消费 `thread/environment/connected|disconnected`；现有 `TaskCenterEnvironmentPanel` 展示 connected/pending/disconnected，工具栏同步状态点，五语言、稳定 DOM 和组件回归已完成。首轮 Gate B 暴露本地 exec-server fixture 重连过快：App Server 在同一 drain 批次发布 disconnected 与 connected，React 最终只渲染 connected；根因位于验收时序而非生产投影，fixture 改为断线后暂缓第二次握手，GUI 取证后再显式放行，不在生产路径增加延迟。最终 `smoke:environment-lifecycle-electron-gate-b` 经真实 Electron、preload/IPC、`app_server_handle_json_lines`、App Server 与本地 WebSocket exec-server 证明 connected -> disconnected -> reconnected，同一 Environment/Thread identity、两次远端连接均完成 initialize/info/status，62 次 electron-ipc，mock/invoke/console/page error 均为 0；不启动 Turn、不调用模型。定向回归 6 files / 39 tests、scoped ESLint、`node --check`、`npm run i18n:check`（100%）、`npm run typecheck`、`npm run test:contracts`（299 checks）、`npm run governance:scripts`、`npm run verify:gui-smoke` 与 `git diff --check` 通过。证据见 `.lime/qc/gui-evidence/environment-lifecycle-electron-gate-b/`。D-3 当前完成，计划仍保持 `active`，下一刀进入 Thread Revert GUI/Gate B。
- 2026-08-25：关闭 Thread Revert GUI consumer 与独立 Electron Gate B。新增 typed Renderer gateway、用户消息旁的共享 `ThreadRevertTrigger`、确认/错误状态 `ThreadRevertDialog` 和五语言文案；canonical Turn 与 fallback 消息投影复用同一 action。真实 Gate B 首轮暴露 `refreshSessionReadModel` 只 merge、无法删除 Revert 后 canonical read model 已不存在的 Turn/Item；现新增显式 `timelineMode=replace`，仅 Thread Revert 使用，普通 streaming/terminal refresh 保持 merge。最终 `smoke:thread-revert-electron-gate-b` 通过真实 Electron GUI 精确点击第二轮回退，两个 canonical Turn 均先完成，随后第一轮保留、第二轮移除、Thread header 与 workspace 文件不变；trace 命中 `electron-ipc -> app_server_handle_json_lines -> thread/revert -> thread/read` 和 `app_server_drain_events`，8 次 electron-ipc，mock/invoke/console/page error 均为 0。定向回归 7 files / 57 tests、`npm run typecheck`、scoped ESLint、Prettier、`npm run i18n:check`（100%）、`npm run governance:scripts`、`npm run smoke:agent-runtime-current-fixture` 与 `npm run verify:gui-smoke` 通过。证据见 `.lime/qc/gui-evidence/thread-revert-electron-gate-b/thread-revert-electron-gate-b-summary.json`。计划保持 `active`，该 Gate 不证明 live provider、packaged app 或 Windows。
- 2026-08-25：把 Codex method 产品范围矩阵从历史 `c9c6c0d` 刷新到当前 `99660ab3`。通过上游 `protocol/common.rs` commit 集合差核实新增 24 个方向化 identity：Queue 7、Project 9、Revert 2、Strict Review 1、MCP event stream 3 与 Bedrock account 2。前 22 项均已存在 Lime 同名 generated manifest、current owner、公共/owner 运行证据与独立 Electron Gate B，更新为 current；Bedrock discover/setup 仅服务 Codex account-auth，归 `product-scope-excluded / forbidden-to-restore`，Lime AWS route 继续由 `model-provider` provider-neutral credential owner 承接。矩阵现为 245/245 唯一 identity，方向 `155 client request / 11 server request / 78 server notification / 1 client notification`，状态 `168 current / 3 Windows planned / 74 excluded`；治理守卫新增当前 22 项同名 contract 与 Bedrock 禁入断言。V2 Event Projection 72 项 fixture 明确保留为旧 schema/hash 基线，不冒充当前 78 条 notification 全集。计划保持 `active`，method 产品范围 98.2% 不代表字段、GUI、恢复、live 或平台完成。
- 2026-08-25：完成 MCP v2 status/resource consumer 收口。Renderer `listServersWithStatus` 删除旧 `{servers}` 正向兼容，遍历 `{data,nextCursor}`、拒绝非法/重复 cursor，并把 exact `runtimeStatus/pluginId/authStatus` 投影给 Settings；current/live/Electron smoke 全部改读 v2 status shape。Workspace MCP App 从 canonical Item 恢复 `toolItemId` 与 `app_context.connectorId`，调用 `mcpServer/resource/read` 时发送 `originCallId/connectorId`，空 identity fail closed。定向 Renderer 3 files / 37 tests、smoke 2 files / 11 tests、app-server-client 130/130、`npm run typecheck`、脚本语法和真实 `npm run smoke:mcp-current` 通过；summary 中 `statusServers=1`、`missingReadMethods=[]`、legacy command 0。App Server projection 定向 Rust 20/20 也通过，旧 V8 404 阻塞项关闭。服务端 call-scoped resource selection、connector meta lowering 与精确 auth enum 仍未完成，计划保持 `active`。
- 2026-08-25：完成 MCP v2 resource 服务端最小正确切片。`originCallId` 无 `threadId` 时 fail closed；只有 `codex_apps` 消费 origin，并从 canonical Thread 完整历史要求 completed MCP Item、server 与 resource URI 一致，非 Apps server 按 Codex 忽略 origin。`connectorId` lowering 为 `x-codex-turn-metadata.mcp_request_meta.selected_connector_ids`，通过 rmcp request extensions 写入真实 `resources/read.params._meta`，不再只回填 App Server response。验证：App Server helper 2/2、公共 `mcp_exact_jsonrpc` 3/3、`lime-agent` MCP runtime 8/8、`lime-mcp --lib` 163/163、`smoke:mcp-current`、contracts 299 checks、legacy 2063 files/0 violation、scripts governance、通用真实 Electron `verify:gui-smoke` 与 `git diff --check` 均通过。计划保持 `active`：bounded provenance/link/account authority、canonical `appContext`/cold restore 和 MCP resource 独立 Gate B 未完成，producer 脏热区继续避让；下一刀先处理这些 owner，随后做 Browser/Computer Use requirements 产品裁决。
- 2026-08-25：关闭 MCP status exact auth lowering。Rust v2 enum 从旧 lowercase wire 收敛到 Codex exact `notLoggedIn/bearerToken/oAuth`，同步 schema fixtures、generated TypeScript 与 Renderer consumer；LocalAppDataSource 现在将 OAuth `available=false` 投影为 `NotLoggedIn`，已授权 OAuth、静态 bearer、无认证与未知状态保持唯一映射。验证：auth lowering 1/1、protocol exact wire 1/1、Renderer MCP 18/18、contracts 299 checks、`smoke:mcp-current` 与真实 Electron `verify:gui-smoke` 均通过。D-2 status/resource 仍保持 `[~]`，剩余 bounded provenance、link/account authority、canonical `appContext`/cold restore 和专用 Gate B。
- 2026-08-25：关闭 D-2 MCP resource origin。冻结的 MCP step route 现在生成 canonical `appContext`，Thread full history 派生 64 条/单条 1024 bytes 的 bounded origin ledger，resource read 重新校验 completed Item、URI、connector、link/account 与当前 tool metadata，并只把 canonical authority lowering 到真实 MCP wire；Workspace `codex_apps` surface 不再把缺失 `pluginId` 误判为未安装。独立 `smoke:mcp-resource-origin-electron-gate-b` 两次启动真实 Electron/App Server，证明首轮和冷恢复使用同一 Item/appContext、WebContentsView resource marker 可见、伪造 Renderer connector 无效、URI mismatch fail closed、恢复未重跑 provider/tool，mock/legacy/invoke/console/page error 均为 0。证据位于 `.lime/qc/gui-evidence/mcp-resource-origin-electron-gate-b/`；D-2 resource 当前完成，但不声称 live provider、remote connector、compaction、packaged app 或 Windows。
- 2026-08-25：关闭 Agent current 聚合门禁的图片 Token usage replay 时序缺陷。根因是图片 Turn 运行中即启用 `thread/tokenUsage/updated` recent replay，唯一 replay 在终态前被消费，导致图片任务虽 completed，但 GUI 缺失 `31.0K Tokens`。`useAgentSessionTokenUsage` 现在只在图片消息缺 usage 且对应 Turn 已终态时补读，不放宽 Gate B 断言；单元回归 17/17，图片单场景中 `hasTokenUsage=true`、`cardCount=1`、`mediaCount=1`、read model Turn=`completed`。随后完整 `npm run smoke:agent-runtime-current-fixture` 通过，覆盖图片命令、active Turn steer、Skills Runtime、Expert Plaza、Expert Panel Skills follow-up、approval/cancel/continue、Plan、MCP、media/content factory；证据见 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-image-command-replay-fix-summary.json` 与 `.lime/qc/gui-evidence/claw-chat-current-fixture/claw-chat-current-fixture-expert-panel-skills-runtime-regression-summary.json`。
- 2026-08-25：完成最新通用 GUI 门禁复核。`npm run verify:gui-smoke` 通过，Shell-01 真实 Electron startup/reload、preload/IPC、App Server JSON-RPC、Workbench/Settings 共 21/21 assertions；证据见 `.lime/qc/project-gates/standalone-shell-01-20260825120315-58733/shell-01-electron-smoke/summary.json`。`npx tsc -p tsconfig.renderer.json --noEmit --pretty false` 同时通过。该 Shell-01 明确不证明 provider Turn、packaged app 或 Windows；下一刀仍是隔离构建并直启 packaged executable。
- 2026-08-25：关闭 macOS arm64 本地 packaged candidate 证据。先核实旧 `release-electron` 为 2026-08-10 的 `1.125.0` 包且缺少 `code-mode-host`，资源校验失败，因此保留原目录不覆盖；随后以 `LIME_ELECTRON_FORGE_OUT_DIR=.tmp/codex-alignment-packaged-20260825 npm run electron:package:dir` 从当前 `1.134.0` 源码隔离构建。`verify-package-resources.mjs` 验证 Lime 主/Helper bundle identity、`com.limecloud.lime`、主包 deep strict signature、桌面资源，以及 app-server/code-mode-host 两个 sidecar；两个 sidecar 因 macOS ad-hoc signing 改写 Mach-O，按严格 signature 分支通过。通过 `LIME_ELECTRON_SMOKE_EXECUTABLE=.tmp/codex-alignment-packaged-20260825/Lime-darwin-arm64/Lime.app/Contents/MacOS/Lime` 直接启动包内 executable，Shell-01 startup/reload、preload/IPC、App Server JSON-RPC、Workbench/Settings 21/21，通过证据见 `.lime/qc/project-gates/codex-alignment-packaged-20260825/shell-01-packaged-electron-smoke/summary.json`；该通用 summary 单独仍是 Gate B-F，packaged claim 由明确 executable 注入、资源校验与 summary 三者组合建立。`smoke:app-server-packaged-external-backend-failure`、`governance:electron-release-workflow`、`verify:app-version` 同时通过。Forge ZIP/local feed 生成 `.tmp/codex-alignment-local-feed-20260825/make/zip/darwin/arm64/Lime-darwin-arm64-1.134.0.zip`（328 MiB，SHA-256 `ba52fc922d5d67f2f34bc5a16a6a4151b35f542b5112a59662b0efa1bce12700`），`RELEASES.json.currentRelease=1.134.0`，`unzip -t` 无错误。首次直接 `node make-zip-local-feed.mjs` 因缺 npm 上下文触发 Corepack pnpm signature key 错误，改走仓库规定的 npm script 后通过；未做全局安装或配置修改。本证据属于本机 macOS arm64 local platform/packaged，不证明 Developer ID、notarization、stapling、DMG、正式 updater endpoint、Windows 或 live provider。
- 2026-08-25：修复 Agent Runtime provider stream 的独立性与取消/终态竞态。对照 Codex `core/src/client.rs` 的 response-stream producer，`agent-runtime/provider_turn` 现在由独立 Tokio producer task 建立 bounded event channel，provider 初始化、首输出/step deadline 与取消互不阻塞；取消时保留同一 raw poll 已生成的 usage，producer 发送优先 `try_send`，避免取消与事件入队竞态。补回归：同 poll usage、session token usage flush、code mode cancellation、首输出 deadline 不受同步阻塞 provider `poll_next` 影响；`npm run test:rust:unit -- -p agent-runtime cancellation_` 3/3、deadline 1/1，`npm run test:rust:related` 全链 `agent-runtime 211`、`app-server 264`、`lime-scheduler 36`、`lime-server 119` 与 `lime-agent` 通过。同步新增 `model-provider` Node 风格 chunked keep-alive SSE 回归，要求 `[DONE]` 后 1 秒内产生 `Finish`，定向测试 1/1 通过，确认 TCP keep-alive 不是终态等待根因。重建真实 App Server sidecar，SHA-256=`8621aa1d934459f76721d7484fabccb30c56b0ba9ae17effe06415599a591886`。
- 2026-08-25：Desktop controlled 5 连续两轮通过。单跑 `go-genai-streamed-function-args`、第一轮完整套件和第二轮完整套件均为 Gate B 全通过；每轮覆盖真实 Electron、preload/IPC、App Server JSON-RPC、provider/tool lifecycle、artifact preview、approval resume、cancel no-ghost-write 与 cold restart。证据目录分别为 `.lime/benchmark/v2/desktop/controlled/20260825T143350Z/` 和 `.lime/benchmark/v2/desktop/controlled/20260825T143916Z/`，每轮 5/5；受控 fixture 只证明 Desktop product path，不证明 DeepSWE verifier、Pier/patch match 或 live provider。此前 Go 任务的间歇性 `terminal-wait` 已在 producer 重建后连续复现通过；失败证据显示 provider response 已 `finish`，后续 SSE 回归确认解析层及时终止。
- 2026-08-25：启动 `npm run smoke:agent-runtime-current-fixture` 与 `npm run verify:gui-smoke` 共享回归。current fixture 的历史/缓存恢复 31/31、流式完成/运行态 32 个有效用例、Electron/App Server guard 99/99 已通过；后续 Electron fixture 构建等待共享 `.lime/electron-fixture-build.lock`，该锁由当前 smoke 自己持有但底层全 workspace Rust/App Server 编译正被并行任务占用，未提前标记 smoke 或 GUI 完成，待编译自然结束后继续收尾。
- 2026-08-25：完成 current Agent Runtime 聚合回归。`npm run smoke:agent-runtime-current-fixture` 在构建锁释放后完整通过，覆盖真实 Electron 的 unknown-item fail-visible/recovery、首页热路径/短问候、Coding Workbench、图片命令/自然语言画图、cancel->continue、approval resume/decline/cancel/full-access、Inputbar rich restore、active steer、Plan history hydrate、Skills Runtime 三入口、MCP structuredContent、media reference、Expert Plaza/Panel follow-up、typed error retry success/failure 与 Content Factory Article Editor；所有 fixture 保持 `liveProviderUsed=false`。
- 2026-08-25：完成最新 GUI smoke。`npm run verify:gui-smoke` 通过 renderer 9,003 modules、Electron Host/App Server sidecar 重建及 Shell-01 startup/reload、preload/IPC、App Server JSON-RPC、Workbench/Settings 21/21 assertions；证据见 `.lime/qc/project-gates/standalone-shell-01-20260825151622-41503/shell-01-electron-smoke/summary.json`。本证据不证明 provider Turn、packaged app、Windows、Developer ID/notarization 或 live provider。
- 2026-08-25：继续 C-3 Windows current owner 收口。`windowsSandbox/setupStart` 不再无条件固定 fail-closed：只有 `plan_sandbox_backend` 返回 `SandboxBackendStatus::Ready` 且 `enforced=true` 时才允许异步 setup completion 成功；当前 Windows 仍为 `Planned/enforced=false`，macOS 仍明确返回仅 Windows 可用，因此现有安全边界和 UI readiness 不变。`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check` 与 `git diff --check` 通过。随后改用 `npm run test:rust:unit -- -p app-server processor::windows_sandbox::tests`，仓库统一入口成功复用已校验 SHA-256 的 `ptrcomp_sandbox_release` V8 artifact，确认旧 HTTP 404 不再是该入口的 blocker；普通增量构建在写 `dep-graph.part.bin` 时因 `ENOSPC` 失败，`CARGO_INCREMENTAL=0` 重试也在最终 App Server test binary 的 LLVM 输出阶段因 `ENOSPC` 失败，因此仍未把 4 个 Rust 单测记为通过。当前源码已由同轮 Forge package 生成新的 `libapp_server` 与 App Server executable，编译证据存在，但不能替代 test runtime。Windows/MSVC 真机、Windows packaged Squirrel Gate B、正式 macOS Developer ID/notarization 与 live provider/verifier 仍是 evidence-pending。
- 2026-08-25：补强 Windows packaged Gate B 证据语义。`scripts/electron/windows-squirrel-rc-smoke.mjs` 在非 Windows runner 上现在产出 `result: "evidence-pending"`、`failedStage: "windows-runner"` 和明确 blocker，同时保持非零退出码；在真实 Windows runner 上的 pass/fail 逻辑不变。macOS 本机运行已生成 `.tmp/windows-squirrel-rc-macos-evidence/summary.json`，确认没有安装器、Squirrel、快捷方式或 Electron 断言被伪造。`npx vitest run scripts/electron/windows-squirrel-rc-smoke.test.mjs` 17/17、`npm run test:contracts` 299 checks、`npm run governance:scripts`、`npm run governance:legacy-report`（2063 文件、0 边界违规/分类漂移）及 `git diff --check` 通过。真实 Windows/MSVC、packaged Squirrel、正式 macOS 分发和 live provider/verifier 仍未获得外部 evidence。
- 2026-08-25：启动当前 `1.135.0` macOS Developer ID candidate 构建。本机两张 `Digital Easy Inc. (93RF7VQ26C)` identity 均有效，但 Forge package 在磁盘仅余约 `333 MiB` 时以 `ENOSPC` 中止；失败临时目录 `${TMPDIR}/electron-packager` 约 `692 MiB`，已完成的 `1.134.0` ad-hoc candidate `.tmp/codex-alignment-packaged-20260825` 约 `1.1 GiB`。本轮未在无确认下删除任何文件，也未把历史 `1.131.0` Developer ID 包当作当前版本证据。下一步需明确确认清理上述构建缓存后重跑 package、`codesign --verify --deep --strict`、资源 verifier 与包内 executable Shell-01。Apple notarization 三项环境变量仍缺失，notarization/staple 不会随本地签名构建自动关闭。

### 本轮进度（2026-08-26）

- 2026-08-26：真实 Windows run `32947104967`（SHA `22b4262aa`）完成 2/7、失败 5/7。pipe 路径诊断为 `CreateProcessWithTokenW=success`、`ResumeThread=success`、进程持续 `STILL_ACTIVE` 但不执行 workspace marker/stdin/output；ConPTY 路径固定 `ERROR_INVALID_PARAMETER`。结合宿主 `CreateProcessAsUserW=ERROR_PRIVILEGE_NOT_HELD`，已排除 plain/extended `STARTUPINFO`、desktop、error mode、profile load、stdio allowlist 与命令行重建等单点原因，停止继续修补 fallback。下一实现按 Codex current 改为 `CreateProcessWithLogonW` 启动 sandbox-account runner，再由 runner 的当前 token 派生 restricted token并通过 framed named pipe 交接 I/O/control；本条仅记录根因与方向，C-3/readiness 仍未完成。

- 关闭 A-Queue 视觉重复：`ThreadQueueStatus` 删除 submission preview/`thread-queue-items`，只保留标题、数量、loading/error；五语言删除无消费者的 preview/more key。补充组件负向回归，Gate B 改为断言 canonical 时间线 marker 唯一、Queue 状态条不含 marker、旧列表节点不存在。
- 验证证据：Queue 定向组件/Gate B/Sherpa 25/25，真实 `smoke:thread-queue-electron-gate-b` 通过（47 次 electron-ipc、`timelineMarkerCount=1`、`queueStatusContainsMarker=false`、`queueItemsPresent=false`、mock/console/page/invoke error 0）；`npm run i18n:check` 100%；`smoke:agent-runtime-current-fixture` 完整通过；`npm run verify:gui-smoke` 21/21；`npm run test:contracts` 299 checks；renderer/node typecheck、scripts governance、legacy governance（2063 文件，0 违规/漂移）、Rust fmt/diff check 通过。
- 修复 CI 可观测性：根 `package.json` 与 lock importer 增加直接 `ws@8.18.3`；`.github/workflows/quality.yml` 的 `windows_shell_runtime` 从 20 分钟调为 60 分钟；Sherpa 旧目录清理、tar 解压前后加入阶段日志；新增 workflow/source 阶段顺序回归。`npm run test:rust:unit -- -p app-server processor::windows_sandbox::tests` 实际运行 4/4，通过 backend readiness/enforced fail-closed 边界。
- 强化 Windows 真机矩阵：`windows_restricted_execution` 新增 workspace 与 `.git/.codex/.agents` 的 SDDL 基线/最终一致性断言，明确证明短生命周期 capability SID ACL lease 回滚；新增 allowlisted stdin handle -> child output 回归；App Server 增加 Windows `timeoutMs -> exitCode 124` 回归。Quality job 将 restricted matrix 前移到 Sherpa/sidecar 之前，使重型准备失败时仍优先产出安全证据；timeout contract 在 sidecar build 后运行。Rustfmt、顺序守卫 13/13 与 contracts 299 checks 已通过，Windows-only 编译/运行仍待 runner。
- 补录同一基线 SHA `0975674a` 的外部 Release run `32864956267`：macOS arm64/x64 Developer ID、notarization/stapling、deep-strict codesign、DMG/ZIP/RELEASES 发布均成功；Windows-x64 Squirrel N-1 packaged Gate B 16/16，安装后 Shell-01 21/21。Windows evidence artifact 为 `9570576514`，summary SHA-256=`f18b45169f11dfebf750627f480d03f7f6f7f1c763f5902fc4ee1ceafae320b5`。
- 本轮完成当前工作树 `1.135.0` arm64 Developer ID candidate：使用 System Keychain 精确 identity `DE71B9673ECED29A0DCA5D11CC498B072C7AA860`，Forge package 成功；资源 verifier 与 `codesign --verify --deep --strict` 通过，`codesign -dvvv` 显示 hardened runtime、TeamIdentifier `93RF7VQ26C`、Developer ID authority、timestamp。包内 executable Shell-01 21/21 通过，真实 `electron-ipc -> app_server_handle_json_lines -> App Server JSON-RPC` 命中 56 次，零 console/page/invoke/legacy/mock/crash/unresponsive error；证据见 `.tmp/codex-alignment-packaged-current-20260826/` 与 `.lime/qc/project-gates/standalone-shell-01-20260825171233-4797/shell-01-electron-smoke/summary.json`。资源 verifier 对 sidecar hash 差异按 `macos-signed-sidecar` 分支接受，不能解释为 manifest hash 完全一致。
- 本轮 Rust related 验证通过：`npm run test:rust:related -- lime-rs/crates/app-server/src/command_exec/tests.rs lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs lime-rs/crates/app-server/src/processor/windows_sandbox.rs` 退出码 0；tool-runtime 本机 363/363 通过，Windows `cfg(windows)` restricted matrix 仍需真实 Windows/MSVC runner。
- 本轮复验 Queue/Sherpa/Windows evidence collector 组合 39/39、`npm run i18n:check` 100%、`npm run test:contracts` 299 checks、scripts/release governance 与 `git diff --check` 通过。
- 本轮完成默认本地综合门禁：`npm run verify:local` 通过，包含版本一致性、i18n 100%/0 unused、renderer/node typecheck、118/118 Vitest 批次、contracts 299 checks、workspace `cargo test`、scripts/release/docs governance 与真实 Electron `verify:gui-smoke`。Rust workspace 核心结果包括 `agent-runtime` 1717/1717、`tool-runtime` 363/363、Queue/Revert/MCP/Environment 公共 JSON-RPC 集成全通过；最新 Shell-01 证据为 `.lime/qc/project-gates/standalone-shell-01-20260825175059-77624/shell-01-electron-smoke/summary.json`，21/21 assertions、真实 App Server JSON-RPC、无 mock/console/page/invoke error。
- 本轮复核 DeepSWE/Pier 外部证据前置条件：`npm run harness:deepswe:preflight -- --slice release-20` 与 `npm run harness:deepswe:desktop:preflight` 均为 `status=pass`，固定 `datacurve-pier==0.3.1`；运行时 prerequisite 明确为 `pier=0.3.1`、`container=spawnSync docker ENOENT`，因此当前仍是 `evidence-pending`，没有启动模型回合、网络 Provider 或 verifier。
- 本轮完成 DeepSWE harness 定向回归：`npx vitest run scripts/harness/deepswe-adapter.test.mjs scripts/harness/deepswe-desktop-benchmark.test.mjs scripts/harness/deepswe-desktop-contract.test.mjs scripts/harness/deepswe-coding-slice.test.mjs`，4 个文件、43/43 tests 通过；覆盖 pinned source/task preflight、隔离 workspace、patch capture、upstream drift 拒绝和 Pier 版本拒绝。
- 本轮文档/治理收尾复验：`npm run governance:scripts` 通过（root/一级目录冻结基线无漂移），`npm run governance:legacy-report` 通过（2063 文件、0 零引用候选、0 分类漂移、0 边界违规），`git diff --check` 通过。
- 本轮补齐 Windows 矩阵证据采集 owner：`scripts/lib/windows-restricted-execution-evidence.mjs` 升级为 schema v2，逐项解析 required case，拒绝缺失、重复、ignored、FAILED 和 unexpected 场景；Quality 在安全矩阵失败时仍上传结构化 summary 和原始 stdout/stderr。本机 macOS 运行明确产出 `result=evidence-pending/failedStage=windows-runner` 并返回非零，未伪造平台通过。Windows 真机运行仍是唯一证据闭环条件。
- 本轮新增 runner 后复验：7 个定向 Vitest 文件 76/76 通过，`npm run test:contracts` 通过（299 checks、协议无漂移、脚本/Release/docs boundary 全通过）。
- 新增脚本后的治理复验仍为零漂移：`npm run governance:legacy-report` 通过（2063 文件、0 零引用候选、0 分类漂移、0 边界违规）。
- 本轮 `npm run verify:local` 已完整退出码 0：118/118 Vitest smart batches、renderer/node typecheck、contracts 299 checks、workspace `cargo test`（含 App Server public JSON-RPC、agent-runtime、tool-runtime、MCP、Queue、Revert、Environment 集成与 doc tests）、scripts/release/docs/legacy governance 全部通过；随后真实 Electron `verify:gui-smoke` 重新构建 9,003 renderer modules 与 App Server sidecar，Shell-01 21/21 assertions 通过，证据为 `.lime/qc/project-gates/standalone-shell-01-20260825182903-19049/shell-01-electron-smoke/summary.json`，无 mock/console/page/invoke error。该综合门禁仍不覆盖 Windows/MSVC 真机、live provider/verifier 或当前工作树正式 notarized release。
- 2026-08-26：在当前未提交工作树上重新运行 `npm run verify:gui-smoke`，退出码 0。renderer 重新构建 9,003 modules，App Server sidecar 以 `1.135.0` 重建并通过 `appserver.v0` 初始化；真实 Electron/preload/IPC/App Server JSON-RPC Shell-01 21/21 assertions 通过，证据为 `.lime/qc/project-gates/standalone-shell-01-20260826000801-14256/shell-01-electron-smoke/summary.json`，无 mock/console/page/invoke error。该证据只更新当前工作树 GUI Gate B，不关闭 Windows/MSVC restricted matrix、live provider/Pier/DeepSWE verifier 或当前工作树正式 notarized/package release。
- 实际运行 `node scripts/lib/windows-restricted-execution-evidence.mjs --output .lime/qc/windows-restricted-execution/summary.json`：macOS 正确以退出码 1 生成 `result=evidence-pending`、`failedStage=windows-runner`，并保留 `.lime/qc/windows-restricted-execution/summary.json`、`windows-restricted-execution.stdout.txt`、`windows-restricted-execution.stderr.txt`。该 artifact 明确没有执行任何 Windows case，待真实 Windows/MSVC runner 上传同路径矩阵后才能关闭 C-3。
- 继续补强 C-3 current owner：`windows_restricted_execution` 新增 allowlisted stdin handle -> child output integration case，验证 restricted process 的唯一继承 stdin 句柄可写、关闭并完成终态；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check`、`npm run test:rust:related -- lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs`（相关 workspace tool-runtime 363/363）和 Windows/Squirrel evidence 脚本回归 20/20 通过。本机 Rust targets 仅有 `aarch64-apple-darwin`，该 case 仍需真实 `x86_64-pc-windows-msvc` runner 执行，不能记为平台 evidence。
- 2026-08-26：继续复验 C-3 收口链。上一轮 `npm run test:contracts` 最终退出码 0（299 checks，协议生成无漂移，scripts/release/docs boundary 全通过）；`npx vitest run scripts/lib/windows-restricted-execution-evidence.test.mjs` 为 5/5，`npm run governance:scripts` 与 `npm run governance:legacy-report` 分别通过（2063 文件、0 零引用候选、0 分类漂移、0 边界违规）。`node scripts/lib/windows-restricted-execution-evidence.mjs --output .lime/qc/windows-restricted-execution/summary.json` 在 macOS 按 schema v2 正确返回退出码 1，artifact 为 `result=evidence-pending`、`failedStage=windows-runner`、required 4 cases 全部 missing；未伪造 Windows 通过，C-3 仍等待真实 MSVC runner。
- 2026-08-26：完成 Windows 事实源漂移收口。`internal/aiprompts/architecture.md` 现在明确 setup/warning/complete 是 current typed protocol/App Server/Renderer lifecycle，但真实 backend enforcement、world-writable scan、ConPTY/网络能力和 Gate B 仍未完成；V1 method matrix、V1 fixture、V2 event projection 表同步记录同一 gap，并保留三个端到端 method 为 `planned`。`npx vitest run src/lib/governance/codexMethodProductScopeBoundary.test.ts` 11/11 与 `npm run docs:boundary` 通过。
- 2026-08-26：继续补齐 Windows setup/warning producer。`tool-runtime::execution_process` 新增跨平台 `WindowsWorldWritableAudit`；Windows owner 使用 `GetNamedSecurityInfoW` 检查 Everyone write ACE，限制 2 秒、每目录 1,000 项、总检查 50,000 项、最多 5 个 sample，并跳过 reparse points/指定系统目录；App Server `windowsSandbox/setupStart` 通过 `spawn_blocking` 发布 `windows/worldWritableWarning`，扫描失败、路径命中和 cwd 无法解析均 fail visible，不改变 `SandboxBackendStatus::Planned/enforced=false`。本机 `cargo fmt`、`git diff --check` 与 `npm run test:rust:related -- lime-rs/crates/tool-runtime/src/execution_process.rs lime-rs/crates/tool-runtime/src/execution_process/tests.rs lime-rs/crates/app-server/src/processor/windows_sandbox.rs` 通过（受影响 workspace `364 passed, 0 failed`）；裸 `cargo check -p tool-runtime -p app-server --lib` 仍因 `v8 v150.4.0` 对 `aarch64-apple-darwin` 的预编译 archive HTTP 404 在依赖构建阶段中止，未将其记为编译通过。Windows-only API 编译/运行、restricted execution 真机矩阵、live provider/Pier/DeepSWE verifier 和当前工作树正式 package 仍未完成。
- 2026-08-26：完成 setup/warning producer 变更后的跨层复验。`npm run test:contracts` 通过（299 checks，协议生成无漂移、命令/脚本/发布/docs 边界均通过）；`npm run verify:gui-smoke` 通过，当前工作树 renderer 9,003 modules、App Server sidecar `1.135.0`、真实 Electron/preload/IPC/`app_server_handle_json_lines`/App Server JSON-RPC Shell-01 为 21/21，证据见 `.lime/qc/project-gates/standalone-shell-01-20260826002654-74544/shell-01-electron-smoke/summary.json`，mock/console/page/invoke error 均为 0。该验证不关闭 Windows/MSVC restricted execution 真机矩阵、live provider/Pier/DeepSWE verifier 或当前工作树正式 notarized/package release。
- 2026-08-26：本轮计划与治理收尾复验完成。`npm run docs:boundary`、`npm run governance:scripts`、`npm run governance:legacy-report` 均通过；legacy report 扫描 2063 文件，零引用候选、分类漂移和边界违规均为 0；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check` 与 `git diff --check` 通过。计划状态保持 `active`：Windows restricted execution 真机矩阵、Windows API/ConPTY/网络 enforcement、live provider、Pier/DeepSWE verifier artifacts/patch SHA，以及包含当前工作树改动的正式 notarized/package 仍没有可验证证据，不能标记完成。
- 2026-08-26：Windows evidence collector 增加 `world_writable_audit_reports_everyone_write_acl` required case，真实 Windows runner 将同时验证 restricted process 与 Everyone-write ACL warning producer；`npx vitest run scripts/lib/windows-restricted-execution-evidence.test.mjs` 为 5/5。macOS 执行 collector 重新生成 `.lime/qc/windows-restricted-execution/summary.json`，schema v2 明确 `result=evidence-pending`、`failedStage=windows-runner`、五个 required cases 全部 missing，并以退出码 1 fail closed；该 case 仍需 Windows/MSVC API 编译和真机运行，不能作为当前平台 evidence。
- 2026-08-26：继续加固 Windows ACL audit 与证据入口。`tool-runtime::execution_process` 现在读取目录 `上限+1` 项以识别 1,000 项截断，使用 Windows `FILE_ATTRIBUTE_REPARSE_POINT` 跳过 junction/reparse point，元数据读取失败和被截断扫描均设置 `failedScan=true`；`windows-restricted-execution-evidence.mjs` 将 Cargo 运行固定为 15 分钟超时，超时仍保留结构化失败摘要；Renderer 对 warning notification 限制最多 5 个非空样本、单路径不超过 Windows 最大路径长度，拒绝异常大载荷。`npx vitest run scripts/lib/windows-restricted-execution-evidence.test.mjs` 为 6/6，`npx vitest run src/lib/api/windowsSandbox.test.ts` 为 4/4，相关 Rust workspace 364/364、TypeScript、`cargo fmt -- --check` 与 `git diff --check` 通过。该改动不改变 `SandboxBackendStatus::Planned/enforced=false`，Windows/MSVC 真机执行、live provider/Pier/DeepSWE verifier 与当前工作树正式 package 仍是外部阻塞。
- 2026-08-26：在上述 Windows warning/parser 边界加固后重跑真实 `npm run verify:gui-smoke`，Shell-01 21/21、renderer 8,993 modules、真实 Electron/preload/IPC/`app_server_handle_json_lines`/App Server JSON-RPC 均通过；证据见 `.lime/qc/project-gates/standalone-shell-01-20260826004748-3184/shell-01-electron-smoke/summary.json`，mock/console/page/invoke error 均为 0。该 Gate B 只证明当前 macOS Desktop 主链，不替代 Windows/MSVC restricted matrix、live provider/verifier 或正式 notarized package。
- 当前仍未完成且不能本地冒充：Windows restricted execution 真机矩阵、live provider 与 Pier/DeepSWE verifier evidence，以及包含当前未提交改动的 notarized/正式 package。当前 signed packaged candidate 与 Shell-01 已完成，但计划保持 `active`。
- 2026-08-26：继续 C-3 Windows owner 对照 Codex current `windows-sandbox-rs`，静态复核确认 restricted token 的 capability SID、`TokenDefaultDacl`、`STARTUPINFOEX` handle/job allowlist、ACL lease rollback、Job Object descendant cleanup 与 bounded pipe reader 均已落在 Lime current `tool-runtime::execution_process` owner；发现并修正 `windows.rs` 在 ACL audit 改动后遗漏的 `LookupPrivilegeValueW`、`SetTokenInformation`、`TokenDefaultDacl`、`TokenGroups` Windows-only imports。`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check` 与 `git diff --check` 通过。真实 `x86_64-pc-windows-msvc` 五项 restricted execution matrix、live provider、Pier/DeepSWE verifier 和覆盖当前工作树的正式 package 仍未获得，因此计划继续保持 active 与 fail-closed readiness。
- 2026-08-26：本轮执行 `npm run verify:local` 完整通过：smart Vitest 118/118 批次、完整 Rust workspace/unit/integration/doc tests、`test:contracts` 299 checks，以及真实 Electron Shell-01 21/21；当前 smoke 证据为 `.lime/qc/project-gates/standalone-shell-01-20260826012007-71585/shell-01-electron-smoke/summary.json`，console/page/invoke/preload/crash/unresponsive/mock/legacy 均为 0。重新运行 Windows evidence collector 仍按预期退出码 1，summary 为 schema v2、`platform=darwin`、`result=evidence-pending`、`failedStage=windows-runner`，五项 required case 全部 missing。此次本地门禁不改变外部阻塞：真实 Windows/MSVC restricted execution、live provider、Pier/DeepSWE verifier 和当前工作树正式 notarized/package 仍未完成，计划保持 active。
- 2026-08-26：收尾治理复验通过：`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check`、`git diff --check`、`docs:boundary`、`governance:scripts` 与 `governance:legacy-report` 均通过；legacy report 扫描 2063 文件，零引用候选、分类漂移和边界违规均为 0。当前工作树保留用户/并行改动，未执行提交、推送、重置或分支操作。
- 2026-08-26：继续加固 `tool-runtime::execution_process::windows_acl` 的 Windows fail-closed 边界：`icacls.exe` 改为 `spawn + try_wait`，固定 15 秒超时后 `kill + wait` 并返回 `TimedOut`；ACL 目标 canonicalization 在 `canonicalize` 前遍历所有 ancestor，读取 `symlink_metadata`，拒绝 symlink 与 `FILE_ATTRIBUTE_REPARSE_POINT`，元数据读取失败直接拒绝，避免 junction/reparse path 绕过 ACL 目标解析。新增普通路径与 child wait 单测；`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check`、`git diff --check`、相关 Rust workspace 364/364 通过。`npx vitest run scripts/lib/windows-restricted-execution-evidence.test.mjs` 为 6/6，`npx vitest run src/lib/api/windowsSandbox.test.ts` 为 4/4。macOS 运行 evidence collector 正确以退出码 1 生成 schema v2 `result=evidence-pending`、`failedStage=windows-runner`，五项 required case 全部 missing；真实 Windows/MSVC 编译与 restricted execution matrix、live provider、Pier/DeepSWE verifier 和当前工作树正式 notarized/package 仍未完成，计划保持 active。
- 2026-08-26：跨层合同门禁复验通过：`npm run test:contracts` 完成 protocol generated types 无漂移、App Server client 299 checks、command/harness/modality/release/scripts/cleanup/docs boundary 全部通过；本轮只新增 Windows current owner 逻辑和执行计划记录，没有协议或生成物漂移。
- 2026-08-26：继续完成 C-3 本机可实现缺口。新增独立 `windows_conpty` owner，restricted-token TTY 通过 `CreatePseudoConsole`、`PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE` 与 Job Object 原子附着启动，保留 capability SID/ACL lease，同一 supervisor 承接 stdin、resize、combined output、interrupt/terminate 与 bounded retained output；不再返回 TTY unsupported，也不回退普通 `portable-pty`。Windows 真机矩阵新增 `restricted_conpty_supports_stdin_resize_and_combined_output`，evidence collector 从五项扩为六项。相关非 Windows Rust workspace 回归通过（`tool-runtime` 364/364），证据解析/Renderer 定向 Vitest 10/10、scripts governance、rustfmt 与 diff check 通过；由于本机仅安装 `aarch64-apple-darwin`，新 Windows API 尚未在 MSVC 编译运行，不能标为真机完成。强网络隔离仍只有环境提示而无 WFP/firewall enforcement，因此 `SandboxBackendStatus::Planned/enforced=false` 保持不变。
- 2026-08-26：ConPTY slice 跨层收尾复验通过：`npm run test:contracts` 完成 299 checks、协议生成无漂移、scripts/release/docs boundary 全通过；`npm run governance:legacy-report` 扫描 2063 文件，零引用候选、分类漂移和边界违规均为 0。macOS evidence collector 按 schema v2 返回退出码 1、`result=evidence-pending`、`failedStage=windows-runner`。显式 `cargo check -p tool-runtime --target x86_64-pc-windows-msvc --lib` 在进入 Lime Windows 源码编译前因目标未安装报 `can't find crate for core`；未全局安装 target，故 Windows API/ConPTY 编译与真机运行仍是明确 blocker。
- 2026-08-26：修正 Windows network enforcement 语义。删除未实施 firewall/WFP 时的代理/offline 环境改写与 `SBX_NONET_ACTIVE=1`；restricted runner 现在要求显式 `network.enabled=true` 才允许进入 ACL/token/spawn，缺失或 false 时返回 `PermissionDenied` 且命令不会执行。新增 `restricted_execution_rejects_unenforced_network_before_spawn`，真实矩阵扩为七项；该 fail-closed guard 防止网络泄漏，不代表离线执行能力完成，readiness 继续 `Planned/enforced=false`。
- 2026-08-26：完成 C-3 Windows-only 静态复核与 owner 拆分。ConPTY 的 `CreatePseudoConsole`/`ResizePseudoConsole`/`ClosePseudoConsole`、`PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE` 传参、backing pipe 生命周期及 root exit 后先关闭 ConPTY 再 join reader 的顺序均与 Codex current owner 一致；Job Object 正常退出 descendant preservation 继续持有 ACL lease，interrupt/terminate/等待失败仍终止整棵进程树。为满足仓库非生成文件上限，将 1,400 行 `windows.rs` 拆为 835 行 runner、317 行 `windows_audit`、270 行 `windows_job` 与 112 行 `windows_conpty`，没有新增第二 backend 或 compat 层。`npm run test:rust:related -- lime-rs/crates/tool-runtime/src/execution_process/windows.rs lime-rs/crates/tool-runtime/src/execution_process/windows_audit.rs lime-rs/crates/tool-runtime/src/execution_process/windows_job.rs lime-rs/crates/tool-runtime/src/execution_process/windows_conpty.rs lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 退出码 0，相关 workspace 与 `tool-runtime` 364/364 通过；evidence/parser + Renderer 10/10、contracts 299 checks、scripts/docs/legacy governance、rustfmt 与 diff check 均通过。macOS collector 已刷新为 schema v2 `platform=darwin/result=evidence-pending/failedStage=windows-runner`，七项 required case 全部 missing；MSVC 编译、真机矩阵和 firewall/WFP enforcement 仍未关闭，计划保持 `active`。
- 2026-08-26：复核 C-3 强网络隔离的 Codex current 实现边界。可交付的最小链必须同时包含 elevated setup helper、独立 offline/online 本地账户、DPAPI 保护的凭证与版本化 setup marker、runner 以 sandbox account token 启动、ACL 对账户与短生命周期 capability SID 的组合授权、按 offline account SID 安装的 persistent firewall/WFP filters、readiness 对 marker/账户/规则的运行时校验，以及真实 Windows/MSVC evidence。Lime 当前 runner 仍使用登录用户的 restricted token；只移植 `ALE_USER_ID` WFP filter 会命中主用户进程，无法证明只隔离 sandbox child，因此禁止把单独 WFP 调用或 `Planned/enforced=false` 文案改写当作完成。下一实现切片先在 `tool-runtime` 建立 setup artifact/account owner 与独立 setup binary，App Server 只负责编排既有 JSON-RPC setup lifecycle。
- 2026-08-26：复核 DeepSWE/Pier 证据前置条件。coding slice 固定 source commit `435ee89ec2f2e2289f33b0da4f992f0b7b7266b9`、`datacurve-pier==0.3.1`，`release-20` preflight 为 20 tasks；Desktop preflight 为 5 tasks，二者均通过。当前主机 `command -v docker` 失败，运行时 prerequisite 为 `container=spawnSync docker ENOENT`，因此没有启动模型回合、网络 Provider 或 verifier，也没有生成 `reward.json`、`ctrf.json` 与 patch SHA；状态继续为 `evidence-pending`，不得用 43/43 harness 回归或 preflight 冒充 verifier 通过。
- 2026-08-26：完成 C-3 setup artifact/account readiness 前置 owner。新增 `tool-runtime::windows_setup`，以 Lime AgentRoot 下的 `.sandbox/setup_marker.json` 与 `.sandbox-secrets/sandbox_users.json` 作为唯一版本化事实源，固定 `LimeSandboxOffline`/`LimeSandboxOnline`，拒绝缺失、未知字段、版本/账户漂移、无效/空/异常大 DPAPI blob；默认 Windows probe 进一步执行 machine-scope `CryptUnprotectData` 与 `LookupAccountNameW` SID 解析。App Server `windowsSandbox/readiness|setupStart` 只消费该 inspection，任何失败继续 `updateRequired`/fail closed。Rust related 反向依赖链通过，`tool-runtime` 368/368；artifact 定向 6/6、contracts 299 checks、scripts/release/docs governance、rustfmt 与 `git diff --check` 通过。Windows-only DPAPI/account API 尚未在 MSVC 编译运行；elevated helper、账户创建、runner account token、ACL 组合授权、WFP install/probe 与真机 evidence 仍未完成，因此 `SandboxBackendStatus::Planned/enforced=false` 不变，下一刀进入独立 setup helper 与打包边界。
- 2026-08-26：继续完成 C-3 setup/runtime/资源切片。新增独立 `windows-sandbox-setup` helper：创建或更新 `LimeSandboxOffline`/`LimeSandboxOnline` 本地账户，生成随机密码并以 DPAPI `LOCAL_MACHINE` 保护，原子写入 `.sandbox/setup_marker.json` 与 `.sandbox-secrets/sandbox_users.json`；`.sandbox` 与 `.sandbox-secrets` 通过 bounded `icacls` 关闭继承并只授权 owner、SYSTEM、Administrators，ACL 失败不会提交成功 marker。helper 纳入 app-server sidecar 成组构建、Electron Windows resource 与 release manifest/verifier（`windowsSandboxSetupSha256`）；App Server `windowsSandbox/setupStart` 的 Elevated 模式走固定 PowerShell `Start-Process -Verb RunAs -Wait`，Unelevated 模式直启，均有 120 秒超时和 bounded 输出。Windows runner 已从 DPAPI 读取 online 凭证，经 `LogonUserW` 再创建 restricted token；WFP 规则改为按账户稳定派生 GUID，setup 只安装并 readiness 验证 offline deny filters。资源/sidecar Vitest 45/45、`npm run test:contracts` 299 checks、tool-runtime 371/371、rustfmt 与 `git diff --check` 通过；裸 cargo check 仍被既有 `rusty_v8 v150.4.0` arm64 archive HTTP 404 阻塞，Windows/MSVC API 编译、UAC/NetUser/WFP/ACL/LogonUser 真机 evidence 尚未获得。生产仍保持 `SandboxBackendStatus::Planned/enforced=false`，无网络授权请求继续 pre-spawn fail closed。
- 2026-08-26：本轮继续完成 Windows current owner 的安全边界复验。Rust 定向测试 `npm run test:rust:unit -- -p tool-runtime` 为 `372 passed, 0 failed`；Windows setup、evidence parser、sidecar、资源 verifier、Squirrel 脚本与 Renderer 定向 Vitest 合计 `62 passed, 0 failed`；`npm run test:contracts` 完成 `299 checks`，`cargo fmt --manifest-path "lime-rs/Cargo.toml" --all -- --check` 与 `git diff --check` 通过。Windows evidence collector 在 macOS 上以退出码 `1` fail closed，生成 `.lime/qc/windows-restricted-execution/summary.json`：`schemaVersion=windows-restricted-execution-evidence-v2`、`platform=darwin`、`result=evidence-pending`、`failedStage=windows-runner`，七项 required case 全部 `missing`。本轮没有改变 `SandboxBackendStatus::Planned/enforced=false`，也没有把本机结果冒充 Windows/MSVC、live provider、Pier/DeepSWE verifier 或正式 package 证据；计划仍为 `active`、`44/46 (95.7%)`。
- 2026-08-26：补齐本轮统一门禁回归。`npm run verify:local` 首次在 smart Vitest 第 23 批暴露 workflow 顺序断言仍查找旧 sidecar 步骤名；将测试断言对齐当前 `Build Windows app-server, code-mode-host, and sandbox setup sidecars` 后，单测 `scripts/prepare-sherpa-onnx-runtime.test.mjs` 为 `13/13`，`npm test -- --resume` 从第 23 批续跑至第 118 批全部通过。使用仓库既有校验 V8 archive/binding 注入后，完整 `npm run test:rust` 退出码 `0`；最新 `npm run verify:gui-smoke` 真实 Electron/preload/IPC/`app_server_handle_json_lines`/App Server JSON-RPC Shell-01 为 `21/21`，renderer `8,993 modules`，证据见 `.lime/qc/project-gates/standalone-shell-01-20260826040827-13846/shell-01-electron-smoke/summary.json`，mock/console/page/invoke/preload/crash/unresponsive 均为 `0`。本轮仅修复测试事实断言并补验证，不改变 Windows readiness 或外部证据分类；计划仍为 `active`、`44/46 (95.7%)`。
- 2026-08-26：统一门禁收尾治理复验通过：`npm run governance:legacy-report` 扫描 2063 文件，零引用候选、分类漂移和边界违规均为 `0`；`npm run governance:scripts` 与 `npm run docs:boundary` 通过，`git diff --check` 通过。工作树未执行提交、推送、重置或分支操作。
- 2026-08-26：继续对照 Codex current `windows-sandbox-rs::token` 完成 C-3 restricting identity 收口。Lime 从 `LogonUserW` base token 读取实际 `TokenUser` SID，并按 capability、account user、logon、Everyone 顺序加入 `CreateRestrictedToken` restricting SID；account user SID 明确不进入 `TokenDefaultDacl`，避免 identity 被当成 capability。同步收敛 active 计划中的重复/过时 Firewall/WFP 状态。验证：`npm run test:rust:related -- lime-rs/crates/tool-runtime/src/execution_process/windows.rs lime-rs/crates/tool-runtime/src/windows_firewall.rs lime-rs/crates/tool-runtime/src/windows_wfp.rs lime-rs/crates/tool-runtime/src/windows_setup.rs lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 退出码 `0`，`tool-runtime` `373/373`；evidence Vitest `7/7`；`npm run test:contracts` `299 checks`；`docs:boundary`、`governance:scripts`、`governance:legacy-report`（2063 files、0/0/0）、rustfmt 与 `git diff --check` 均通过。macOS collector 以退出码 `1` 刷新 schema v3 evidence，明确记录 `platform=darwin`、`result=evidence-pending`、`failedStage=windows-runner` 和七项全部 missing。本机未安装 Windows/MSVC target，Windows-only `TokenUser`/COM/WFP/ConPTY API 编译和七项真机矩阵仍无证据，计划保持 `active`、`44/46 (95.7%)` 与 `Planned/enforced=false`。
- 2026-08-26：继续对照 Codex current `windows-sandbox-rs::setup_main::sandbox_users` 与 write-root ACL 语义，关闭 restricted token 双重访问检查缺口。`tool-runtime::windows_setup` 现在幂等创建 `LimeSandboxUsers` 本地组，将 offline/online 账户加入组；默认 Windows readiness 与每次 runner 启动均验证组 SID 和目标账户成员关系，组缺失、成员漂移或 NetAPI 错误 fail closed。`windows_acl` 将 cwd/显式 permission 拆成 read/write/write-deny/access-deny plan：普通 token 与 restricted token 两侧分别由 sandbox group 和短生命周期 capability SID 获得 read/write ACE，metadata/read-only carveout 只拒绝 capability 写，显式 `Deny` 同时拒绝 group/capability 全部访问。lease 不再通过 `/remove:g` 删除稳定主体 ACE，而是在每个路径第一次修改前保存原始 DACL security descriptor，终态逆序恢复。Windows integration matrix 在 workspace process 存活时断言新增 SID 同时包含 sandbox group 与另一条 capability SID，并继续覆盖 online/offline identity、workspace write、metadata/external deny 和全部目标原始 SDDL rollback。本机 `npm run test:rust:related -- ...` 退出码 `0`，反向依赖链与 `tool-runtime 373/373` 通过；Windows-only NetAPI/GetFileSecurityW/SetFileSecurityW 与增强矩阵仍必须由真实 MSVC runner 编译执行，因此 checklist 保持 `44/46 (95.7%)`、状态 `active`、readiness `Planned/enforced=false`。
- 2026-08-26：收口 Windows setup 文件规模与职责边界。将本地账户创建/更新、本地组与成员关系、组 SID/readiness 校验及 account SID 解析从 1,086 行 `windows_setup.rs` 原样迁入唯一 current 子 owner `windows_setup/accounts.rs`；主编排文件降至 817 行，子模块 281 行，没有新增 backend、compat API 或行为分支。`npm run test:rust:related -- ...` 退出码 `0`，反向依赖链与 `tool-runtime 373/373` 通过；`npm run test:contracts` 完成 App Server client `299 checks` 及 command/harness/modality/scripts/release/docs guards；`npm run governance:legacy-report` 扫描 2063 文件且零引用候选、分类漂移、边界违规均为 `0`；evidence parser `7/7`，rustfmt 与 `git diff --check` 通过。macOS collector 继续以退出码 `1` 生成 schema v3 `evidence-pending`，七项全部 missing。该拆分只消除仓库 1,000 行硬约束，不改变安全完成度：真实 Windows/MSVC restricted matrix、live provider/Pier/DeepSWE verifier artifacts 与覆盖当前工作树的正式 notarized/package 仍未完成，计划保持 `active`、`44/46 (95.7%)` 与 `SandboxBackendStatus::Planned/enforced=false`。
- 2026-08-26：继续 C-3，删除已由真机证据判定无效的 `CreateProcessWithTokenW` fallback、suspended/resume 与后置 Job 分配，改为 `CreateProcessWithLogonW -> sandbox-account runner -> current token 派生 restricted token -> CreateProcessAsUserW + STARTUPINFOEX` 唯一 current 链；有界双 named-pipe framing 承接 spawn/stdin/close/resize/terminate/output/exit/error，pipe ACL 只允许目标 account SID且连接后校验 runner PID，pipe 与 ConPTY 均通过 job-list attribute 原子入 Job，宿主 reaper 持有控制 pipe 与 ACL lease 到 runner 退出。`windows-sandbox-runner.exe` 已进入 dev/Quality 成组构建、Electron Windows resource、release manifest `windowsSandboxRunnerSha256` 和 packaged verifier；Everyone-write case 的 `icacls` 输出改为进程内捕获，避免污染 evidence parser 的稳定 test 行。本地 `cargo fmt --check`、Prettier、`git diff --check`、packaging/sidecar/evidence 定向 Vitest `66/66`、Rust related 反向依赖链、`tool-runtime 373/373`、`app-server-daemon 38/38`、contracts `299 checks`、scripts/release/docs/legacy governance 全部通过，legacy 扫描仍为 2063 files、0/0/0。当前主机未安装 Windows target，因此这些结果不替代 MSVC 编译和 schema v3 七项真机矩阵；计划继续保持 `active`、`44/46 (95.7%)` 与 `Planned/enforced=false`，下一刀是推送该窄提交并监控 Windows Quality evidence。
- 2026-08-26：Windows Quality run `32950744293`（SHA `3ca895af0`）已取得 schema v3 artifact `9600137436`，但在 setup binary 的 MSVC 编译阶段失败，七项尚未执行。失败收敛为单一 Windows-only import：`windows_runner_child.rs` 从 `Win32::Foundation` 导入 `EqualSid`，而 `windows-sys 0.52` 的 owner 是 `Win32::Security`；同时报告本轮 `windows.rs` 的两个 unused import。已按实际模块归属修正并准备最小重跑；该 run 不改变 `44/46 (95.7%)` 或 `Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32951556136`（SHA `dfc05e1e1`）已通过 setup 并完成 MSVC 编译，schema v3 artifact `9600453484` 首次执行完整七项矩阵：Everyone-write audit 通过，其余六项在 runner transport 建立阶段共同失败，错误均为 `SetNamedPipeHandleState(runner pipe): ERROR_ACCESS_DENIED`。根因是 Lime 为保留 15 秒有界连接而以 `PIPE_NOWAIT` 创建 pipe，连接后切换 `PIPE_WAIT`；output server pipe 使用 `PIPE_ACCESS_INBOUND` 时只有 `GENERIC_READ`，不满足 Windows 对 `SetNamedPipeHandleState` 的 `GENERIC_WRITE` 或 `GENERIC_READ + FILE_WRITE_ATTRIBUTES` 合同。已将 output server handle 收敛为 duplex access，runner 仍只请求 write access，account-only DACL、随机 pipe name 与 client PID 校验不变；该 run 为 `1/7` 失败证据，继续保持 `44/46 (95.7%)` 与 `Planned/enforced=false`，待下一轮真机矩阵验证。
- 2026-08-26：Windows Quality run `32952578959`（SHA `315c1cd79`）确认 runner transport 已越过 pipe state 切换，setup/MSVC 编译继续通过；schema v3 artifact 的七项矩阵仍为 `1/7`，六个 restricted child 均已 spawn 但在产生输出前以 code `1` 退出。对照 Codex current command runner 后确认 Lime 漏掉 restricted PowerShell/stdio 初始化所需的 NUL device ACL；Codex 对 capability SID 授权，而 Lime capability 是每回合随机 UUID，直接照搬会在全局设备 DACL 永久堆积短生命周期 ACE。当前修复改由 elevated setup 一次性向两个稳定 sandbox account SID 授予同等 NUL read/write/execute 权限，并在 readiness 对 effective rights 做 read-back；account SID 仍只作为 restricting identity，不进入 `TokenDefaultDacl`，workspace capability 边界不放宽。该 run 不改变 `44/46 (95.7%)` 或 `Planned/enforced=false`，待真机重跑验证此根因假设。
- 2026-08-26：Windows Quality run `32954904978`（SHA `5eebd985f`）验证稳定 sandbox account 的 NUL ACL 仍不足：setup/MSVC 编译通过，七项矩阵为 `1/7`，六个 restricted child 均 spawn 后立即以 exit code `1` 结束且无输出。继续对齐 Codex current：新增 `windows_null::NullDeviceLease`，用 `Local\\LimeSandboxNullDeviceAcl` named mutex 串行授予/撤销当前 capability SID 的 NUL read/write/execute ACE，lease 在 child/descendant 完成后回收；普通 pipe child 同时移除强制 `CREATE_NO_WINDOW`，恢复常规命令 inherited-console 语义。macOS 定向 related Rust `373/373`、fmt/diff check 通过；Windows-only 编译与七项真机证据待新 SHA 重跑，计划仍保持 `44/46 (95.7%)`、`active`、`Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32957046834`（SHA `e147a8731`）完成 setup/MSVC 编译，但六项 restricted case 在 child spawn 前统一返回 `CreateFileW(Windows sandbox NUL): ERROR_ACCESS_DENIED`，七项仍为 `1/7`。该证据证明低权限 sandbox-account runner 不能持有 NUL 的 `WRITE_DAC`；修复将 capability NUL lease 移至宿主，在 `CreateProcessWithLogonW` 前安装 ACE，并与 workspace ACL lease 一起由 supervisor/reaper 持有到 runner 及 descendants 退出，sandbox account 不获得全局 DACL 管理权。定向 related Rust `373/373`、fmt/diff check 通过；待下一轮 Windows schema v3 真机重跑，完成度/readiness 不变。
- 2026-08-26：Windows Quality run `32957785741`（SHA `a2a6caa85`，artifact `9602756033`）在 setup 阶段编译 `tool-runtime` 失败，七项矩阵未执行。MSVC `E0277` 明确指向 `NullDeviceLease` 持有 `LocalSid(*mut c_void)`，因此无法跨 supervisor/reaper 线程移动；修复为 lease 仅保存 capability SID 字符串，安装/撤销 ACE 时在当前线程短暂解析 `LocalSid`，不添加 `unsafe impl Send`。同时移除日志暴露的 Windows-only unused import；本机 related Rust 反向依赖链与 `tool-runtime 373/373`、rustfmt、`git diff --check` 通过。该 run 继续是 `failedStage=setup`、矩阵 `0/7`，不改变 `44/46 (95.7%)` 或 `Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32958651645`（SHA `37f385db6`，artifact `9603104216`）已通过 setup 和 MSVC 编译，schema v3 七项矩阵完整执行为 `1/7`：Everyone-write audit 通过，其余六项 restricted child 仍在无输出前 exit `1`，terminate 场景因 runner 提前结束返回 `ControlClosed`。该 run 证明 NUL lease 的 `Send` 编译问题已关闭，但 NUL ACE 未解决 child 启动。继续对照 Codex `setup_main::sandbox_users` 发现 Lime 遗漏将 sandbox account 加入 Windows 内置 `Users`（SID `S-1-5-32-545`）；当前修复对本地化组名执行 setup 幂等加入、readiness 与每次 runner 启动 read-back，避免子进程缺少仅授权给内置 Users 的系统资源访问。完成度继续为 `44/46 (95.7%)`，readiness 继续 `Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32960057901`（SHA `f1bec2328`，artifact `9603664649`）再次通过 setup/MSVC 编译并完整执行 schema v3，但矩阵仍为 `1/7`；六项 restricted child 继续无有效输出 exit `1`，说明内置 `Users` 成员身份已正确写入/read-back，却不是唯一缺口。继续逐段对照 Codex `gather_read_roots -> run_setup_refresh -> apply_read_acls` 后确认 Lime 尚未刷新 `C:\Windows`、Program Files/ProgramData 与当前用户 profile 非敏感子目录的默认 RX ACL；这会让已创建的 restricted PowerShell 在加载 DLL、模块或 AppData 时静默失败。当前修复将该行为收归 Windows setup、补 read-back 并提升 setup marker version 强制旧安装重跑；完成度/readiness 不变。
- 2026-08-26：Windows Quality run `32961870012`（SHA `740d5b61b`，artifact `9604315141`）证明默认平台/profile RX ACL 已通过 setup/read-back 与 MSVC 编译，但 schema v3 仍为 `1/7`；六项 PowerShell child 全部在首条输出前 exit `1`，因此 read roots 不是最后一个缺口。继续对照 Codex `PermissionProfile::workspace_write -> FileSystemSpecialPath::Tmpdir -> windows_temp_env_roots` 后确认 Lime ACL plan 只给 cwd/显式 grant 可写，遗漏 child 继承的 `%TEMP%/%TMP%`；sandbox account 对宿主 Temp 只有 RX，符合 PowerShell 初始化阶段无输出失败。当前修复把已过滤 child environment 中的 Windows Temp 根纳入 workspace-write ACL lease，保持原始 DACL rollback、metadata deny、完成度 `44/46 (95.7%)` 与 `Planned/enforced=false` 不变。
- 2026-08-26：Windows Quality run `32963357155`（SHA `9af4c627a`，artifact `9604867800`）验证 `%TEMP%/%TMP%` workspace-write ACL lease 已生效，schema v3 从 `1/7` 提升至 `3/7`：`restricted_conpty_supports_stdin_resize_and_combined_output`、`restricted_execution_bounds_large_output`、`world_writable_audit_reports_everyone_write_acl` 通过；普通 pipe 的 workspace write、offline network、allowlisted stdin 与 terminate 仍失败，均表现为 PowerShell 在首条输出前 exit `1`，terminate 因 runner 已结束返回 `ControlClosed`。继续对照 Codex `windows-sandbox-rs::desktop::LaunchDesktop` 与 `process::spawn_process_with_pipes` 后确认 Lime 只填 `Winsta0\\Default` 字符串，未创建并授权 sandbox logon SID 的 desktop；当前修复新增 `windows_desktop::LaunchDesktop`，按 child token 的 logon SID 创建/授权短生命周期 desktop，并让 pipe/ConPTY 的 `STARTUPINFOEX` 复用同一 desktop，child/descendants 完成后再释放。新增 `windows-sys` 的 GDI/StationsAndDesktops features；本机 `test:rust:related` `373/373`、`cargo fmt --check`、`git diff --check` 通过，但无 Windows target，下一轮必须由 MSVC runner 验证，完成度继续 `44/46 (95.7%)`、readiness 继续 `Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32965197227`（SHA `07b04bfcb`，artifact `9605628837`）已通过 elevated setup 和 MSVC 编译，schema v3 七项矩阵完整执行仍为 `3/7`：ConPTY stdin/resize/combined output、bounded large output 和 Everyone-write audit 通过，workspace write、offline account/network block、allowlisted stdin 与 terminate/job cleanup 失败。该结果排除 private desktop 是四项失败的单一根因，但不证明该安全对齐无需保留。已收紧四个失败断言：workspace 在 child 提前终态时立即打印 snapshot，offline/stdin/large-output 打印终态 `retained_output`，terminate 在发送控制前记录早退状态；不新增矩阵项、不放宽 token/ACL/firewall。本机 `npm run test:rust:related -- lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 退出码 `0`，反向依赖链与 `tool-runtime 373/373` 通过；Windows-only 断言编译/执行待新的 MSVC run。完成度继续 `44/46 (95.7%)`，readiness 继续 `Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32966584306`（SHA `63e4de484`）的增强 schema v3 证据仍为 `3/7`，但已把四个失败的首个共同错误定位为 PowerShell 访问 cwd 时抛出 `UnauthorizedAccessException`；stdin 与 terminate 并未进入各自控制边界。对照 Codex `effective_write_roots_for_permissions` 只做 exact dedup 后确认，Lime 在 `9af4c627a` 引入的祖先折叠会在 cwd 位于 `%TEMP%` 时删掉 cwd 本身；向 Temp 祖先新增可继承 ACE 不能代替对已存在/受保护子目录的显式授权。当前修复保留 cwd 和 Temp 两个 exact root，并在任何 ACL 变更前捕获全部原始 DACL，以避免嵌套 root snapshot 被早先授权污染。`npm run test:rust:related -- lime-rs/crates/tool-runtime/src/execution_process/windows_acl.rs lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 退出码 `0`，反向依赖链与 `tool-runtime 373/373` 通过；rustfmt 与 `git diff --check` 通过。该修复不改变 token、firewall、deny carveout 或 readiness，Windows-only 单测与七项矩阵仍待 MSVC 验证。
- 2026-08-26：Windows Quality run `32967946457`（SHA `e6c658aaa`，artifact `9606571669`）通过 elevated setup 和 MSVC 编译，schema v3 仍为 `3/7`；四个失败继续由 PowerShell cmdlet 报 cwd `UnauthorizedAccessException`，证明 exact-root 保留不是充分修复。继续检查 current supervisor 后确认，`RunnerMessage::Exit` 会先完成用户可见终态，持有 ACL lease 的 runner reaper 随后才等待进程并 rollback；下一命令可在同一路径安装新 ACE 后，被上一命令的旧 snapshot 恢复覆盖，矩阵从首个 ConPTY/纯输出通过到后续 cwd 操作连续失败的顺序与该竞态一致。当前修复以进程内 registry 为 exact path 共享首个原始 DACL snapshot 和活跃 lease 引用计数，重叠执行只在最后一个 lease 退出时恢复；workspace 必需项新增连续 restricted `cmd.exe` native write -> PowerShell write 回归，并提前记录 active SDDL。`npm run test:rust:related -- lime-rs/crates/tool-runtime/src/execution_process/windows_acl.rs lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 退出码 `0`，反向依赖链与 `tool-runtime 373/373` 通过；Windows-only registry 与连续执行回归仍待 MSVC。readiness 继续保持 `Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32970595099`（SHA `844a2316a`，job `98183292205`，artifact `9607584715`）setup/MSVC 通过，schema v3 仍为 `3/7`。workspace 新增的连续 restricted `cmd.exe` 原生写入已成功，active SDDL 同时包含 `LimeSandboxUsers` 与短期 capability SID，说明 exact workspace write 与共享 lease 已实际生效；该 case 随后被“group SID 必须相对 baseline 新增”的诊断断言提前截断。stdin/offline/terminate 仍在 PowerShell cmdlet 初始化 cwd provider 时对 `C:\\Users\\RUNNER~1\\AppData\\Local\\Temp\\.tmp*` 抛 `UnauthorizedAccessException`。对照 Codex current `unified_exec` tests 后确认，其 sandbox command cwd 固定为仓库工作树，系统 Temp 仅承接 sandbox home；Lime evidence 把另一用户的私有 `%LOCALAPPDATA%\\Temp` 子目录同时当成产品 workspace，混入了宿主 profile ACL 边界。当前回归改为仓库工作树内隔离 fixture，并把普通组断言改为 active DACL 绝对包含、仅 capability 要求相对 baseline 新增；不放宽生产 ACL、token、firewall 或 readiness。`npm run test:rust:related -- lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 退出码 `0`，反向依赖链与 `tool-runtime 373/373` 通过，rustfmt 与 `git diff --check` 通过；Windows-only fixture 仍待 MSVC。
- 2026-08-26：Windows Quality run `32971998836`（SHA `7c0c0574b`，job `98187791909`，artifact `9608137451`）elevated setup 通过，但 Windows-only evidence test 在 MSVC 编译阶段因 terminate 场景的 `handle.wait()` 需要可变借用而报 `E0596`，七项矩阵未执行，schema v3 因此为 `0/7 missing`，不是运行时回退证据。已只把该场景 handle 恢复为 `mut`；其他只调用共享引用方法或把 ownership 交给 `run_to_terminal` 的 handle 保持不可变。下一步先做本地 related/fmt/diff 门禁，再推送窄提交并重跑 Windows Quality；计划保持 `active`、`44/46 (95.7%)` 与 `SandboxBackendStatus::Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32973017956`（SHA `b7842eccc`，job `98191056610`）通过 elevated setup 和 MSVC 编译，schema v3 artifact 首次达到 `6/7`：workspace write/metadata 与 external deny、bounded large output、allowlisted stdin、ConPTY、Everyone-write audit、terminate/job cleanup 全部通过。唯一失败的 offline 场景使用了正确的 `LimeSandboxOffline` identity，但随机 loopback TCP 仍连接成功并以 code `9` 退出。WFP 12 项与 Codex current 一样只补 ICMP/DNS/SMB，随机 TCP 归 Firewall owner；Lime 旧实现将 loopback 合并为 `protocol=ANY` 单规则，而 Codex current 使用独立 TCP/UDP loopback block。当前修复保留非 loopback 全协议规则，将 loopback 拆为 TCP/UDP 两条并对协议做 read-back；不引入尚无产品需求的 proxy-port allowlist，不改 token/ACL/WFP/readiness。`npm run test:rust:related -- lime-rs/crates/tool-runtime/src/windows_firewall.rs lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 已通过反向依赖链与 `tool-runtime 373/373`，rustfmt 和 `git diff --check` 通过；下一步重跑 Windows Quality。完成度仍为 `44/46 (95.7%)`，readiness 仍为 `Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32974377348`（SHA `f6e305025`，job `98195543766`）再次为 `6/7`。setup 明确安装 `firewall_rules=3`、`wfp_filters=12` 并通过 read-back，但 offline process 仍以正确 identity 成功连接随机 loopback TCP；因此 Codex 风格 Firewall TCP/UDP 拆分不是充分条件，规则存在也不能冒充 enforcement。当前在同一 `windows_wfp` owner 新增 V4/V6 两条 user-only ALE_AUTH_CONNECT 全流量 block filter，使 offline account 的任意 IP connect 在内核分类层 fail closed；原 12 条 ICMP/DNS/SMB filter 与 3 条 Firewall rule 保留为 defense in depth，online account 不匹配。`npm run test:rust:related -- lime-rs/crates/tool-runtime/src/windows_wfp.rs lime-rs/crates/tool-runtime/src/windows_firewall.rs lime-rs/crates/tool-runtime/tests/windows_restricted_execution.rs` 已通过反向依赖链与 `tool-runtime 373/373`，rustfmt、docs boundary 和窄写集 diff check 均通过。下一步推送窄提交，再用 Windows setup 的 `wfp_filters=14` read-back 和 schema v3 七项矩阵验证。计划仍为 `44/46 (95.7%)`、`active`、`Planned/enforced=false`。
- 2026-08-26：Windows Quality run `32975574520`（SHA `19e08daa2`，job `98199506057`，artifact `9609510419`，artifact digest `a9bcfdfe71aac4723aa7edb7040376ee69117908d449bdf6966a3d9c4508d7c5`）关闭 C-3。elevated setup 输出 `firewall_rules=3`、`wfp_filters=14`；schema `windows-restricted-execution-evidence-v3` 为 `result=pass`、`7 passed/0 failed`、`missing=[]`、`duplicates=[]`、`ignored=[]`、`unexpected=[]`。offline case 在正确 `LimeSandboxOffline` identity 下真实阻断 loopback TCP，其余 workspace/metadata/external、large output、stdin、ConPTY、Everyone-write audit 与 terminate/job cleanup 同轮通过。该矩阵实际启动 sandbox-account runner，构成 runner 的 Windows/MSVC 执行证据；Electron manifest/resource 定向回归 `32/32` 进一步锁定 `windowsSandboxRunnerSha256`，并对 runner 缺失或 digest 漂移 fail closed。`npm run test:contracts` 299 checks、release workflow、scripts/docs governance 通过。C-3 更新为 `Ready/enforced=true`，计划进度 `45/46 (97.8%)`；实际 setup/account/Firewall/WFP read-back 失败继续 `UpdateRequired`。同一 Quality run 的 Frontend Full 失败来自既有 DeepSWE fixture/git PATH 前置条件，与 Windows 安全矩阵无关；Windows job 后续 Sherpa prepare 仍在运行，不用于冒充 C-3 证据。
- 2026-08-26：readiness 提升后的 SHA `438e8e4eb16a3ebf39d2eec40db04fc9111aa20b` 已由 Quality run `32978840712` 的 Windows job `98210288038` 再次执行真实 MSVC restricted matrix；artifact `9610757889`（digest `sha256:b2a6ecb78b97f94d63e71a05cf5692a2db31df67dc7d855f652ce59bfb091cba`）复核为 schema `windows-restricted-execution-evidence-v3`、`platform=win32`、`result=pass`、`7 passed/0 failed`，matrix `missing/duplicates/failed/ignored/unexpected` 均为空，setup 仍为 `firewall_rules=3`、`wfp_filters=14`。Windows job 随后卡在无关 Sherpa prepare，不影响已上传的 restricted execution artifact；该证据确认 `Ready/enforced=true` 所在提交自身没有把 C-3 回退。
- 2026-08-26：修复 DeepSWE current-chain harness 的 v2 wire 漂移。`thread/start` 不再发送 retired `sessionId/appId/workspaceId/workingDir/businessObjectRef`，从 response 读取 canonical Thread/Session identity；`turn/start`、`thread/read`、`turn/interrupt` 统一使用 canonical `threadId/turnId`，provider budget/generation 通过 typed `additionalContext.metadata` lowering，旧 `runtimeRequest` 进入负向断言。拆出 provider-step evidence 测试后主测试由 1,033 行降至 948 行；core 为 1,408 行且本轮净删减，已登记再次扩展前必须按职责拆分。格式化后定向 Vitest `23/23`、`npm run test:contracts`（299 checks）、Prettier 与窄写集 `git diff --check` 通过。下一刀固定使用用户指定的 `agnes-ai / agnes-2.5-flash` 真实 route 运行 live turn，成功生成 patch 后再进入 Pier `0.3.1` verifier；不得再让 auto provider 选择离线 fixture。
- 2026-08-26：完成 DeepSWE harness 超限文件拆分。`deepswe-adapter-core.mjs` 从 1,408 行降至 984 行，provider event/evidence、failure classification 与基础 value helper 分别收归 `deepswe-provider-evidence.mjs`、`deepswe-failure.mjs`、`deepswe-value-utils.mjs`；stdio transport 从隔离 App Server canonical event log 读取 `provider.request.started/provider.step/provider.usage`，不修改产品协议，也不把 audit-only event 暴露给 GUI。拆分后定向 Vitest `26/26`、Prettier 与 diff check 通过；event reader 变更后的 contracts 仍需复验。
- 2026-08-26：以 `agnes-ai` provider（id `custom-79f1f42e-8172-45a3-bb6b-b9afd2d76df5`）和 `agnes-2.5-flash` 运行三次 live DeepSWE。`20260826T144300Z-*` 持续约 38 分钟并产生 15+ provider steps 与候选测试文件，但在 terminal 前人工中断，不能计成功或失败；`20260826T152159Z-*` 使用 `max_output_tokens=8192`、thinking 开启，第 7 次 sampling 因 Lime 60 秒首个用户可见输出 deadline 失败；`20260826T154010Z-*` 关闭 thinking 后首个 sampling 仍触发同一 deadline，两次均未产生 patch。对照 Codex current 后确认上游只有 provider stream idle timeout，没有固定 60 秒“首个用户可见文本”失败语义。
- 2026-08-26：DeepSWE CLI 新增显式 `--first-visible-output-timeout-ms`，沿既有 `additionalContext.metadata -> RuntimeRequest.metadata.harness.generation` 下沉到 current `agent-runtime` override；默认值保持 unset，避免测试 harness 形成新的产品配置 owner。parser、run context 与 exact lowering 回归已补齐，定向 Vitest `26/26`、`npm run test:contracts`（299 checks）、`npm run governance:scripts` 与 Prettier 通过。当前等待用户把截图中的连接地址、端口、认证方式及连接类型转成文本；本会话没有 `computer-use` skill 要求的 `node_repl`，直调只读 app-state 又被 macOS 以 `-1743` 拒绝，无法可靠 OCR。用户确认后已通过 Homebrew 安装 Docker CLI `29.7.2`；默认 context 指向 `unix:///var/run/docker.sock`，但本机没有 daemon/socket。Pier 仍固定 `0.3.1`；若截图提供现成 Docker endpoint，应直接配置该 context，不再重复安装本地 engine。
- 2026-08-27：已按用户指定连接改用本机 Colima Docker endpoint `unix:///Users/coso/.colima/default/docker.sock`；Docker CLI `29.7.2` 已连接 daemon `29.5.2`。宿主 CLI 未全局安装 Compose，Pier 通过仓库隔离的临时 `DOCKER_CONFIG=.lime/benchmark/tools/docker-cli-config` 与 `docker-cli-plugins/docker-compose` 使用 Colima guest Compose `v5.1.4`，不改全局 Docker 配置。固定镜像 `public.ecr.aws/d3j8x8q7/swe-bench-202605:kh7c2re7cvbseq7xz6samd1xr182y1dc-v1.1` 的本地 digest 为 `sha256:930ec9d5c14868da048c6cdd96a06dc394ec09b0b7b12a2cad2e63476a59c3e6`；截图连接字段阻塞关闭。
- 2026-08-27：继续使用显式 provider `custom-79f1f42e-8172-45a3-bb6b-b9afd2d76df5` / model `agnes-2.5-flash` 运行 live DeepSWE。`20260826T161154Z-*` 在 thinking/8192 下完成 21 个 provider step，第 13 次 sampling 首个可见输出约 208 秒，证明 300 秒 diagnostic override 必要，但第 22 次 sampling 重复推理约 30 分钟后人工中止；`20260826T165200Z-*` 关闭 thinking 后仍在 300 秒内无可见输出，`owner=model` 且 patch 为空；`20260826T165819Z-*` 在 thinking/4096 下完成 18 个 provider step，第 19 次请求无后续事件，90 分钟总超时且 cancel 未收口，`owner=budget`，产生 2,810-byte patch，SHA-256 `2a991154c5363ee8b9ddd0eb990cbe7b9436a159d4ea7a7425e40e85ce64aabb`。三轮都没有 `currentChain=completed`，不能计成功 evidence。
- 2026-08-27：Pier `0.3.1` 已对 `20260826T165819Z-happy-dom-abort-pending-body-reads` 的同一候选 patch 真实执行；容器回收后的 patch 与原 patch 的 `git patch-id --stable` 均为 `a97dd19bd73138d42b74ca7eda8033a32b4fae14`。`reward.json`、`ctrf.json`、`test-stdout.txt` 已落盘，结果 `reward=0`、`F2P=0/14`、`P2P=165/165`、`partial=0.9217877094972067`，证明 verifier/Compose/Colima 链路已打通，但候选实现失败，不能关闭 checklist。同期修复 verifier-only 状态覆盖：仅 `currentChain=completed` 可写 `verified`，timeout/failed/缺失统一保留为 `verified_with_product_failure`；DeepSWE 定向回归为 3 files、`27/27`，Prettier 通过。下一刀改跑结构面更集中的 `oxvg-structural-selector-preservation`，仍要求 live current-chain completed、非空 patch、Pier artifact 完整且 verifier 通过后才提升进度。
- 2026-08-27：修正 DeepSWE verifier 完成判定。`verifierCompletionStatus` 现在同时要求 `currentChain=completed`、无 `providerStepExhaustion`/预算耗尽信号、非空 patch 且 Pier `reward=1`；实时与 `--verifier-only` 路径共用该判定，Pier 结果额外记录标准化 reward。预算耗尽但 read model 仍返回 `completed` 的旧 Prometheus 证据将不再被误标为 `verified`；新增回归覆盖预算、终端消息、空 patch、reward=0/缺失，DeepSWE adapter `23/23`、benchmark/desktop/coding slice `21/21`、release-20 preflight `20/20` 通过。
- 2026-08-27：真实 live route `custom-79f1f42e-8172-45a3-bb6b-b9afd2d76df5` / `agnes-2.5-flash` 在 `csstree-shorthand-expansion-compression` 上启动，Colima/Pier 连接正常；current chain 真实产生 8 个 provider steps、371 个 item 事件后卡在 `Read(lib/__tests/lexer-match-property.js)` 的 `item.started`，约 15 分钟无完成事件。App Server worker 无活跃子命令、无 approval pending，终止自有 sidecar 后生成 `20260826T200141Z-csstree-shorthand-expansion-compression/adapter-result.json`，状态 `product_failed`、patch `0 bytes`、无 Pier artifacts；该 run 证明当前 blocker 收敛为 `Read` 工具执行/sidecar 收口，不计入 live 成功证据。

- 2026-08-27：复核 `oxvg-structural-selector-preservation` 两轮 live evidence：`20260826T204916Z-*` 在 84 个 provider step 后 wall timeout，产生 36,114-byte patch（SHA-256 `86ffa86553d9e80cbee15a9e904ccd359851ab72681c41d5027630a72f623557`）；`20260826T222503Z-*` 正确重放同一候选并继续 41 个 provider step，仍 wall timeout，产生 39,565-byte patch（SHA-256 `f2ef2a4153db9968c6ccb36cb411ab6a5b0f197c2a81cc991694378e0916e2e9`）。两轮都不是 `currentChain=completed`，且旧 Pier verifier-only 因临时 Compose resource 已回收而没有形成 reward/CTRF artifacts，不能计成功证据。
- 2026-08-27：完成 stdio 工具阻塞复盘和窄修复。`csstree` 日志中的 `Read` 不能单独证明根因；`oxvg` canonical event log 显示 `Read`/Grep 均正常终态，真正长期未收口的是模型重复发出的 `grep -rn ... ~/.cargo/registry/src`、`find ~/.cargo/registry/src` 等无界命令。active `exec_command` 在约 10 秒 yield 后已经返回 `session_id`/`waiting|output`，provider 可继续下一 sampling；新增真实 App Server 回归证明 250ms yield 后立即返回 active session，后续 `write_stdin` poll 能观察同一原始 call 的 terminal/exit 0。为避免同步文件解析占用 Tokio worker，本地 `Read` 移入可取消的 `spawn_blocking`；候选 continuation 指令前置并禁止无界 home/dependency registry/filesystem 扫描，只允许围绕现有 diff 或具体测试失败做定向检查。相关 Rust 反向依赖门禁通过：`agent-runtime 211/211`、`app-server 1719/1719`、`tool-runtime 373/373`，其余相关 crate 同轮通过；continuation Vitest `3/3` 通过。下一刀是在重建 App Server 后复用最新 `oxvg` 候选重跑 live current chain/Pier，不把 active session 的非终态 GUI item 伪装成进程完成。

## 6.1 本轮续跑记录

- 2026-08-27：继续修复 DeepSWE live 证据链。`tool-runtime::supervise_local_process` 在 direct child 退出后立即发布 terminal snapshot，stdout/stderr 只保留 100ms trailing-output grace，超时后 abort 未关闭 reader；unified exec 在 terminal 首次可见后等待同一 grace 再做 final drain。新增永不 EOF duplex reader/direct child `exit 7` 回归和 App Server `exec_command("exit 7")` 回归，要求 `success=false`、`observation.kind=terminal`、`process_active=false`、`exit_code=7`、空输出。受影响 Rust 反向依赖测试退出码 `0`（最后一组 `tool-runtime` 374/374），适配器 Vitest `25/25`，continuation Vitest `4/4`，contracts `299 checks`、脚本/文档边界均通过。continuation 失败名改为稳定顺序去重后再限制 12 项；无 `currentChain` 的失败 run 从 `run-context.continuation.provider` 恢复固定 route。使用仓库已有 sandbox V8 cache 重建 `target/debug/app-server` 成功；手工 canonical stdio initialize 返回 `appserver.v0`。真实 run `20260827T050504Z-oxvg-structural-selector-preservation` 使用同一 `49,660` bytes 候选和固定 route `custom-79f1f42e-8172-45a3-bb6b-b9afd2d76df5 / agnes-2.5-flash` 续跑，但同一 provider step 的第一条 `cargo test --package oxvg_optimiser` 已终态后，第二条直接 function call `cat crates/oxvg_optimiser/src/utils/structural_selector.rs` 只发出 `item.started`，没有 `updated/completed` 且没有 App Server 子进程；最终 sidecar 被 harness 以 `SIGKILL` 回收，adapter 状态为 `product_failed`、owner=`agent-runtime`。独立 Pier retry 超过 1800 秒且未生成新 reward，不能计成功或测试失败。进度保持 `45/46 (97.8%)`。
- 2026-08-27：为上述第二个直接调用补 `current_executor_starts_short_command_after_delayed_terminal_command` 回归：同一 `CurrentTurnToolExecutor`、session/thread/turn 中，第一条延迟终态 shell command 之后的第二条短命令必须在 2 秒内终态，且两次调用都到达 live execution gateway。定向 `lime-agent` 测试 `1/1` 通过。真实续跑 `20260827T060242Z-oxvg-structural-selector-preservation` 随后完成 current chain：`30` 个 provider steps、budget tokens `321972/500000`、无 exhaustion，工具多调用批次持续终态，并生成 `49,660` bytes patch（SHA-256 `67c027a8c220a387e417c40f01bf49a6d09963d5f8ac288f51323a05a62bb981`，候选 HEAD `ef46e91c81a869ee48717b569dde11f856e4e464`）。该证据关闭 live provider、current chain 和非空 patch 子缺口。
- 2026-08-27：同一 run 的 Pier `0.3.1`/Colima prerequisite 为 pass，但 verifier environment setup 立即失败：Pier 子进程执行 `docker compose --project-name ...` 时没有发现仓库隔离的 Compose plugin，Docker CLI 返回 `unknown flag: --project-name`；`pier-stdout.txt` 和 `pier-jobs/**/result.json` 已保存 RuntimeError，未生成 `reward.json`、`ctrf.json` 或 `test-stdout.txt`。这不是 verifier test failure，不能推导 reward。按用户要求结束多日续跑；本轮 harness/npm/node/临时 App Server 已自然退出，保留用户级既有 App Server，不再重跑 Pier、GUI smoke 或正式发布。状态改为 `stopped-with-known-gaps`，进度保持 `45/46 (97.8%)`。

## 7. 阻塞与避让

- 2026-09-05：Windows setup mode 语义完成闭环。`WorkspaceSandboxConfig.mode` 进入持久化配置与 runtime metadata；`LocalExecutionSandbox` 携带 `elevated/unelevated`，Windows tool-runtime 对两者共享 restricted-token runner。elevated 由 sandbox account + Firewall/WFP artifact 启动，unelevated 由当前用户启动 runner、在 runner 内基于当前用户 token 创建 restricted token，并拒绝 managed network，避免“unelevated 名义 + elevated 实际执行”。补充 config YAML、sandbox mode 解析与 App Server setup/readiness 定向测试；Windows 真机证据继续引用 `32975574520` 与 readiness 复验 `32978840712`，本机不宣称 Windows 执行通过。

- 当前工作树的浏览器/Electron/benchmark 文件存在未知并行改动，本计划不覆盖、不回滚、不删除。
- live provider/current chain、连续直接工具调用和非空 patch 已由 `20260827T060242Z-oxvg-structural-selector-preservation` 关闭。当前 verifier blocker 是 Pier 子进程没有继承/发现仓库隔离的 Docker Compose plugin，导致 environment setup 在测试前以 `unknown flag: --project-name` 失败；此外覆盖当前 SHA 的 notarized/正式 package 尚未执行。若未来恢复本计划，第一刀只需修复 Pier subprocess 的 Compose plugin 可见性并对现有 run 执行 verifier-only；只有 artifacts 完整且 `reward=1`，并补齐当前 SHA 正式分发后，才能标记 `46/46`。本轮按用户要求停止，不继续消耗 provider、Docker 或发布资源。
- Windows 强网络隔离已形成 elevated helper、独立 sandbox account、DPAPI/marker、account token、ACL、Firewall/WFP、readiness 与真机 evidence 全链。任何后续改动使 schema v3 矩阵或 packaged runner 守卫失败时，backend readiness 必须回退 fail closed。
- 裸 Cargo 若未注入 artifact 仍可能命中 `v8 v150.4.0` 的 `aarch64-apple-darwin` GitHub archive HTTP 404；仓库统一 Rust 入口现可从 `${TMPDIR}/lime-rusty-v8` 复用已校验 SHA-256 的 `ptrcomp_sandbox_release` archive/binding。本轮 App Server Windows sandbox 定向单测已实际复用该 artifact 并 4/4 通过；V8 缓存仍不能冒充 Windows/MSVC、Electron Gate B 或 live provider 证据。
- 当前数据卷约余 `70+ GiB`（2026-08-26 复核），足够保留现有缓存/证据并重建候选包；本轮未删除缓存或证据。当前工作树 Developer ID candidate 不是 notarized 正式包；已存在的 `1.135.0` 正式发布证据对应 SHA `0975674a`，不证明当前未提交改动已进入正式发布包。
